# 量化监测-股票：项目规则

## 1. 项目边界与文档路由

- 唯一真实项目根目录：`/Volumes/HermesSSD/AntigravityData/量化监测-股票`。不创建或混用重复项目副本。
- `AGENTS.md`：只保存本项目长期有效的技术、业务、数据、高风险和验证规则。
- `PRD.md`：当前产品和生产基线、仍有效的 P0/P1 约束与路线。
- `docs/股票监测助手V5.0升级规划书.md`：V5.0“主线雷达”目标、阶段和验收门槛。
- `NEXT_CHAT_HANDOFF.md`：项目已有且唯一的长期开发状态文件。不再创建 `PROJECT_STATUS.md`或其他同职能文件。
- 继续任务时先读本文件和 `NEXT_CHAT_HANDOFF.md` 顶部最新检查点，再定位 `PRD.md`/升级规划书的当前相关章节，最后用真实代码、Git、服务、数据结构和测试复核。不默认全文重读长文档。
- 判断“当前已实现什么”时，真实代码、运行和测试证据高于文档；判断“V5.0最终应实现什么”时，以升级规划书为主，同时保留 `PRD.md` 中未被取代的生产约束。

## 2. 产品定位与技术基线

- 产品是“准实时股票监测 + 强提醒 + 行业洞察 + AI 证据解释”系统，不是自动交易系统，不提供自动买卖、收益承诺或毫秒级行情。
- 前端：Next.js `16.2.10`、React `19.2.4`、TypeScript 5、ECharts 6、CSS Modules，位于 `stock-monitor/`。
- 后端：Python、FastAPI、Pydantic 2、APScheduler，位于 `backend/`。
- 数据：SQLite 为当前生产存储；腾讯财经、东方财富、AKShare、新浪财经、巨潮资讯、交易所和行业/指数官方来源按字段用途分工。
- AI：OpenAI 兼容 API，密钥、地址和模型只从服务端环境配置读取。
- 部署：前端使用 Vercel；后端是独立服务，当前由 macOS `launchd` 运行资产承载，不属于 Vercel 前端部署。
- 引入 PostgreSQL、Redis、DuckDB、Parquet、Polars、Dramatiq、pgvector 等新基础设施前，必须用真实瓶颈证明必要性，给出增量迁移和回退方案；现有 SQLite/pandas/已有模块能安全满足时优先增强而非重写。

## 3. 目录和架构边界

- `stock-monitor/src/app/`：Next.js App Router 页面和服务端 API 代理；处理该目录时同时遵循 `stock-monitor/AGENTS.md`，并以安装版 Next.js 的 `node_modules/next/dist/docs/` 为 API 依据。
- `backend/radar/`：V5.0 主线雷达的独立数据、规则、状态、仓储和 AI 模块。
- `backend/tests/`：后端正式自动测试；Mock/Fixture 只能出现在测试边界内。
- `backend/data/`：数据库、备份、验收证据和本地运行数据，默认不提交 Git；不得为了测试或排查默认读写生产 SQLite。
- `ops/launchd/`：FastAPI、ngrok 和雷达影子运行资产；`ops/health-monitor/` 是外部健康检查。
- 前端页面只负责编排，通用请求、格式化和类型收敛到共用目录；后端路由、数据源、规则、仓储和调度保持分层。
- V5.0 只在当前项目中增量接入，不新建重复工程；未经批准不破坏已上线页面、接口兼容性、提醒历史和生产数据。

## 4. 并行协作边界

- 本项目不设“默认只能一个子 Agent”的项目级限制。当任务包含两项以上互相独立的代码定位、真实数据核验、测试分析或审查时，允许按当前运行时并发上限使用多个子 Agent。
- 所有 Agent 共享同一真实工作树；并行修改必须事先划分不重叠文件或明确单一写入者，不得同时编辑同一文件。
- 只读搜索、日志分析、数据证据归纳和独立审查可并行；生产 SQLite、环境变量、`launchd`、调度开关、Git 和部署操作不得交给并行子 Agent 自主执行。
- 主 Agent 负责合并结论、复核共享工作树 diff 和运行最终验证；子 Agent 结论不能代替真实命令证据。

## 5. 本地端口与常用命令

固定本地入口：

- 前端：`http://localhost:4000`。
- 后端：`http://127.0.0.1:8001`。
- 前端服务端代理和后端自调用继续使用 `8001`；不用 `4001`、`8000`、`8002` 等端口临时绕过冲突。

启动：

```bash
cd backend
./venv/bin/python main.py

cd stock-monitor
npm run dev
```

后端验证：

```bash
cd backend
./venv/bin/python -m unittest discover -s tests
PYTHONPYCACHEPREFIX=/private/tmp/codex-pycache ./venv/bin/python -m py_compile market_calendar.py database.py main.py
./venv/bin/python -m pip check
```

前端验证：

```bash
cd stock-monitor
npx tsc --noEmit
npm run lint
npm run build
```

`npm run dev` 和 `npm run start` 已在 `package.json` 固定端口 `4000`。不安装或升级依赖，除非当前任务已单独批准。

`launchd` 只读检查：

```bash
ops/launchd/manage.sh validate
ops/launchd/manage.sh status
ops/launchd/manage.sh preflight
```

## 6. 股票业务和前端契约

- 首页默认股票代码为 `000725`（京东方A）。
- 自选/监测列表始终同时保存 `stockCode` 和 `stockName`，当前产品上限为 10 只；移出后不删除已有提醒和 AI 历史。
- A股、港股和 ETF 的交易规则、时间、数据覆盖和缺失语义分开；交易日历不永久写死在单一年份。
- 股票代码变化且存在旧 React 状态或 Next.js 路由缓存污染风险时，使用 `window.location.href` 强制刷新；普通页面导航使用 Next.js `Link`。
- 后端代码只多出 `sh`/`sz`/`hk`/`bj` 前缀时不重复刷新 URL。
- 切换 K 线周期只更新图表数据，不重新加载全页行情、新闻、行业和相关股票。
- `ReactECharts` 默认差量合并，不设置 `notMerge={true}`；例外必须说明并验证性能。
- 金额、百分比、日期和股票代码在进入 UI 前统一格式化；正常、缺失和失败分支使用稳定字段名和明确缺失语义。

## 7. 真实数据和证据底线

生产返回值只能是：

1. 带来源、原始时间和完整度的真实原始数据。
2. 由真实原始数据按明确、可版本化口径生成的计算数据。
3. 明确标注为模型解释或推断的 AI 输出。
4. `null`、空数组或明确的缺失/失败状态。

强制规则：

- 生产接口和页面禁止 Mock 新闻、虚构行情、硬编码财务数字、模拟资金流或随机数据。测试 Fixture 不得进入生产返回值。
- 真实 `0` 与缺失必须区分；不用净利润代替扣非净利润，不用当日涨跌幅推算20日涨跌幅、量比或资金流。
- 区分上游原始时间、本系统抓取时间和页面渲染时间；浏览器当前时间不得冒充数据时间。
- 事实性新闻、政策和公告尽量保存原文链接、来源和发布时间；无可靠原文时不标记“已验证”。
- 每个参与正式计算的新字段必须定义主源、备用源、单位/口径、原始时间、最大延迟、覆盖率、缺失语义、冲突策略、调用限制和本轮是否参与评分。
- 外部数据源必须有超时、错误分类和降级状态；失败时不静默展示上一只股票、过期值或伪造备用值。
- 主营与催化的正式关联只能来自官方分类、公司公告/年报/IR 或人工审核的版本化映射；新闻标题、同义词猜测、模糊包含和 AI 线索不得补齐正式关系。
- ETF 份额变化、申购赎回、成交活跃、折溢价、IOPV、跟踪误差和跟踪差异是不同口径，不得自动改写为“资金净流入”。

## 8. 刷新、调度和雷达规则

- 行情等高频接口使用时间戳和 `cache: 'no-store'`；公司资料、历史财务和公告按业务周期缓存。不得把行情、行业、ETF、龙头、公告、新闻和财务强制成同一刷新频率。
- 高频任务必须防重入、限制并发并记录开始/成功/失败/耗时；单机部署中同一调度任务只有一个有效实例。
- 交易门禁必须区分 A股/港股、盘前、连续交易、午休、收盘、周末和法定节假日；非交易时段在打开数据库和请求高频来源前跳过不必要任务。
- 市场环境、主线行业、ETF 观察池和三级龙头只由真实数据、确定性规则、版本化配置和状态机决定。
- AI 只能在正式规则结果和冻结证据包之后解释证据、反证、风险和失效条件；不得修改状态、分数、排名、优先级或证据等级。
- 新旧 AI 保持独立提示词、表、缓存、锁、任务、配置和前端状态；雷达 AI 关闭、失败、未配置或证据不足时，确定性规则和页面仍可用。
- 不同模块的正式门分别验收。现有提醒“20条/10个日期”、ETF 真实历史窗口与5个现场交易日、三级龙头状态和整体雷达影子观察不得互相替代。
- 规则或正式数据未通过自己的真实门槛时，保持影子/未就绪，不得用 Mock、旧快照、首版复制、AI 线索或其他模块通过结果补门。

## 9. 密钥、数据库和运行服务高风险边界

- `.env`、`.env.*`、SQLite、备份、日志、`backend/venv`、`node_modules`、`.next`、隧道运行文件和本机缓存不提交 Git；仓库只保留无真实值的 `.env.example`。
- `NEXT_PUBLIC_*` 会暴露给浏览器，不得保存密钥或后端 Token。后端私密 Token 只由 Next.js 服务端代理持有，直访敏感后端接口继续校验 Token。
- 当前用户已接受单人自用系统的公开网址访问风险；登录、会话、CSRF、用户权限和基于用户/会话的限流暂缓，不作为 V5.0 开发、影子运行或正式启用的前置门槛。
- 生产 SQLite `backend/data/stock_monitor.db` 是运行数据。修改表结构或业务数据前必须单独授权，先执行一致性备份并验证 SHA-256、迁移结果、完整性和回退路径；禁止直接覆盖现有库。
- 不默认将全市场逐证券分钟级原始快照全量写入 SQLite。新表、新存储或自动清理接管业务前，必须证明容量、保留周期、查询成本和回退可行。
- `ops/launchd/manage.sh` 中除 `validate`/`status`/`preflight` 外的命令会改变本机服务或雷达运行配置，例如安装、卸载、重载、启停雷达和回退，必须单独授权。未授权时不停止、重启或重载已有 `4000`/`8001`/ngrok 进程。
- `RADAR_ENABLED`、`RADAR_SHADOW_MODE`、行业/ETF/市场/龙头独立开关，以及管理型全市场重算、历史回放和 `RADAR_AI_MANUAL_ENABLED` 都是生产高风险配置；不因本地代码或测试就绪而自动打开。
- 部署、Vercel 环境变量、生产后端重启、生产调度和生产数据写入是彼此独立的授权阶段，不由“本地修改完成”自动推导。

## 10. 验证要求

- 文案、规则文档或无行为变化的低风险修改：检查目标文件、`git diff --check` 和实际 diff，不默认运行完整构建。
- 后端行为修改：先运行最相关测试和受影响模块的语法/导入检查；跨模块、数据契约、调度、数据库、AI 隔离或阶段收口时运行后端完整测试。
- 可能导入 `database.DB_PATH` 的完整测试必须先精确重定向到 `/private/tmp` 临时 SQLite 并初始化必要表；测试不读写生产 SQLite。
- 前端行为修改：运行 TypeScript、ESLint 和 Next.js build；涉及 UI 时补桌面端 16:9 页面检查，并检查浏览器控制台、空态、失败、过期和加载状态。
- 数据类修改至少覆盖真实 `0`、缺失、上游失败、过期/未来时间、无效证券、A股、港股和不适用分支。
- 雷达状态和正式门至少覆盖空榜、覆盖不足、缺失、过期、数据源失败、同股状态唯一、升级/降级/移出、滞回、冷却、任务防重入、功能开关和 AI 不修改状态。
- 部署或生产操作前额外检查密钥、`.env`、数据库、日志、大文件和无关改动，并按实际产品链路验证回退。
- 未运行或无法运行的检查必须明确记录原因；不用旧测试数量、旧日志或“应该可以”代替本轮证据。

## 11. 长期状态维护

- 只维护根目录 `NEXT_CHAT_HANDOFF.md`；最新检查点放在顶部，以后不新建阶段交接文件或 `PROJECT_STATUS.md`。
- 在重要功能完成、阶段结束、发现影响后续的长期问题，或准备切换会话时自动更新。
- 每个新检查点只记录：当前阶段/目标、稳定完成项、修改文件、新鲜验证证据、服务与 Git 状态、未解决门槛和下一步最多 3 项。不复制完整日志、聊天、思考过程或短期尝试。
- 更新前重新核对工作树、HEAD/远端差异、`4000`/`8001` 服务、当前阶段和验证输出。状态文件与真实状态冲突时，先以真实证据修正文件。
