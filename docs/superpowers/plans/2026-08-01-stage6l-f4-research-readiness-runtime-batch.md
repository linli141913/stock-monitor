# 阶段 6L-F4 研究审计批次编排闭环实施计划

**目标：** 保存同轮生成的冻结强类型研究组件，用完整E3条目组装F2，
再把可信审计映射零重算回挂F3。

**限制：** 只做纯内存和本地验证；不接生产运行时、数据库、调度、API、
页面、正式门禁、服务或Git操作。

## Task 1：失败测试

新增F4专项测试，先确认模块缺失，并覆盖：

- 组件证券、时点、类型、顺序和冻结性；
- ready与可信风险缺失；
- 组件及E3单股错配隔离；
- E3条目缺席和批次合同错误；
- E3顺序不能重排候选；
- 回挂时五类特征构造器零调用；
- 正式标志严格关闭。

## Task 2：最小实现

- 增强`leader_runtime_inputs.py`，在候选循环内同时保留冻结组件批次；
- 新增不可变审计回挂纯函数，不修改原Assembly；
- 新增叶子模块`leader_research_readiness_runtime_batch.py`；
- 严格校验Assembly、组件绑定和完整E3批次，再逐股调用现有F2；
- 只把F2 ready/partial可信映射回挂F3。

## Task 3：验证与交接

运行F4专项、F1-F4联合、全部龙头、Python语法和差异卫生检查。完整后端
回归在导入`database`前将默认生产SQLite路径重定向到`/private/tmp`。
最后只读核对4000/8001、阶段开关和Git状态，更新唯一交接文件。
