# 阶段6本地运行时接入包实施计划

1. 新增默认显式缺失研究提供器及合同测试。
2. 让单次编排在提供器全缺失时提前返回，并暴露稳定门禁原因。
3. 在现有市场影子任务回调中按候选计划、提供器、单次编排顺序接入。
4. 把阶段6仓储访问推迟到研究输入非全缺失之后，保持市场任务异常隔离。
5. 用临时SQLite覆盖关闭、缺失、提供器异常和已验证输入写影子四条路径。
6. 运行运行时专项、阶段6全量、完整后端回归、语法和差异卫生检查。
7. 只读核对固定服务与阶段5/6开关；不重启服务、不操作生产SQLite、不执行Git操作。
