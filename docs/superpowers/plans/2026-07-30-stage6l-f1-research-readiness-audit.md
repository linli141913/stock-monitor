# 阶段 6L-F1 候选研究证据完整度与首次否决审计实施计划

> **执行方式：** 使用测试驱动和主线程实现；子代理只做独立契约与规则审查。

**目标：** 新增纯内存研究审计，统一输出候选证据完整度、缺失项和首次否决。

**架构：** 新模块只消费阶段 6L 已有强类型结果，按固定顺序生成冻结逐项
审计和汇总结果。运行时接入、正式门禁、状态机和持久化均留在后续阶段。

**技术栈：** Python 标准库、现有 `backend/radar` 冻结 dataclass 和 unittest。

## 全局限制

- 不读取或写入生产 SQLite；
- 不新增依赖、迁移、环境变量、API、页面或调度；
- 不修改正式评分、`LeaderGateInput` 或状态机；
- 不执行服务重启、Git 暂存、提交、推送或部署。

## Task 1：先写失败测试

**文件：**

- 新增 `backend/tests/test_radar_leader_research_readiness_audit.py`

测试先覆盖：

- 七项固定顺序和完整输出；
- 正式首次否决与候选研究首次阻断的独立优先级；
- 真实 0 研究分不被当成缺失；
- 可交易性排除、仅预备、回流缺失和主营证伪；
- 风险投影缺失、身份/时点/合同/正式标志伪造；
- 可信负面结论仍属于证据完整，但不得冒充候选通过；
- 冻结结果、压缩证据和所有正式标志关闭。

先运行新测试，确认因 F1 模块不存在而失败。

## Task 2：最小纯内存实现

**文件：**

- 新增 `backend/radar/leader_research_readiness_audit.py`

实现冻结输入、逐项审计、汇总结果和
`build_leader_research_readiness_audit()`：

- 严格校验外层类型、证券、带时区时点和风险投影固定合同；
- 从 E3 单证券批次条目保留风险投影的就绪与非就绪语义；
- 从横截面结果只读取 `industry_strength` 与 `market_leadership`；
- 区分证据完整、研究阻断与正式门禁缺失，保留上游原因；
- 分别选择正式首次否决和研究首次阻断；
- 不使用正式分数阈值，所有正式标志固定为假。

## Task 3：验证、审查与交接

运行 F1 专项、6L-A 至 F1 联合、全部龙头测试、相关 Python 语法、敏感依赖
搜索和差异卫生检查。完整后端回归必须先把 `database.DB_PATH` 重定向到
`/private/tmp` 并初始化临时基础表，禁止打开生产 SQLite。

最后只读核对 4000/8001，更新唯一 `NEXT_CHAT_HANDOFF.md`。不执行 Git 或
服务操作。
