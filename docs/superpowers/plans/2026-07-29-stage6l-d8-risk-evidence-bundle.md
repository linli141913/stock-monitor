# 阶段 6L-D8 风险研究证据包实施计划

> 执行时遵循 `superpowers:executing-plans`，按测试驱动完成并在每个验证点复核结果。

**目标：** 只在内存中封装 D2-D7 当前研究证据链，并输出不可误解为正式可用的门禁差距审计。

**架构：** 新增纯函数模块 `leader_risk_evidence_bundle.py`。入口重新执行 D7 防止伪造，随后从已验证链提取压缩引用、生成稳定证据包 ID，并输出固定正式门禁差距。

**技术栈：** Python 标准库、现有 dataclass/Enum、unittest；不新增依赖。

---

## 任务 1：失败测试固定合同

**文件：**

- 新增 `backend/tests/test_radar_leader_risk_evidence_bundle.py`

覆盖：

- 合法 D2-D7 链生成不可变证据包；
- 同一输入重复构建结果完全一致；
- 伪造 D7 结果拒绝；
- D7 `stale` 和失败状态传播；
- `resolves`/`supersedes` 精确关系字段保留；
- 正式门禁差距完整且正式标志全部为假；
- 畸形输入稳定降级；
- `repr` 不泄露页文本、事件摘要、审核摘要、URL和标题。

先运行专项测试，确认因 D8 模块缺失而失败。

## 任务 2：最小纯函数实现

**文件：**

- 新增 `backend/radar/leader_risk_evidence_bundle.py`

实现：

- D7 重算及逐字段一致性检查；
- 当前 D5 工件和目标 D1 事件精确定位；
- D2-D7 压缩引用提取；
- 稳定证据包 ID；
- 固定正式门禁差距和禁用标志；
- 敏感文本不进入输入输出对象表示。

运行专项测试直到通过。

## 任务 3：回归与安全复核

运行：

- D1-D8 联合测试；
- 全部龙头测试；
- 后端完整测试；
- 新模块语法编译；
- `git diff --check` 和敏感依赖搜索。

只读复核 4000/8001 PID、HEAD 和变更文件。禁止数据库操作、服务重载、环境修改和 Git 写操作。

## 任务 4：唯一交接更新

**文件：**

- 修改 `NEXT_CHAT_HANDOFF.md`

记录 D8 完成内容、测试证据、正式门禁仍关闭和下一阶段建议。
