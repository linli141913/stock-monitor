# 阶段 6L-E3 风险候选投影批次组装器实施计划

## Task 1：失败测试

新增 `backend/tests/test_radar_leader_risk_candidate_projection_batch.py`，
覆盖：

- 全部就绪和压缩批次审计；
- 部分就绪及合法投影映射；
- 空批次与畸形合同；
- 重复证券整体拒绝；
- 映射证券身份冲突；
- 单条批次时点冲突；
- D9 伪造结果的单条隔离；
- E1 未预期异常的单条隔离；
- 冻结合同、隐藏输入和全部正式标志为假。

先运行新测试并确认因 E3 模块不存在而失败。

## Task 2：最小纯内存实现

新增 `backend/radar/leader_risk_candidate_projection_batch.py`：

- 冻结批次输入、条目、逐条结果和批次结果；
- 先执行批次级合同检查；
- 逐条调用 E1 并转换为稳定状态；
- 未预期异常只降级当前条目；
- 只把合法投影放入按证券映射；
- 输出压缩审计，不重复完整投影；
- 不直接调用 E2 运行时。

## Task 3：验证与交接

运行 E1-E3 专项、全部龙头测试、相关语法和敏感依赖检查。完整后端回归
必须在导入 `database` 后立即把 `DB_PATH` 重定向到 `/private/tmp` 并初始化
临时基础表。最后只读核对 4000/8001，更新唯一
`NEXT_CHAT_HANDOFF.md`。

不读取或写入生产 SQLite，不执行 Git 暂存、提交、推送、服务重启或部署。
