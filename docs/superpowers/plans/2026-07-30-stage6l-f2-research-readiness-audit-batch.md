# 阶段 6L-F2 多证券研究就绪审计批次实施计划

> **执行方式：** 主线程按测试驱动实现，三个只读子代理并行核对契约、
> 失败语义和测试矩阵，不让代理阻塞关键路径。

**目标：** 新增纯内存F2批次组装，按统一证券名单和批次时点逐股执行F1，
输出有序审计条目和只读可信审计映射。

**技术栈：** Python标准库、现有 `backend/radar` 冻结dataclass和
unittest。

## 全局限制

- 不读取或写入生产SQLite；
- 不连接网络、仓储、调度、API、页面、提醒或AI；
- 不新增依赖、迁移、环境变量或运行开关；
- 不修改正式评分、`LeaderGateInput`或状态机；
- 不执行服务操作或Git操作。

## Task 1：失败测试

新增
`backend/tests/test_radar_leader_research_readiness_audit_batch.py`，覆盖：

- 全部ready、ready与partial混合、全部blocked；
- 重复外层或内层证券整体拒绝；
- 单股载荷、身份和时点错误隔离；
- F1异常脱敏和伪造结果防御；
- 嵌套状态/证据/否决组合防伪和稳定原因码白名单；
- 空批次、无时区和错误容器；
- 等价时区、顺序稳定、只读映射和冻结合同；
- 所有正式标志关闭。

先运行专项测试，确认因F2模块不存在而失败。

## Task 2：最小批次实现

新增
`backend/radar/leader_research_readiness_audit_batch.py`：

- 冻结批次输入、条目、结果和批次状态；
- 在构造映射前检查重复证券；
- 逐条校验身份和UTC时点，调用F1并捕获异常；
- 防御性复核F1合同、七项顺序、计数和正式标志；
- 聚合批次状态与计数；
- 输出有序条目和不可变可信审计映射；
- 压缩证据中不重复完整F1内容。

## Task 3：验证与交接

运行F2专项、F1-F2联合、全部龙头测试、Python语法、敏感依赖搜索和差异
卫生检查。完整后端回归必须在导入业务数据库模块前拦截生产SQLite默认
路径，并重定向至 `/private/tmp` 临时库。

最后只读核对4000/8001和阶段开关，更新唯一
`NEXT_CHAT_HANDOFF.md`。不执行Git或服务操作。
