# 阶段 6L-F3 研究就绪审计运行时只读接入实施计划

> **执行方式：** 主线程按测试驱动实现，三个只读子代理并行核对运行时
> 契约、测试矩阵和依赖防御，不让代理阻塞关键路径。

**目标：** 把F2可信审计映射作为可选只读输入接入运行时候选
`researchFeatures`，保持全部正式能力关闭。

**技术栈：** Python标准库、现有冻结dataclass、运行时组装器和unittest。

## 全局限制

- 不读取或写入生产SQLite；
- 不连接网络、仓储、调度、API、页面、提醒或AI；
- 不重算F1/F2，不新增依赖、迁移、环境变量或运行开关；
- 不修改正式评分、`LeaderGateInput`或状态机；
- 不执行服务操作或Git操作。

## Task 1：失败测试

修改 `backend/tests/test_radar_leader_runtime_inputs.py`，覆盖：

- 默认缺失且无副作用；
- F1 ready/partial压缩接入；
- 真实F2只读映射端到端消费且不重算F1/F2；
- 身份、无时区、错时点和等价时区；
- 合同、四个正式标志、blocked状态和嵌套语义伪造；
- 白名单外原因脱敏；
- 单股失败隔离和错误映射容器稳定降级；
- 运行时v3、候选排序、正式门禁和缺失前置保持不变。

先运行运行时专项，确认新增参数尚不存在并失败。

## Task 2：最小实现

修改：

- `backend/radar/leader_research_readiness_audit_batch.py`
- `backend/radar/leader_runtime_inputs.py`

实现：

- 将F2深层校验包装为公共只读守卫；
- 新增可选审计映射参数；
- 按候选逐股校验并输出压缩摘要；
- 错误映射容器和单股错误稳定降级；
- 运行时证据合同升级为v3；
- 所有正式字段继续固定关闭，不改变其他运行时结果。

## Task 3：验证与交接

运行F1-F3专项联合、全部龙头测试、Python语法、敏感依赖和差异卫生检查。
完整后端回归必须在导入 `database` 前精确拦截默认生产SQLite路径，并
重定向至 `/private/tmp` 临时库。

最后只读核对4000/8001及阶段开关，更新唯一
`NEXT_CHAT_HANDOFF.md`。不执行Git或服务操作。
