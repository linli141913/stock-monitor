# 阶段6真实研究提供器来源上下文实施计划

1. 只读复核历史、主营催化、可交易性和D8/D9风险的现有生产来源及严格合同。
2. 用失败测试定义冻结来源上下文、深拷贝、候选全集和批次错配语义。
3. 在默认研究提供器中只接受已验证上下文，继续生成显式缺失。
4. 在现有阶段6市场回调内从同轮行情、健康、主档和行业记录构建上下文，再调用一次提供器。
5. 验证默认缺失不触碰迁移5、提供器异常不影响市场任务、注入已验证输入仍只写analysis-only快照。
6. 运行运行时、阶段6龙头全量、完整后端、语法和差异卫生检查。
7. 更新唯一交接，明确下一步需要外部官方数据权限或合规数据服务决策。
