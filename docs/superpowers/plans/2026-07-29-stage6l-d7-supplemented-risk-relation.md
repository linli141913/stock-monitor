# 阶段 6L-D7 补录事实关系复核桥接实施计划

> 执行时遵循 `superpowers:executing-plans`，按测试驱动分步实施并在每个验证点复核结果。

**目标：** 仅在内存中把 D6 当前人工补录事实视图桥接到一次版本化关系审核，输出独立研究关系。

**架构：** 新增纯函数模块 `leader_risk_supplemented_relation.py`。入口先重算并核对 D6 视图，再验证人工审核及 D1 精确事件版本，最后生成 D7 专用不可正式使用关系对象。

**技术栈：** Python 标准库、现有 dataclass/Enum、unittest；不新增依赖。

---

## 任务 1：失败测试固定合同

**文件：**

- 新增 `backend/tests/test_radar_leader_risk_supplemented_relation.py`

覆盖：

- 合法 `resolves` 和 `supersedes`；
- D6 视图重算不一致；
- 非事实补录工件、无人工事实或已有关系；
- 错误事件、版本、文档、身份和时间；
- 依据事实不存在、重复或未引用人工事实；
- 类别所需案号/报告期不匹配；
- AI 审核、未来审核、过期审核；
- 异常嵌套值稳定降级；
- `repr` 不泄露正文、事件摘要和审核摘要。

先运行专项测试并确认因 D7 模块缺失而失败。

## 任务 2：最小纯函数实现

**文件：**

- 新增 `backend/radar/leader_risk_supplemented_relation.py`

实现：

- D6 重新执行和逐字段一致性检查；
- 当前事实补录工件及研究视图边界检查；
- 人工审核、时间、身份、精确事件版本验证；
- 合并事实和人工事实依据验证；
- `resolves`/`supersedes` 版本规则；
- D7 稳定关系 ID 和只读研究结果。

运行专项测试直到通过。

## 任务 3：回归与安全复核

运行：

- D1-D7 联合测试；
- 全部龙头测试；
- 后端完整测试；
- 新模块语法编译；
- `git diff --check`。

只读复核 4000/8001 PID、HEAD、变更文件及敏感边界。禁止数据库操作、服务重载、环境修改和 Git 写操作。

## 任务 4：唯一交接更新

**文件：**

- 修改 `NEXT_CHAT_HANDOFF.md`

记录 D7 已完成内容、测试证据、仍未启用边界和下一阶段建议。交接内容须以最终真实状态为准。
