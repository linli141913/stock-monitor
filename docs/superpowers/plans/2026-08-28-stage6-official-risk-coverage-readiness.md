# 阶段6官方风险覆盖证明就绪合同实施计划

> 按 `superpowers:test-driven-development` 逐项执行；每个行为先看到失败测试，再做最小实现。

**目标：** 建立发行人上市日至今官方风险覆盖证明的版本化证据合同与失败关闭边界；在正式覆盖查询政策未批准时绝不启门。

**架构：** 新增独立纯内存模块，只消费可信候选计划、现有官方生命周期和调用方提供的上市证据。政策审批必须精确命中代码内冻结注册表；当前注册表为空，因此只输出可审计的未批准结果，不生成 D1 覆盖证明。

**技术栈：** Python 3.9、dataclass、Enum、datetime、hashlib、urllib、unittest。

### Task 1：冻结合同与未批准主路径

- [x] 新增失败测试：合法生命周期、完整上市证据且无审批时返回 `policy_unapproved`。
- [x] 新增失败测试：伪造审批和字典审批不能令政策通过。
- [x] 最小实现合同、空审批注册表、生产者令牌和整批结果。
- [x] 运行专项测试确认通过。

### Task 2：上市证据和同轮身份校验

- [x] 新增失败测试：证据缺失、错序、重复、发行人错位。
- [x] 新增失败测试：未来上市日期、无时区/未来来源时间、非HTTPS或非官方域名、非法校验和。
- [x] 最小实现逐证券校验和稳定失败原因。
- [x] 运行专项测试确认通过。

### Task 3：生命周期重放和防篡改

- [x] 新增失败测试：错轮生命周期、手工构造生命周期和关系未完成对象被拒绝。
- [x] 新增失败测试：手工构造或替换覆盖结果的正式字段不能通过验证器。
- [x] 最小实现生命周期绑定、输出重放验证和脱敏证据投影。
- [x] 运行专项测试确认通过。

### Task 4：回归、静态检查与交接

- [x] 运行官方风险覆盖、生命周期、D1/D2/D8/D9相关测试。
- [x] 精确隔离生产 SQLite 后运行完整后端测试。
- [x] 运行 Python 编译、`pip check` 和 `git diff --check`。
- [x] 只更新根目录 `NEXT_CHAT_HANDOFF.md` 顶部最新检查点。
- [x] 复核 Git、服务只读状态和仍需交易窗口完成的现场门。
