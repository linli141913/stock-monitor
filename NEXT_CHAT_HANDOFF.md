# 股票监测助手 V5 当前续做检查点

> 保存时间：2026-08-16 09:55 CST
> 本文件只保存跨对话检查点，不代替真实代码、Git、服务、数据库结构和测试证据。

## 2026-08-16阶段5 ETF观察验收口径修订

- 经用户确认，阶段5 ETF取消“从启用日起现场等待20个交易日”的单一门槛，改为三项独立验收：冻结规则要求的20日或60日真实时点历史覆盖、连续5个真实A股交易日现场影子运行、六类异常场景自动测试。历史数据必须保留报告日、生效日和当时有效身份，当前名册或分类不得倒填过去。
- 新增独立`EtfObservationPolicy`、`EtfObservationEvidence`和`evaluate_observation_readiness`确定性合同；现场最少5日，异常场景固定为来源失败、数据过期、必需字段缺失、重复调度、身份漂移和非交易时段。60日盘中特征保留继续属于存储策略，不再与现场等待天数混为一个字段。
- 历史窗口、5日现场运行和异常测试不能相互替代；5日通过只证明运行链稳定，不证明ETF排名或预测价值。排名有效性仍由阶段9历史回放和阶段10正式启用验收判断，中期1/3/6个月观察继续独立记录。
- 本次修订只适用于阶段5 ETF。阶段3行业阈值校准、阶段10整体雷达影子门槛、阶段6龙头证据及提醒历史20条/10日门槛均未修改。ETF策略专项现为9项、全部ETF相关73项均通过；本批未读取或写入生产SQLite，未修改环境变量、服务、开关、迁移或依赖，也未执行Git暂存、提交、推送和部署。

下一步继续按TDD实现“龙头生产只读正式研究输入桥接器”，不因ETF观察口径缩短而放宽行业、龙头或雷达AI的真实证据门禁。

## 2026-08-15 manual-review-v2正式研究影响只读审查

- 现场安全状态保持不变：8001仍由PID 34061监听，阶段5 ETF与阶段6龙头开关为`true`，D8写入开关不存在即`false`，4000未监听；既有未提交工作树完整保留。本批未写生产SQLite、未改环境或开关、未调用外部AI、未停止/重启/重载服务，也未执行Git暂存、提交、推送或部署。
- 当前生产`RadarRuntime`仍默认调用`radar-leader-research-explicit-missing-provider-v1`，对候选全集显式返回`history`、`business_catalyst`、`tradability`和`risk_projection`四类缺失；`leader_research_source_admission.py`、`leader_formal_research_batch.py`和`sector_rule_readiness.py`目前只有纯内存合同与测试，没有生产运行时调用方。因此新增D8版本不会自动进入单轮研究编排或生成龙头快照。
- 最新真实D2审核批次为同一8月12日候选计划，共385只候选、1919份公告、385个公告证券身份和7类完整查询；类别分布为审计1024、业绩370、减持305、解禁130、诉讼53、监管28、立案9。生产只保存3份正文快照，其中只有603055的`cninfo:1225468495`有2个审核版本，002124诉讼公告和000096业绩公告尚无人工版本；当前人工审核覆盖为1/385只、1/1919份公告。
- 使用生产只读仓储重建`manual-review-v1 -> manual-review-v2`，得到2个连续D8证据包。以当前时点2026-08-15T15:13Z重算603055风险投影为`ready`且确有投影；代入旧候选计划时点2026-08-12T06:35Z则封闭为`source_unverified / risk_candidate_projection_future_evidence`，因为v2证据形成于8月15日。旧计划不得被事后证据回填，只有新的同轮候选计划才能合法消费v2。
- 运行`/api/radar/leaders`仍返回`state=not_ready`、`quality=unavailable`和`candidate_snapshot_missing`，没有历史成功快照；D2审核队列本身为`ready`且版本数为2，但正式状态仍关闭。按当前真实运行调用链，正式研究就绪为0/385；如果仅增加D8风险桥接，在新时点也最多先让603055的`risk_projection`一项具备输入，其余384只仍缺风险投影，而且所有候选仍缺历史、主营催化、可交易性和行业正式门。
- 龙头正式研究批次、风险生命周期与来源准入相关39项合同测试通过。一次性只读审查脚本已删除，`manual-review-v2`、生产库、运行服务和现有代码均未被本批修改。

下一批按TDD实现“龙头生产只读正式研究输入桥接器”：先冻结新的同轮候选计划身份和顺序，再把生产D8版本逐候选重放为与计划完全对齐的风险投影批次（无版本者明确`missing`），最后接入现有来源准入、研究就绪度和正式研究批次合同。第一版只输出逐项`ready/missing`和原因，不生成分数、梯队状态或状态迁移，也不把单一603055证据扩散到其他候选。

## 2026-08-15第二个真实D8版本受控保存与2/2验收

- 经用户明确授权后，写入前先对生产库执行`PRAGMA quick_check=ok`，并使用SQLite在线备份生成`backend/data/backups/stock_monitor-before-d8-v2-20260815-215336.db`。备份约1.4 GiB，独立复核`quick_check=ok`且D8版本数为1；如需数据级回退，必须另行授权停止后端并恢复整库，不得绕过D8追加式触发器直接删改版本行。
- 使用项目既有`ops/launchd/manage.sh enable-leader-d8-write`加载本地新代码并临时开启写入。首次重载遇到macOS `launchctl bootstrap`瞬时I/O错误，脚本自动恢复原运行资产和关闭状态；确认8001健康后第二次重载成功。受保护预检精确返回`ready`、`official_status_changed`、`manual-review-v2`、`submissionAllowed=true`和空原因码后，接口唯一一次保存返回`created=true`、`supersedesReviewVersion=manual-review-v1`、`reviewVersionCount=2`、`formalUsable=false`。
- 新版本保持同一官方目标公告和`resolves`关系，仅把上市流通日前提前写成`completed`的官方状态纠正为`active`；生产链现为`manual-review-v1(completed) -> manual-review-v2(active)`。写入后生产库再次复核`quick_check=ok`、外键问题0，未更新或删除首版。
- 保存后立即执行`disable-leader-d8-write`。首次关闭同样遇到瞬时I/O错误并自动恢复到写入开启资产；确认服务健康后第二次关闭成功。最终已安装LaunchAgent中阶段5 ETF与阶段6龙头开关均为`true`，D8写入项不存在即恢复`false`；8001由PID 34061监听，主机侧`/docs`返回200，ngrok保持运行。
- 运行接口已加载本批代码并完成只读验收：审核表单显示`writeEnabled=false / d8_manual_review_write_disabled`，下一版本为`manual-review-v3`；D8-D9重放诊断为`ready`，`reviewVersionCount=2`、`bundleCount=2`、`activeReviewVersion=manual-review-v2`、`materialChangeRequired=false`且原因码为空。既有`/api/radar/leaders`合同保持兼容，审核队列版本数为2，但`formalStateEnabled=false`、`formalUsableCount=0`，不能把这一个真实2/2版本链写成正式龙头状态已就绪。
- 本批未调用付费AI、未修改生产环境文件、迁移、依赖或数据库结构，未停止ngrok或执行Git暂存、提交、推送和远端部署。一次性提交脚本已删除；生产备份和LaunchAgent运行资产归档按回退要求保留。

下一批先只读复核`manual-review-v2`对D8-D9风险证据审计、龙头研究就绪度和正式研究批次的实际影响，明确剩余候选覆盖与正式规则缺口；不得因603055单一文档已经形成2/2版本链，就跳过其余385只候选、行业正式规则、可交易性、历史连续性和风险证据门禁。

## 2026-08-15第二个真实D8版本现场证据审查

- 经用户“继续”授权后，仅用SQLite只读URI核对生产D8上下文。现场仍只有1条`manual-review-v1`，对应603055台华新材、解禁类别、当前公告`cninfo:1225468495`（公告编号2026-054），人工关联的原公告为`cninfo:1220848442`（公告编号2024-072）。首版关系为`resolves`，目标事件时间为2026-08-10至2026-08-17，但在2026-08-13审核时已标记`officialStatus=completed`。
- 巨潮官方“解除限售”窄窗口查询只返回当前公告；为避免把关键词未命中写成覆盖证明，又枚举603055在2026-08-12至08-15的全部官方公告。接口报告共2条、`hasMore=false`：解锁上市公告`1225468495`及与本事件无关的回购股份股东名单公告`1225468480`。窗口内没有更正、延期、撤回或新的上市结果公告。
- 已下载两份巨潮官方PDF到`/private/tmp`并用Poppler渲染、`pypdf`提取后逐页核对。当前公告明确：30名激励对象共1,428,900股，解除限售条件已成就，股票上市流通日为2026-08-17，并引用2024-072首次授予结果公告。原公告确认首次授予登记日为2024-08-09、登记数量433万股及第二个解除限售期为登记满24个月后的首个交易日起。
- 用本地新预检代码直接消费生产只读仓储，分别构造两份草稿且未调用保存：完全复制首版稳定返回`missing / d8_review_version_preflight_material_change_missing`；仅将上市日前提前完成的`officialStatus=completed`纠正为`active`后，返回`ready / official_status_changed`。预检前后仓储版本数均为1，`formalUsable=false`、`stateTransitionAllowed=false`。
- 根据现有D1合同，只有`officialStatus=active`、已生效且未超过`effectiveUntil`的事件才是当前有效事件；因此2026-08-17上市流通日之前保持`active`、到期后再由时间门禁自然移出，比首版在08-13直接写`completed`更符合当前版本化语义。这是一个有官方日期依据的真实纠正候选，不是复制首版或换措辞补位。
- 本批只读生产SQLite、调用巨潮官方免费接口并下载官方PDF；未调用付费AI，未写库、未修改D8开关、环境变量、迁移或依赖，未停止/重启/重载服务，未执行Git暂存、提交或推送。临时PDF和渲染文件在检查后删除。

下一步已进入高风险写入边界：需用户单独明确授权后，先备份生产SQLite，再受控加载本地新代码、临时开启D8写入并将上述`active`状态纠正草稿保存为`manual-review-v2`；保存后必须重读D8-D9证据链、确认2/2版本可重放且仍不形成正式龙头状态。

## 2026-08-15第二个真实D8版本可用性前置审查工程

- 新增受服务端Token保护的纯只读`POST /api/radar/leaders/review-queue/review-version/preflight`及`radar-leader-review-version-preflight-v1`强类型合同。接口使用同一份D8草稿在内存中构造候选版本，不调用仓储保存，然后复用现有D5-D9重放与证据包差异审计输出`ready/missing/source_unverified/stale/source_failed`。
- 复制首版、只换审核者或措辞仍稳定返回`missing / d8_review_version_preflight_material_change_missing`；只有新增/移除真实事实、关系变化、更正/替代版本、官方状态/发布时间、依据事实或合法门禁缺口变化才能进入`ready`。`formalUsable` 与 `stateTransitionAllowed`继续固定为`false`，预检通过不等于正式龙头状态就绪。
- 人工审核表单增加“检查实质变化”按钮和结构化结果。D8写入开关关闭时仍可只读预检；草稿任一字段变化会立即作废旧结论；已有首版时，只有预检`ready`才在界面上具备提交资格。窄屏确认区已收拢为纵向布局，保持现有紧凑工作台风格。
- 按TDD新增复制首版保持`missing`、真实事实变化进入`ready`且不落库、官方状态变化进入`ready`和预检POST鉴权边界。人工审核与审计相关249项、隔离式完整后端1336项、前端完整ESLint、TypeScript、Next.js 16.2.10生产构建、全量Python编译和`git diff --check`均通过；预检路由注册静态核对为`True`。
- 本批没有调用外部付费AI、修改迁移/依赖/环境变量、停止/重启/重载服务或执行Git暂存、提交、推送。需如实保留一项操作边界事件：最早的鉴权专项命令直接导入`test_data_integrity`，旧`database.py`会在导入时对默认SQLite执行幂等`init_db()`。本批未执行D8保存或业务数据改写，但该命令确实打开了默认库并执行建表/补列式初始化；后续完整测试已改为导入前精确重定向到`/private/tmp`并通过。
- 当前Git仍为本地`main` HEAD `1541c6b`，远端跟踪引用`d76c188`，本地领先1个原有提交；前六批代码、测试与本检查点仍全部未提交。8001仍由PID 778监听，4000未监听；运行进程未加载本批代码，因此不得声称运行页面或真实生产数据已完成预检。

下一批按顺序进入“第二个真实D8版本现场证据审查”：需先单独允许对生产D8上下文做只读核对、对对应官方公告做外部真源核对，并在受控加载新代码后只调用预检端点。如没有实质变化，继续保持`history_insufficient/missing`；只有真实草稿预检`ready`后，才能再单独备份生产SQLite、授权D8写入开关和保存`manual-review-v2`。

## 2026-08-15阶段5/6正式证据细化第五批：龙头正式研究批次提供方

- 新增纯只读`radar-leader-formal-research-batch-v1`批次提供方，严格复用现有`LeaderRuntimeCandidatePlan`、`radar-sector-rule-readiness-v1`和`radar-leader-research-readiness-audit-batch-v1`合同，按候选发现、行业正式规则输入、可交易性、行业/市场跨截面、流动性、历史连续性、主营催化和风险证据8项输出逐候选与整批`ready/missing`。
- 所有输入必须属于同一候选计划、`radarRunId`和带时区`asOf`，候选代码与顺序必须完整一致。行业规则合同、七维审计合同、嵌套逐股审计、原因码和固定关闭的正式门禁标志都会重新校验；跨批次、跨时点、候选错位、伪造状态或畸形嵌套均封闭为`missing`，不能把D2公告审核队列重新贴标签后代入。
- 本合同的`ready`只表示研究输入证据齐备，不表示候选已通过正式门槛。逐股结果继续保留现有首次否决原因，`formalScoreReady/formalGateReady/formalUsable/stateTransitionAllowed`全部固定为`false`；不生成分数、预备/候选/已确认状态或状态迁移，也不复制`manual-review-v1`补风险证据。
- 按TDD先确认模块缺失红灯，再补齐完整同轮、单股历史缺失、行业规则缺失、跨运行/时点错配、D2/伪造合同拒绝和冻结脱敏输出共6项专项测试。专项6项、直接相关51项、全部龙头663项、隔离式完整后端1333项通过；完整测试在导入`database`前把默认生产SQLite连接精确重定向到自动清理的`/private/tmp`临时库。前端ESLint、TypeScript、Next.js 16.2.10生产构建、Python编译和`git diff --check`均通过。
- 本批未读取或写入生产SQLite，未调用外部付费AI，未修改迁移、依赖、环境变量、API、页面、调度、仓储或正式规则开关，也未停止、重启或重载服务。现场8001仍由PID 778监听，4000未监听；运行进程尚未加载本批代码。Git仍为本地`main` HEAD `1541c6b`、远端跟踪引用`d76c188`、本地领先1个原有提交，前五批改动与本检查点均未提交。

下一批按既定顺序先做第二个真实D8版本的可用性前置审查：只能接受相对`manual-review-v1`可追溯的新增事实、官方状态变化、更正/替代关系或其他合法门禁变化；若现场没有实质变化，必须继续保持`history_insufficient/missing`，不得复制首版、改版本号或换审核措辞凑`manual-review-v2`。任何生产SQLite读取、外部来源核对、D8写入和服务重载仍需按当批边界单独执行。

## 2026-08-15阶段5/6正式证据细化第四批：ETF正式准入证据提供方

- 新增逐产品纯只读`radar-etf-formal-admission-evidence-v1`提供方，固定核对产品身份、官方生命周期、行业/主题范围、ETF与指数关系、指数方法、当前完整成分权重、100%行业暴露、五个正式排名输入和规则策略共9项；每项及总结果只输出`ready/missing`与稳定原因，不生成ETF分数、排名、代表产品、候选或正式状态。
- 生命周期和行业/主题范围新增强类型冻结证据，要求ETF代码、`asOf`、来源URL、SHA-256和抓取时间完整。第一版行业观察池继续排除宽基、主动、跨境、非股票及未知产品；没有官方活跃生命周期或行业/主题范围证据时保持`missing`，不从名称猜测。
- 既有产品主档、指数关系、方法、成分、行业暴露和排名输入合同现在会在提供方内做跨对象身份核对：必须属于同一只ETF、同一指数公司/代码/名称和同一`asOf`。产品主档目标指数与双向指数证据冲突、未来/失效版本、内部非官方方法、成分空集/重复/缺权重、报告总权重与明细合计不一致、权重合计超出`100%±0.5`、行业映射不足100%、跨产品排名输入或非有限数值都会封闭失败。
- 默认`radar-etf-rule-v1`继续保持禁用，权重和阈值仍为空，`formal_source_inputs_incomplete`和`ranking_calibration_sample_missing`继续保留。因此即使测试构造完整同身份证据验证`ready`分支，当前真实运行仍不会生成正式ETF；本批没有向生产代码预填测试权重或阈值，也没有把代表样本POC冒充全量来源。
- 按TDD先验证缺模块红灯，再两轮补齐身份冲突和权重合计漏洞；新增8项专项测试，全部ETF相关70项、隔离式完整后端1327项通过。完整测试将`database.DB_PATH`重定向到自动清理的`/private/tmp`临时SQLite并正常初始化基础表；Python编译和`git diff --check`通过。
- 本批未读取或写入生产SQLite，未修改迁移、依赖、环境变量、API、页面、调度、仓储或正式规则开关，未调用外部付费AI，也未停止、重启或重载服务。现场8001仍由PID 778监听，4000仍未监听。Git仍为本地`main` HEAD `1541c6b`、远端跟踪引用`d76c188`、本地领先1个原有提交，前四批改动与本检查点均未提交。

下一批进入龙头正式研究批次提供方：复用现有候选发现、可交易性、行业正式门、跨截面、流动性、历史连续性、主营催化和风险证据审计合同，形成同一`radarRunId/asOf`的逐候选与整批`ready/missing`结果；不得把D2公告审核队列或影子行业状态冒充正式输入，不得复制`manual-review-v1`凑第二版，也不提前生成龙头正式状态。

## 2026-08-15阶段5/6正式证据细化第三批：行业规则输入就绪度合同

- 新增纯只读版本化`radar-sector-rule-readiness-v1`合同，固定审计分类历史、100%行业映射、当前行业原始特征、同批次市场等权/总市值加权基准、历史覆盖、至少20个同分钟成交额样本、至少5个完整交易日持续性、至少20个可比行业和八阶段阈值批准证据。每项及总结果只返回`ready/missing`与稳定原因，不输出行业分数、排名、正式状态或`formalUsable`。
- 历史、市场基准和阈值批准证据都使用强类型冻结身份。行业历史逐大类保存真实交易日期而不是只接收一个布尔值；跨`radarRunId`、`asOf`、规则版本或行业文档摘要的证据封闭失败；异常交易日过滤合同未验证、历史日期晚于`asOf`或早于分类知识生效时间均不能进入就绪结果。
- 当前行业特征即使刻意保持`formalUsable=false`，只要影子原始字段完整且成交额/总市值单位已验证，仍可作为“当前原始输入”接受审计；这不会绕过正式门禁。按现有真实代码状态，市场收益基准提供方、20日同分钟历史、5日持续性和阈值批准证据均不存在，中上协分类历史仍有未验证项，因此实际正式规则仍必须保持`missing`。
- 按TDD先验证缺模块红灯，再完成最小实现；新增5项专项测试，行业及单位证据相关57项、隔离式完整后端1319项通过。完整测试先将`database.DB_PATH`重定向到自动清理的`/private/tmp`临时SQLite并执行正常基础表初始化，未读取或写入生产SQLite。Python编译使用`/private/tmp`缓存路径通过，`git diff --check`通过。
- 修正1个既有雷达提醒测试的跨日时钟耦合：用例继续验证“雷达行业未加入监测列表也可见”，但固定独立的“是否今天”判断，避免固定8月14日事件在8月15日后误报失败；生产提醒代码没有改变。
- 本批未修改数据库迁移、依赖、环境变量、API、前端、调度或正式规则开关，未调用外部付费AI，也未停止、重启或重载服务。现场8001由PID 778监听，4000当前未监听；该状态是本轮只读复核结果，本轮没有尝试恢复。Git仍为本地`main` HEAD `1541c6b`、远端跟踪引用`d76c188`、本地领先1个原有提交，前三批改动与本检查点均未提交。

下一批进入ETF正式准入证据提供方：复用现有阶段5产品主档、指数关系、方法文件、成分集合、行业暴露和排名输入合同，逐项生成可追溯`ready/missing`证据；不补假ETF、不预填权重或阈值、不写生产库，也不提前生成正式ETF排名或状态。

## 2026-08-14阶段5/6正式证据细化第二批：市值单位证据链

- 2026-08-14只读核对腾讯第一方行情响应：字段45总市值原值、字段3最新价、字段73精确总股本和字段82币种可在同一响应内交叉校验。平安银行源时间`20260814102521`的真实样本为`2171.52`、`11.19`、`19405918198`和`CNY`；`2171.52×1亿元`与`11.19×19405918198股`相差约22.46万元，符合总市值保留两位小数对应的50万元舍入容差。本次只用于验证字段合同，没有保存真源响应或写入数据库。
- `QuoteSnapshot`新增`marketCapCny`、`marketCapUnitStatus`、`totalSharesSource`和`currency`强类型字段。只有原市值、人民币市值、正价格、正总股本和CNY币种完整时才允许标记已验证；真实0可继续保存为原始值，但不能冒充活跃证券的有效市值证据；`NaN`、`Inf`和负市值不会进入行情合同。
- 新增版本化`radar-market-cap-unit-evidence-v1`批次合同。全部活跃A股必须逐股通过同响应校验，停牌不计入活跃分母；缺失、重复、来源不符、币种不符、非正/非有限值和跨字段不一致均封闭失败，不根据常识硬编码“亿元”单位，也不依赖证券名称或外部模糊匹配。
- 行业影子执行器现同时消费成交额和市值批次证据。完整市值证据只会把行业原始特征的`marketCapUnitStatus`提升为`verified`并移除`market_cap_unit_unverified`；`formal_use_not_approved`继续保留，中上协分类历史、未确认映射、同分钟历史基线和状态阈值仍未通过，因此不生成正式行业分数、排名、八阶段状态、提醒或雷达AI证据。
- 本批按TDD完成多轮先红后绿，新增11项测试；行情/合同/行业专项77项、扩展相关回归153项、隔离式完整后端1314项通过，相关Python编译与`git diff --check`通过。完整测试继续在导入`database`前把生产SQLite路径精确重定向至`/private/tmp`临时库。
- 本批未读取或写入生产SQLite，未调用外部付费AI，未修改迁移、依赖、环境变量、API或前端，未停止、重启或重载服务。现场端口仍为4000/PID 40713和8001/PID 85231；运行进程尚未加载本批代码。Git仍为本地`main` HEAD `1541c6b`、远端跟踪引用`d76c188`、本地领先1个原有提交，第一、第二批改动与本检查点均未提交。

下一批先实现行业正式规则输入就绪度的版本化缺口合同，明确核对分类历史、映射完整度、至少20个同分钟成交额样本、5日持续性、跨行业不少于20个可比行业和阈值批准状态；只输出`ready/missing`及原因，不拍阈值、不生成正式分数或状态。该合同完成后再进入ETF正式准入证据提供方。

## 2026-08-14阶段5/6正式证据细化第一批：成交额单位证据链

- 新增版本化 `radar-turnover-unit-evidence-v1` 聚合合同，只使用腾讯同一响应中的原始成交额字段与精确人民币成交额字段交叉核对；真实0保持有效，停牌证券不计入活跃覆盖分母，缺失、重复、来源不符、负值/非有限值和跨字段不一致均封闭失败。没有猜测单位，也没有用旧快照或其他字段补值。
- 市场和行业影子执行器现会消费该合同，不再把上游已经验证的成交额单位无条件降回 `unverified`。市场影子输入在单位证据完整时可如实记为 `succeeded`；行业影子输入可保存 `turnoverUnitStatus=verified`，但市值单位和正式使用门禁仍保持未验证/未批准，因此不会生成正式行业状态。
- 按TDD新增3项合同测试及2项运行器集成测试，先验证缺模块与旧硬编码行为确实失败，再完成最小实现。成交额合同、市场和行业运行器专项28项通过；相关市场/行业/运行时回归127项通过；完整后端1302项通过；相关Python编译与 `git diff --check` 通过。
- 完整后端测试在导入 `database` 前把默认生产SQLite路径精确重定向到 `/private/tmp` 临时库，测试结束自动清理；未读取或写入生产SQLite，未调用外部付费AI，未修改环境变量、依赖、迁移、API或前端，也未停止、重启或重载服务。
- 现场Git为本地 `main` HEAD `1541c6b`、远端跟踪引用 `origin/main` 为 `d76c188`，本地原有领先1个提交；本批6个代码/测试文件与本检查点尚未提交。端口只读复核显示4000仍由PID 40713监听、8001仍由PID 85231监听；现有运行进程尚未加载本批代码，不能把静态和测试结果写成运行页面已生效。

下一批按既定顺序补市场/行业市值单位及行业正式规则的可追溯证据，随后推进ETF正式准入证据提供方，再处理龙头正式研究批次提供方与第二个真实D8版本。任何一项证据不足都继续返回缺失/失败，不复制首版、不制造Mock、不提前开启正式状态或雷达AI。

## 2026-08-14阶段8提交与受控上线

- 阶段6至阶段8的4个本地提交已推送到 `origin/main`，当前发布代码提交为 `d76c188 feat: 完成阶段8提醒与独立雷达AI工程`；推送前后本地 `main` 与远端 `main` 一致。
- 8001已通过 `ops/launchd/manage.sh enable-leader-stage6` 受控重载，运行PID为85231，旧运行资产归档于 `/Users/linjian/Library/LaunchAgents/stock-monitor-archive/20260814-100845-radar-2b6b2b`；`/docs` 返回200，阶段5 ETF与阶段6龙头开关保持启用，D8写入保持关闭。
- 阶段8生产运行时如实保持门禁关闭：`/api/radar/ai/health` 返回 `status=disabled`、`enabled=false`、`manualEnabled=false`、`configured=false`、`storageReady=false`；AI总览为 `not_run / stage8_storage_not_ready`，提醒偏好返回安全默认值。本次未应用迁移7、未修改生产环境变量、未发起付费AI调用、未写生产SQLite。
- Vercel生产部署 `dpl_2eRUgoFMdCHSoSPNFYAkBhKEHVcm` 已为 `Ready`，正式别名为 `https://stock-monitor-murex-one.vercel.app`。线上首页、主线雷达、提醒中心、监测列表和携带雷达上下文的行业洞察均返回200并通过应用内真实浏览器验收；首页真实行情加载正常，行业页联动提示正确，1280px桌面视口无页面级横向溢出，浏览器控制台0错误，Vercel最近一小时无错误日志。
- `/api/health` 当前为 `degraded`，Vercel、隧道与FastAPI均健康；唯一降级项是既有 `radarLeaderStage6` 正式研究提供方缺失（`leader_research_single_pass_provider_missing`、`leader_research_input_provider_batch_missing`），不是本次发布故障。不得把该状态写成全健康，也不得用影子数据或D2审核队列补正式证据。

下一步：先补齐行业、ETF、龙头正式状态与服务端冻结证据提供方；通过真实证据门禁后，再备份生产SQLite并受控应用迁移7，最后单独配置和启用雷达AI，以第一个真实正式证据包验收。阶段9历史回放仍未开始。

## 2026-08-14阶段8提醒与独立雷达AI本地工程

- 新增独立 `backend/radar/ai/`，完成市场、行业、ETF、龙头四套版本化提示词，强类型冻结证据与结构化输出合同，来源编号、证据完整性、越权字段和投资建议校验，以及独立OpenAI兼容客户端、配额、复用键、进程锁和失败分类。AI只能解释正式状态，不能修改状态、分数、排名、优先级或证据等级。
- 新增迁移版本7的 `radar_ai_analysis_runs`、`radar_ai_outputs`和 `radar_notification_preferences`，只在临时SQLite验证；旧迁移1至6名称和校验值保持不变，没有应用生产迁移。跨进程相同证据键冲突会稳定复用或返回“正在运行”，不会遗留本进程锁。
- 新增独立 `/api/radar/ai/*`只读、历史、健康和受保护手动核对合同，稳定区分 `success`、`not_run`、`not_configured`、`evidence_insufficient`和 `failed`。手动POST只接收对象身份，证据必须由服务端解析；新旧AI不共用历史、缓存、锁、配置或前端状态。
- 新增正式行业、ETF和龙头状态变化提醒映射、来源去重和独立邮件偏好。影子、非正式和状态未变化均零提醒；雷达提醒不触发旧单股AI，提醒中心返回对应雷达页。
- 主线雷达页已增加独立AI组件、五种状态、来源编号、对象切换取消与受开关控制的手动核对；提醒设置增加雷达站内/P2/P3邮件偏好。没有明确行业对象时不伪造行业ID。
- 当前正式状态与冻结证据前置门槛仍未满足：服务端证据解析当前如实返回无可用证据，自动任务也返回空证据集，因此本轮没有发起真实付费AI调用、没有发送真实提醒或邮件。用户已允许未来调用AI，但生产迁移、环境变量和服务重载仍须在正式证据就绪后单独受控执行。
- 阶段8专项及完整后端共1297项通过；前端ESLint、TypeScript和Next.js 16.2.10生产构建通过；Python编译和 `git diff --check` 通过。4000仍为PID 40713，8001仍为PID 39086，两个旧进程均未重载，运行页面尚未加载阶段8代码。

下一步：先完成行业、ETF、龙头正式状态和服务端冻结证据适配；然后备份生产SQLite并受控应用迁移7；最后单独配置雷达AI环境、受控重载8001并用第一个真实正式证据包验收。不得用现有影子快照或D2审核队列替代正式证据。

## 2026-08-13阶段7股票详情、行业洞察和监测列表联动

- 新增只读 `GET /api/radar/stocks/{symbol}` 与 `radar-stock-v1` 强类型合同。接口只读取最新龙头影子快照，区分 `matched`、`not_listed`、`no_snapshot`、`stale`、`failed`、`not_enabled`；只有命中最新公开预备/候选/已确认梯队才返回龙头条目，不把D2审核队列冒充龙头状态，不触发重算或写库。
- 股票详情新增“主线雷达状态”，展示公开梯队状态、行业、规则版本、数据时间和首个否决原因；无快照、未入榜、过期、失败和未启用均保持独立文案，不伪造分数或状态。
- 主线雷达行业卡可携带行业代码和名称跳转行业洞察；行业洞察明确显示“来自主线雷达”上下文并提供返回雷达入口，同时明确现有公开资讯尚未按该行业精确过滤。
- 预备、候选、已确认龙头条目均提供手动“加入监测列表”，统一复用现有 `useWatchlist` 及 `stockCode + stockName` 合同；已存在时按钮禁用并显示“已在监测列表”，不会自动加入，也不会把D2公告审核队列当候选股加入。
- 后端新增6项测试，已按先红后绿覆盖命中、未入榜且不混入D2、无快照、过期、失败、未启用和非法代码；后端完整1246项通过。前端ESLint、TypeScript与Next.js生产构建通过。
- 本阶段没有读取或写入生产SQLite，没有触发外部付费AI，没有修改环境变量、依赖或数据库结构，没有停止、重启或重载服务，也没有执行Git暂存、提交、推送、PR或部署。4000与8001仍是阶段7实施前的既有进程，因此运行页面尚未加载本阶段代码，不能把静态/测试验收写成现场页面验收。
- 阶段7工程整包已完成本地实施。下一步若要现场验收，需要用户另行授权受控重载4000/8001；阶段8提醒/雷达AI、阶段9历史回放及D8第二个真实人工版本均未进入，`manual-review-v1` 仍是唯一真实版本，禁止复制首版凑第二版。

## 2026-08-13阶段7运行验收与联动修复

- 用户确认开始后，8001通过 `ops/launchd/manage.sh enable-leader-stage6` 受控重载；旧运行资产归档于 `/Users/linjian/Library/LaunchAgents/stock-monitor-archive/20260813-230117-radar-2b6b2b`，新后端PID为39086。阶段5 ETF与阶段6龙头开关继续启用，D8写入继续关闭；主机侧 `/docs` 返回200，`GET /api/radar/stocks/000725` 已加载 `radar-stock-v1`，真实返回 `no_snapshot / candidate_snapshot_missing`，D8人工版本仍为1。
- 前端完成生产构建并按原 `screen + npm run start` 方式受控恢复，最终4000 PID为40713、首页返回200。应用内真实浏览器验证000725详情页显示“尚无龙头影子快照，等待真实数据”，没有用D2审核队列或假分数填充状态。
- 真实行业73“研究和试验发展”从雷达跳行业洞察成功，页面显示“来自主线雷达”和“当前仅携带行业上下文；下方公开资讯尚未按该行业精确过滤”。验收发现返回入口原先只回雷达总览，已修复为携带 `tab=sectors + industryCode + industryName`；返回后自动进入行业主线页、显示“来自行业洞察”上下文并高亮对应行业卡。
- 龙头真实快照仍为空，龙头页如实显示等待快照；D2风险公告审核工作台可见，但加入监测/已在监测按钮均为0，确认没有把D2公告队列当候选股。候选按钮的真实点击与已加入状态仍需等首个真实公开梯队条目出现后补验，不能用Mock填生产页面。
- 1920×1080行业雷达页、390×844行业雷达与行业洞察页均无页面级横向溢出。手机验收发现行业页标题被105px固定导航遮住，已增加局部安全顶部间距并使页头、徽标和雷达上下文在窄屏纵向排列；最终标题顶部120px，完整可见。浏览器各验收页控制台0错误。
- 同步修正龙头总览D8下一步文案：当前1个真实人工版本时明确等待包含实质证据变化的第二个连续版本，禁止复制补位，不再错误提示尚未形成首版。前端ESLint、TypeScript与Next.js生产构建通过。
- 本轮未手工触发D8写入、外部付费AI、雷达重算、Git推送、PR或部署；没有修改环境变量、数据库结构或依赖。阶段7运行验收已完成，下一阶段可进入阶段8本地工程设计与实现；本轮验收修复尚未另行Git提交。

## 2026-08-11当前开发批次

- 现场确认截图两张空卡不是前端渲染故障：ETF已有2244只官方产品主档，但主动/被动、境内股票资产范围和指数证据尚未通过正式门禁，因此候选保持0；龙头模块的生产版本5迁移和 `RADAR_LEADER_STAGE6_ENABLED` 仍未启用。
- D2真实候选分片为385只/13片；已修复多分片首个严格终态误报、巨潮真实 `.PDF` 类型、缺类型官方 `.html` 终页，以及下游候选合并器漏认同一官方 `.html` 的跨层合同断裂。只接受 `static.cninfo.com.cn/finalpage` 下这两类官方终页，其他URL约束未放宽。
- D2只对 `cninfo_source_request_failed` 做同页最多一次重试并计入910次全局预算；解析、身份、范围、分页和其他 `source_unverified` 不重试。真实完整运行789次请求中4次网络失败均重试成功，获得785个有效页和22018份去重文档；随后因下游HTML合同断裂被整体阻断。修复后的复验在15:02连续竞价结束后被门禁以0请求停止，尚缺下一交易窗口的最终真实D2证据。
- 针对阶段5自然任务日志中间歇出现的 `sqlite3.OperationalError: disk I/O error`，本地仓储仅在 `BEGIN IMMEDIATE` 尚未开始事务时等待1秒重试一次；其他错误和第二次失败仍原样抛出，不重放业务写入。
- 新增受控运行资产 `etf-stage5-enabled` 与 `leader-stage6-enabled`，以及 `enable/disable-etf-stage5` 和 `enable/disable-leader-stage6` 命令；启用龙头时会继续保留市场、行业与ETF影子开关，重载失败仍使用既有归档回退。
- 前端总览两卡已在本地代码增加真实进度指标：ETF展示产品主档/正式准入/待补证据/覆盖率，龙头展示生产开关/快照/正式状态或真实计数；不生成虚假候选。
- 当前相关D2来源/候选/生命周期专项65项、全部 `test_radar*.py` 911项、LaunchAgent资产、TypeScript和相关ESLint均已通过，`git diff --check` 通过。
- 15:02只读复核4000仍为PID 23280、8001仍为PID 38620。两个现有服务尚未加载本批未提交代码；本批未读写生产SQLite、未修改已安装LaunchAgent、未停止/重启/重载服务，未执行Git暂存、提交、推送或部署。
- 一次性自动验收 `6-d2` 已更新为下一次09:35执行：使用当前工作树、临时SQLite和真实候选全集，保持同页网络失败最多一次重试、910次总预算及首个具体合并失败条件诊断；禁止生产库、服务重载、Git、部署和付费AI。

## 新对话先做

全程中文，只操作唯一真实项目：

`/Volumes/HermesSSD/AntigravityData/量化监测-股票`

开始前必须按顺序读取并现场复核：

1. `AGENTS.md`
2. `PRD.md`
3. `docs/股票监测助手V5.0升级规划书.md`
4. `NEXT_CHAT_HANDOFF.md`
5. 当前代码、`git status`、HEAD与远端、固定端口服务、雷达运行配置和相关测试

不得只依赖本检查点，不得覆盖、回滚或清理用户的新修改，不得读取或写入生产SQLite。

## 当前准确位置

- V5共阶段0至阶段10，11个大阶段。
- 阶段0至阶段3已完成工程实施；阶段3市场/行业联合生产门槛已通过。
- 阶段4“主线雷达页面骨架”已完成实现和4E浏览器验收，并随2026-07-30生产版本发布到Vercel。
- 阶段4历史提交为 `cc79cd4 feat: 完成V5阶段4主线雷达页面骨架`；当前本地HEAD为 `47d9313 docs: 更新阶段6发布检查点`，后续仍须以实时Git状态为准。
- 阶段5A“行业ETF字段级数据合同”已完成，用户批准“方案B增强版”。
- 阶段5B“官方产品主档与分类POC”已完成独立代码、Fixture测试和真实官方源只读验证；尚未接入生产SQLite、调度、API或页面。
- 阶段5C“指数身份、方法和成分版本POC”已完成独立代码、Fixture测试和三方官方源真实只读验证；代表样本仍因关系日期、方法生效时间或当前完整权重缺失而未正式就绪。
- 阶段5D“行业暴露和同指数产品组POC”已完成纯计算代码、Fixture测试和000300真实历史权重只读验证；结果只作研究证据，未正式就绪。
- 阶段5E“行情、日频事实和排名输入POC”已完成独立份额适配器、排名输入审计、四代码官方只读探测和临时SQLite容量上限估算；正式候选仍被来源和准入证据阻断。
- 阶段5F已完成迁移与仓储设计审查，并已在内存/临时文件SQLite完成版本4和独立仓储测试；没有执行生产SQLite迁移。
- 阶段5G第一步已完成默认关闭开关、同行情批次影子执行器、独立锁和临时版本4运行时测试；低频来源任务和自然运行尚未完成。
- 阶段5G第二步已冻结正式准入、规则禁用状态和60日/20日保留策略；低频任务因产品版本切换和指数全量来源未就绪继续阻断。
- 阶段5G第三步已完成产品版本切换、历史时点读取和更新区间触发器。
- 阶段5G第四步已冻结首次观察/官方生效时间语义，并完成默认关闭的产品主档低频执行器、批次审计、每日一次门禁和独立调度注册。
- 阶段5I已完成生产SQLite只增版本4迁移、后端阶段5开关生效和只读接口核对；未回填ETF业务数据，真实状态仍为 `not_ready`。
- 阶段6A至6L-F6及本地运行时接入包已完成开发：现有市场影子任务会在阶段6开关开启时构建同轮候选计划并调用一次研究提供器；默认提供器只返回显式缺失，全缺失路径在阶段6仓储、Assembly和F4之前降级退出。非全缺失输入才可进入单次Assembly/F4及既有影子仓储；其余阶段6既有能力与详细记录见下文。
- 阶段6真实研究来源上下文已完成：提供器现在只接收同一run/asOf/quoteBatch冻结的候选行情、行情健康、候选主档和行业身份集合；默认仍明确缺失，没有任何现有来源被提前升级为READY。
- 阶段6L-C4A已完成本地纯内存“供应商可交易性数据交付与批次准入合同”：系统侧独立可信策略、静态/动态来源逐证券绑定、授权和来源域名白名单、90秒动态时效、整批覆盖、敏感URL拒绝及全内容批次哈希已冻结。通过只表示可进入真实POC，未接账号、网络、运行时、数据库、正式评分或状态迁移。
- 阶段6L-C4B-1已完成本地隔离RQData HTTP字段适配器：固定官方端点、20秒超时、无重试、最多8只样本、CSV字段/身份/交易日/时效/枚举审计和脱敏摘要已冻结。Fixture固定为`real_poc_status=not_run`，真实HTTP全空为`no_data`；用户已提交试用申请，但尚未提供Token或执行真实RQData调用。
- 阶段6L-C4B-2单次真实POC入口已完成本地准备：只有显式确认、官方交易日、A股连续交易窗口和查询合同全部通过后才隐藏读取Token；周六干跑在Token前返回`not_run`。官方HTTP文档当前说明Token当天有效；RQData改为可选备用对照，不再作为后续开发或免费方案验收的前置条件。
- 阶段6L-C4C免费公开复合源已完成合同与交易时段真实POC：交易所公开名单/公告作为主证据候选，腾讯行情仅作动态交叉核对，东方财富风险警示板只在命中且具有逐行源时间时生成聚合备证；普通行情、价格、成交量或未命中不得推断`trading`。最终真实状态为`partial`，所有正式评分、准入和状态迁移继续关闭。
- 阶段6剩余开发已合并到 `docs/superpowers/plans/2026-08-08-stage6-remainder-consolidated-delivery.md`，不再新增细碎阶段编号。已完成已验证组件的统一运行时组合入口、风险D8连续证据包到D9/E1批次装配、主营催化官方材料批处理与版本化人工复核契约，以及免费可交易性POC到C3/C1运行时输入的严格适配；所有路径继续保持analysis-only。
- 阶段6四来源统一准入包已完成：`leader_research_source_admission.py`内部重放历史POC并核对完整点时行业成员，只接受版本化主营人工复核批次、由原始查询/观察重建的公开可交易性运行输入和同计划风险投影批次；候选错位、契约改写、时间漂移或行情内容改写整批阻断，部分来源精确保留`source_failed/stale/source_unverified`。输出已实际贯通F5提供器与F6单轮编排，F6强制复验冻结来源上下文且畸形嵌套封闭失败；正式评分、正式门禁和状态迁移仍全部关闭。
- 阶段6风险生命周期批次已完成：`leader_risk_lifecycle_batch.py`只消费同计划D2原始发现页和原始人工审核版本，内部重放D5-D8后接入既有D9/E1/E3及统一准入。七类固定关键词、分页/总数/重复文档守恒、开放事件身份与有效期结转、更正版本时序、真实人工审核24小时时效、空历史与来源失败语义均已锁定；真实D2批次和真实人工双版本尚未产生，不能据此宣称风险来源就绪。
- 阶段6风险生命周期受控交付入口已完成：`leader_risk_lifecycle_delivery.py`只有在显式确认、冻结候选计划、连续窗口、24小时采集时效和强类型人工版本全部通过后才允许采集D2；七类固定关键词在全局页请求预算内轮转，无重试，来源失败、预算耗尽、页数/总数漂移和跨页重复均失败关闭。输出仅保留压缩计数、稳定原因码和关闭的正式门；Fixture transport固定为`realPocStatus=not_run`。这仍是本地纯内存能力，不代表真实D2或人工双版本验收已经完成。
- 2026-08-09已完成D2真实候选范围和分页语义复验：巨潮官方代码查询可精确形成`symbol + orgId`发行人范围，多候选查询返回受该范围约束；七类全市场数学总页数约909，因此正式交付只允许冻结候选全集的有界查询。来源原始`totalpages`在非整页时采用向下取整语义，代码现同时保留原始值和数学总页数；仅该已验证语义可归一化，任意其他分页漂移、跨页原始值变化、范围外文档和非法原文身份继续失败关闭。相关风险链64项、全部雷达887项通过。
- 阶段6当前仍是“本地工程主体已完成、真实验收未完成”，不能写成阶段6正式完成：C4C-2B免费路径已为`partial`；D2已用真实注册/行业/市场链生成385只`ready`冻结候选，但现有巨潮合同单批最多30只，已按`cninfo_query_candidate_scope_unverified`在发行人解析和七类查询前阻断；真实D2批次和人工审核双版本仍未产生。阶段5观察门槛、版本5生产迁移和阶段6生产开关均未通过。
- 2026-08-09 18:10只读核对：4000仍由既有Node进程监听、8001仍由既有Python进程监听，`/radar`与`/docs`均返回200；本轮没有停止、重启或重载服务。
- 阶段5与阶段6L-D9代码检查点已提交为 `49199de feat: 完成阶段6D9与AI健康链路加固` 并推送到 `origin/main`；唯一交接文件的后续文档提交仍须以实时Git状态复核。
- 生产SQLite仍只有版本1至4，阶段6开关未启用；版本5生产迁移、后端配置和后端重载均未执行。
- 阶段5的20个交易日观察已于2026-07-27进入第1日：阶段5影子锁在09:33:09首次创建，代表ETF腾讯源时间在09:49:45已为当日盘中时间。收盘现场确认服务持续运行且未发现当日阶段5专属失败日志，但现有日志没有足够的阶段5完成/候选快照证据，且临近收盘市场与行业输入多次因过期或不完整被门禁拒绝；在禁止读取生产SQLite的边界下，第1日只能记为“已观察、日结证据不完整”，暂不能计作合格观察日。

## 阶段5A已批准合同

- 完整合同：`docs/superpowers/plans/2026-07-24-stage5a-etf-data-contract.md`。
- 第一版正式观察池只评估证据完整的境内股票型被动ETF。
- 主动ETF、跨境、宽基、债券、商品、货币、混合、策略ETF、LOF、REIT和未知类别只进入名册与健康层。
- 保留现有 `etf_product_registry` 作为原始发现和历史层；新增产品归一化、指数关系、指数方法与成分、行业暴露、指标观测和候选结果等版本化逻辑实体。
- 同一ETF允许多个行业暴露；未映射权重不重新分配。同一指数的多个ETF先归组，只允许一个代表占用行业前5名。
- IOPV、净值、折溢价、份额变化、跟踪差异和跟踪误差分开；份额变化不得表述为资金流。
- 未通过真实来源POC的精确价差、IOPV、PCF、历史净值和跟踪指标保持 `source_unverified`，不参与正式排名。
- 雷达与旧业务共用 `backend/data/stock_monitor.db`；隔离依靠 `radar_etf_*` / `radar_index_*` 新表、`backend/radar/` 仓储、独立默认关闭开关和任务锁，不是独立数据库。
- 阶段5A没有修改生产代码、SQLite、环境变量、依赖、调度或服务；ETF页面仍为 `not_enabled`。

## 阶段5B真实结果

- 新模块：`backend/radar/sources/etf_product_master.py`，当前没有被运行时、仓储、API或页面导入。
- 分类映射版本：`radar-etf-product-classification-v1`。
- 真实官方源POC时间：`2026-07-24T23:32:28+08:00`。
- 官方上市基金2,225/2,225条，行覆盖率1、来源问题0。
- ETF 1,608条：上交所907、深交所701；另有LOF 404、REIT 86、其他上市基金10、上交所未定义 `F300` 117。
- ETF资产类别确认覆盖50%：境内股票685、跨境股票31、债券53、商品8、货币27、未知804。
- ETF主动/被动确认覆盖0%；全部保持 `unknown`，不得生成正式候选。
- 上交所 `F131` 只确认跨境，不确认股票资产；深交所“股票基金”不确认境内或跨境。
- 官方列表没有统一报告日期，`sourceTime=null`，只保存 `fetchedAt`；连续两个A股交易日没有健康检查时才标记来源过期。
- 新增8项测试；与现有ETF来源和雷达合同合计32项通过，相关语法编译通过。
- 未读取或写入生产SQLite，未修改环境变量、依赖、调度、API、页面或服务。

## 阶段5C真实结果

- 新模块：`backend/radar/sources/etf_index_evidence.py`，当前没有被运行时、仓储、API或页面导入。
- 真实官方源POC时间：`2026-07-25T00:13:43+08:00`。
- 159915基金官方页面明确“被动股票指数”、创业板指数和代码399006，与国证399006身份一致；关系发布日期和生效日缺失，身份已核对但 `formalReady=false`。
- 510310基金官方页面明确“被动股票指数”和沪深300指数名称，但没有基金方指数代码；不得按名称猜000300，保持 `pending_evidence`。
- 国证399006方法PDF使用内部证据版本 `internal-2025-06-10-02f76513f29ee502`；2026-07-24成分100只，仅10只披露权重、合计58.79%，其余权重不补等权。
- 中证000300方法官方标签 `2023-09`；2026-07-24当日成分300只但没有权重；2026-06-30月末权重300/300、合计100.008%，只能作为历史版本证据。
- 两家指数公司的公开方法证据均没有足够的准确公布/生效时间，当前方法版本保持未正式就绪。
- 静态身份和方法每日最多健康检查一次，连续两个A股交易日未健康检查才过期；当前成分漏一个已完成交易日就阻止新候选。历史月末权重不得冒充当前版本。
- 新增11项指数证据Fixture测试，覆盖身份缺失/冲突、主动ETF、版本时间、成分重复/空集/缺失权重/异常权重和真实0。
- 连同阶段5B产品主档、现有ETF来源和雷达合同共43项测试通过，相关Python语法编译通过。
- 未保存官方文件或逐产品结果；未读取或写入生产SQLite，未修改环境变量、依赖、调度、API、页面或服务。

## 阶段5D真实结果

- 新模块：`backend/radar/etf_industry_exposure.py`，当前没有被运行时、仓储、API或页面导入。
- 历史计算时点：`2026-06-30T15:30:00+08:00`；真实只读抓取时间：`2026-07-25T00:30:42+08:00`。
- 中证000300历史权重300/300，合计100.008%；中上协2025H2行业版本映射300/300，原始权重映射覆盖100%。
- 前五大行业原始权重：计算机、通信和其他电子设备制造业30.739%，电气机械和器材制造业9.772%，货币金融服务9.415%，专用设备制造业5.476%，资本市场服务5.126%。
- 成分版本缺精确公布/生效区间，行业版本为本系统事后核验；结果原因固定为 `constituent_version_not_formal`、`industry_mapping_retrospective_unverified`、`industry_mapping_not_formal`，不得用于正式历史回放。
- 000300与中证800同日历史版本共同300只、并集800只，代码Jaccard 0.375，原始权重重合率约0.7143785686；因成分版本未正式就绪，重合度也不转正式。
- 同指数产品组每组只提供一个候选槽位；阶段5D没有排名输入，因此不选择代表ETF，`representativeSymbol=null`。
- 新增8项测试；阶段5B至5D相关测试与现有ETF来源、雷达合同合计51项通过，语法检查和 `git diff --check` 通过。
- 未保存官方文件或逐产品结果；未读取或写入生产SQLite，未修改环境变量、依赖、调度、API、页面或服务。

## 阶段5E真实结果

- 新模块：`backend/radar/sources/etf_daily_facts.py`、`backend/radar/etf_ranking_inputs.py`，当前没有被运行时、仓储、API或页面导入；
- 真实探测请求 `159915`、`159949`、`510300`、`510310`，上交所与深交所均返回 `2026-07-24` 报告日，份额字段覆盖率和行覆盖率均为100%，来源问题0；
- 腾讯代表行情两只均有价格、涨跌幅和源时间；原始成交额字段存在但单位仍 `source_unverified`，审计结果把它排除出正式排名；
- 净值、规模、IOPV、PCF、精确价差、历史净值回报、跟踪差异、跟踪误差和相关性没有通过同口径来源POC，保持未验证；当前正式候选数0，不选择代表ETF；
- 份额变化公式为 `currentShares / priorShares - 1`，前一期真实0不改写为0，不输出资金流语义；
- 独立临时SQLite容量场景 `685 × 48轮/日 × 60交易日`：20日657,600行、133,148,672B；60日1,972,800行、397,508,608B；事务P95约31.6ms、行业查询P95约0.132ms、备份约0.570s、完整性`ok`；临时文件已清理；
- 新增9项测试，阶段5B至5E相关测试、ETF来源和雷达合同共60项通过；语法编译和`git diff --check`通过；
- 未读取或写入生产SQLite，未修改环境变量、依赖、服务、调度、API或页面；4000/8001仍保持监听，ETF页面继续`not_enabled`。

## 阶段5F设计审查结果

- 只审查迁移和仓储设计，没有修改`backend/radar/migrations.py`，没有执行生产SQLite迁移；
- 拟议新增只增结构版本，表范围为产品归一化、指数关系/方法/成分、行业暴露、日频事实、盘中特征和候选快照/明细；
- 拟议独立`backend/radar/etf_repository.py`只接受调用方显式迁移连接，使用UTC时间、时点查询、记录哈希幂等、冲突停止和整批事务；
- 迁移演练必须覆盖版本漂移、未来版本、时间区间重叠、缺失权重、外键、重复写入、整批回滚、关闭重开、完整性和旧版本1至3不变；
- 60日容量约397MB，20日/60日/长期汇总保留策略尚未确认；
- 生产备份、复制库演练、正式迁移、环境/任务变更和回退恢复均需单独授权，本轮全部未执行。

## 阶段5F临时库实现记录

- 新增可选迁移版本4 `etf_versioned_storage` 和 `STAGE5_RADAR_MIGRATIONS`；默认生产 `RADAR_MIGRATIONS` 仍为版本1至3；
- 新增 `backend/radar/etf_repository.py`，只接收显式迁移连接，覆盖产品、指数关系/方法/成分、行业暴露、日频事实、盘中特征和候选快照/明细；
- 新增 `backend/tests/test_radar_etf_repository.py`，内存与临时文件SQLite专项4项通过；迁移、通用仓储、市场迁移、行业仓储、ETF排名输入和阶段5F专项合计66项通过；
- 语法编译和 `git diff --check` 通过；未读取或写入生产SQLite，未改环境变量、依赖、服务、调度、API或页面。

下一步：

1. 先补产品版本切换仓储接口和临时库测试；
2. 再设计产品主档低频任务，指数全量刷新继续等待正式来源扩展；
3. 在此之前保持阶段5任务、ETF真实API和页面真实结果关闭。

## 阶段5G第一步完成记录

- 新增默认关闭的 `RADAR_ETF_STAGE5_ENABLED` 配置读取，实际运行环境未修改；
- 新增 `backend/radar/etf_shadow_runner.py`，只消费现有ETF行情任务已经抓取并健康判定的同一内存批次，不重复请求行情；
- 阶段5使用独立跨进程锁和实时门禁；关闭时不创建锁、不连接版本4仓储；失败不会改变既有ETF行情任务的健康结果；
- 当前只保存 `formal_usable=false` 的盘中特征和真实空候选，候选数为0，原因是产品证据未就绪和规则未冻结；过期/失败批次不保存特征；
- 默认运行时仍只要求迁移版本1至3，但已知版本4可以共存并继续核对校验值，未知迁移版本仍拒绝；
- 新增8项阶段5G专项测试；相关90项和扩展160项测试通过，语法编译和 `git diff --check` 通过；
- 未读取或写入生产SQLite，未改环境变量、依赖、服务、API或页面，阶段5G低频任务和生产自然运行仍未完成。

## 阶段5G第二步完成记录

- 新增 `backend/radar/etf_stage5_policy.py`，冻结境内股票型被动ETF、有效生命周期、正式指数证据、100%行业映射和正式排名字段等硬门槛；
- `radar-etf-rule-v1` 当前明确禁用，权重和阈值为空；正式来源和校准样本不足时不得拍数值或生成正式候选；
- 盘中特征保留60个交易日，最短影子观察20个交易日；日频事实和候选摘要长期保留，不保存上游响应正文，自动清理关闭；
- 低频任务必须同时具备产品生效时间、产品版本切换仓储、指数全量刷新、日频代码池和已批准保留策略；
- 当前产品生效时间、版本切换和指数全量刷新仍未就绪，因此没有注册或伪运行低频任务；
- 新增6项策略专项测试，阶段5及既有雷达相关166项通过；语法编译和 `git diff --check` 通过；
- 未读取或写入生产SQLite，未改实际环境变量、依赖、服务、API或页面。

## 阶段5G第三步完成记录

- 版本4产品表补全官方分类、投资类型、目标指数、映射版本、分类原因和来源字段；
- 产品内容和证据未变化时忽略抓取时间差异，不制造每日重复版本；
- 新增产品版本原子切换和历史时点读取；回填冲突、内容冲突和区间重叠整体回滚；
- 产品、指数关系和指数方法均增加更新区间重叠触发器，直接SQL也不能绕过；
- 阶段5仓储专项增至5项，阶段5及既有雷达相关167项通过；语法编译和 `git diff --check` 通过；
- 产品官方源仍缺统一生效时间，产品主档低频任务继续阻断，不用抓取时间冒充生效时间。

## 阶段5G第四步完成记录

- 版本4产品表新增 `version_time_kind`、`official_effective_from`、`first_observed_at`；当前官方产品主档只能写 `first_observed`，首次观察不能冒充官方历史生效；
- 新增 `backend/radar/etf_product_master_runner.py`，完整批次才原子推进产品版本，来源失败/空榜/重复/不完整只记录批次审计；
- 新增 `radar_etf_product_master_runs` 审计表、同内容跨日幂等、内容变化版本切换、`historicalReplayReady` 读取语义；
- 只有 `RADAR_ENABLED`、`RADAR_SHADOW_MODE`、`RADAR_ETF_STAGE5_ENABLED` 同时开启时才注册独立产品主档任务，86400秒间隔、150秒错峰；同一上海日已有尝试时只读跳过；
- 阶段5及既有雷达相关271项测试通过；未读取或写入生产SQLite，未启用阶段5，未修改环境变量、依赖、服务、API或页面。

## 阶段4真实能力

- 前端一级导航“主线雷达”和 `/radar` 已存在，桌面端1920×1080优先。
- 后端新增只读：
  - `GET /api/radar/overview`
  - `GET /api/radar/sectors`
- 两条API使用SQLite `mode=ro` 和 `query_only`，无数据库写入、迁移、调度、环境变量或依赖变更。
- 页面只接阶段3真实市场与行业聚合；不生成正式市场四状态、行业评分、排名、提醒、AI或交易建议。
- 状态合同区分 `available`、`empty`、`stale`、`failed`、`not_ready`、`not_enabled`；质量区分 `complete`、`partial`、`unavailable`。
- 页面分别展示原始数据时间、抓取时间和页面渲染时间；收盘后不按浏览器当前时间继续把最后成功批次判成交易时段过期。
- 市场按60秒、行业按180秒只读刷新；页面隐藏时暂停，恢复时重新获取，并有请求防重入。
- ETF、预备/候选/已确认龙头和历史验证只保留产品位置，明确为阶段5、6、9未启用；禁止Mock。

现场验收快照：

- 市场：`available / partial`，源时间 `2026-07-23T06:57:11Z`。
- 行业：`available / partial`，源时间 `2026-07-23T06:59:10Z`。
- 行业总数83，可用81，不可用2。
- ETF、龙头、历史：`not_enabled / unavailable`。

## 验证证据

- 不打开生产数据库的阶段4后端专项测试：39项通过。
- 前端：ESLint、TypeScript、Next.js生产构建通过。
- `git diff --check`通过。
- 真实Chrome 1920×1080验收：正常、83行业、手动刷新、未启用，以及测试拦截下的失败、真实空榜、交易时段过期均通过；拦截已移除，最终回到真实接口。
- 浏览器控制台0个错误；仅有Next.js开发环境CSS预加载提示。
- 完整后端测试未作为阶段4隔离证据：既有 `radar.runtime → monitoring_health → database.py` 导入链会触发生产库初始化。后续运行全量测试前应先隔离该副作用，不能把“测试通过”误写成“没有打开生产库”。

## 运行服务与影子配置

2026-07-30 生产发布完成后只读复核：

- Node PID 23280 仍监听 `*:4000`，首页HTTP 200；
- Python PID 38620 仍监听 `127.0.0.1:8001`；
- `com.linjian.stock-monitor.fastapi` 中
  `RADAR_ETF_STAGE5_ENABLED=true`；没有
  `RADAR_LEADER_STAGE6_ENABLED`，代码默认值为 `false`；
- 本轮没有停止、重启或重载任何服务，没有修改环境变量或LaunchAgent；
- Vercel生产域名 `https://stock-monitor-murex-one.vercel.app` 已完成新版本发布，首页、`/radar` 和 `/api/health` 均返回200，健康接口四项均为 `healthy`；
- 本轮没有重新确认ngrok状态，也没有读取或写入生产SQLite。

PID和数据会继续变化，下一对话只能现场复核。未经明确授权，不得停止或重启服务、修改运行资产或改写影子数据。

## 固定边界

- 当前页面只读；不得从API或页面写生产数据库。
- 真实0与缺失分开；来源失败、真实空榜、过期和未启用不得互相冒充。
- 成交额单位仍未验证，不暴露未经确认的金额；市场和行业仍是影子观察，不是正式信号。
- 阶段3至少20个交易日影子观察仍继续，不能由阶段4页面验收替代。
- 旧单股AI与未来雷达AI的目录、配置、API、存储、调度和UI状态严格隔离。
- 不保存全市场逐证券原始行情快照。
- 固定本地端口只能使用前端4000、后端8001。
- 未经单独授权，不修改生产数据库、环境变量、依赖、系统启动项，不部署。

## 阶段5H与5I完成记录

- 新增只读 `GET /api/radar/etfs`；总览ETF模块改为真实阶段5状态合同；
- 新增行业ETF页读取官方产品主档、候选组、代表/替代标的、规则版本、覆盖率、源/抓取/页面渲染时间；默认关闭、未就绪、真实空榜、失败和过期语义分开；
- 阶段5及既有雷达相关276项测试通过；前端ESLint、TypeScript、Next.js生产构建通过；`git diff --check`通过；
- 阶段5I生产执行已完成：在线备份、临时副本版本4迁移演练、生产SQLite只增版本4迁移和完整性/外键核对通过；未回填ETF业务数据；
- `RADAR_ETF_STAGE5_ENABLED=true` 已写入后端LaunchAgent并生效；2026-07-28用户授权数据源加固后第二次受控重载8001，现场PID为95079；4000仍为PID 23280且未重载；
- 主机侧只读核对 `/docs`、`/api/radar/overview`、`/api/radar/etfs` 均返回200；ETF模块当前为真实 `not_ready`，没有Mock或正式候选；
- 重载发生在2026-07-26休市日，20日计数尚未开始，将从下一个有效A股交易日（预计2026-07-27）开始；
- 正式 `radar-etf-rule-v1` 仍禁用，只能积累影子审计，不能生成正式候选或交易建议；
- 当前4000/8001服务未停止或重启；2026-07-29 09:31在应用内浏览器打开了前端 `http://localhost:4000/` 和后端 `http://127.0.0.1:8001/docs`，4000仍是既有未重载构建。
- 2026-07-29 AI无效输出修复经用户授权后完成真实付费受控验证：第一次本地注入无效JSON，第二次真实模型调用成功，结果为`attempts=2`和`status=success`。22:00自然轮次中`000725`、`000519`成功，`000021`完成一次受控重试后仍以`APIConnectionError / llm_connection_failed`失败，`retry_attempted=true`，没有再出现`llm_output_invalid`。
- 用户随后确认保持单次60秒和总计最多两次调用；本地实现把重试间隔由1秒改为5秒，并增加仅记录尝试次数、异常类型和底层原因类型的脱敏诊断，不记录错误正文、请求地址或密钥。相关AI、调度健康与提醒回归94项通过，语法检查和`git diff --check`通过。
- 8001受控重载首次`launchctl bootstrap`返回I/O error，管理脚本自动恢复原运行资产并重新启动；当前后端PID为38620、前端仍为PID 23280，8001/4000监听正常，`/docs`与前端首页均返回200。新进程启动时间晚于代码修改时间，新重试策略已加载。
- 2026-07-30 10:30自然AI轮次中，`000725`、`000519`、`000021`三只股票全部成功；没有使用额外付费直连验证。一次性自动核对`10-30-ai`已完成并删除。

## 2026-07-30发布检查点

- 发布前完整后端回归在导入前将默认SQLite连接重定向到 `/private/tmp`，849项测试通过；没有读取或写入生产SQLite。
- 前端健康路由2项测试、ESLint、TypeScript和Next.js生产构建均通过；构建路由包含 `/radar`，`git diff --check`和敏感信息扫描通过。
- 阶段5与阶段6L-D9代码以提交 `49199de` 推送到 `origin/main`；此次推送同时包含此前本地阶段5提交 `bb9c9c5`。
- GitHub提交没有Vercel状态或Actions记录，因此没有依赖自动部署；使用项目既有 `.vercel` 绑定执行生产部署，构建成功并别名到 `https://stock-monitor-murex-one.vercel.app`。
- 线上只读验收：首页200且导航包含“主线雷达”，`/radar`由旧部署404更新为200，`/api/health`为200且 `vercel/tunnel/fastapi/backgroundTasks` 全部健康。
- 生产SQLite仍只有版本1至4，阶段6开关仍未启用，版本5生产迁移未执行；本次发布没有重启4000/8001，没有修改LaunchAgent或生产数据库。

## 当前下一步

1. C4C-2B已于2026-08-10连续交易时段完成免费路径真实验收，当前为`completed / partial`，不得接入运行时。若后续获得RQData试用或交易所市场参与者文件权限，可作同窗对照；未获得前不重复调用免费源伪造“全部正常”结论。
2. D2下一步先审查并修复“冻结候选计划385只、巨潮单批范围上限30只”的合同冲突。只能设计可重放、覆盖候选全集且保持同一计划身份的确定性分片/合并合同；不得任意裁剪30只、抽样替代全集或放宽来源校验。
3. 分片合同通过专项测试后，在下一有效连续交易时段重新生成真实冻结计划并执行受控D2；成功后再由真实人工审核形成至少两个连续D8版本。历史当前仍为47/48完整且`partial`，主营仍需真实官方事实与人工复核工件；不得用Fixture、AI摘要或复制版本补齐。
4. 真实来源通过后，再集中申请版本5生产迁移、阶段6环境开关、受控重载8001和生产影子启用授权。阶段5的20个交易日观察继续并行，不能由开发或测试替代。

## 阶段6A至6I完成记录

- 6A：冻结版本化三级状态机合同，覆盖进入、保持、升级、降级、移出、最短保持、滞回、冷却、数据失败和同股状态唯一。
- 6B：完成确定性评分审计，真实0、缺失、过期、来源失败和首次否决原因严格分开。
- 6C：新增显式可选迁移版本5和 `LeaderRepository`；版本4运行时继续兼容，版本5只在调用方显式选择时应用。
- 6D：新增默认只写影子结果的 `LeaderShadowRunner`，具备进程内防重入、幂等和事务回滚。
- 6E：新增来源身份、时间、覆盖率和业务暴露证据门禁；缺失或不合格来源不补分。
- 6F：新增黄金样本和三级榜单投影，每级最多5只，完整审计仍保留。
- 6G：新增只读 `GET /api/radar/leaders`，状态区分 `not_enabled/not_ready/available/empty/stale/failed`；正式可用快照会被拒绝。
- 6H：前端新增三级梯队契约与页面，复用服务端统一代理；桌面和390px移动端五状态浏览器验收通过，移动端固定导航遮挡已修复。临时预览路由已删除，最终构建没有Mock路由。
- 6I：新增文件型临时SQLite全链路测试，真实应用版本1至5迁移，运行证据门禁与影子状态机，关闭写连接后由FastAPI只读接口返回 `available`；正式可用数量保持0。
- 验证：阶段6相关64项测试通过，后端完整611项测试通过；前端TypeScript、ESLint、Next.js生产构建和 `git diff --check` 通过。
- 2026-07-26用户授权后，前端4000已按原 `screen + npm run start` 方式受控重载；后端8001未重载。生产SQLite未读取或写入。

## 阶段6J完成记录

- 现场复核时HEAD仍为 `bb9c9c553f58cce8208006695827b0368ccb2803`；当前4000 PID为23280，8001 PID为84334；LaunchAgent中 `RADAR_ETF_STAGE5_ENABLED=true`，没有 `RADAR_LEADER_STAGE6_ENABLED`，阶段6保持默认关闭。
- 新增v4文件库在线备份与v5副本迁移演练：源库和SHA-256校验备份保持版本1至4，只有独立演练副本应用版本5；旧 `radar_runs` 记录、迁移账本、完整性和外键均通过，v4回退副本可读。
- 容量探针按83行业、每行业5只、每5分钟48轮/日的极保守上限执行：每天19,920条候选明细和19,920条状态历史，单日增量17,616,896B；20日估算352,796,672B，60日估算1,057,472,512B。
- 同一探针的一天写入约1.55秒，最新快照查询100次均值约1.70ms，18,075,648B数据库在线备份约0.12秒；完整性 `ok`、外键问题0。项目磁盘现场可用约1.8TiB。
- 生产启用硬阻塞：当前 `backend/radar/runtime.py` 和调度注册没有阶段6任务，也没有把既有行情批次、市场/行业聚合和业务暴露证据组装为 `LeaderInputEvidence`；现在迁移版本5并打开开关只会长期返回 `not_ready`。
- 因上述硬阻塞，阶段6J没有读取或写入生产SQLite，没有修改LaunchAgent，没有重载8001；版本5生产迁移和阶段6开关均未执行。
- 验证：迁移/备份/全链路专项23项、阶段6相关65项、后端完整612项通过；`git diff --check`通过。

## 阶段6K完成记录

- 新增纯计算 `backend/radar/leader_runtime_inputs.py`：只消费市场任务已经取得的同一内存全市场行情批次、当前市场聚合、不晚于本轮 `as_of` 的最近行业聚合、精确行业版本映射和证券主档；不发起第二次全市场行情请求，不连接数据库，不读取环境变量。
- 阶段6作为现有市场影子任务的默认关闭同批次后处理器接入，不新增独立抓取任务。只有 `RADAR_LEADER_STAGE6_ENABLED=true` 时才构造处理器；处理器失败、版本5存储未就绪或输入门禁拒绝不会改变既有市场任务的运行结果和健康状态。
- 第一版运行时范围只保留上交所和深交所证券，明确排除北交所；按已接受的精确行业映射分组，每行业只保留当轮涨跌幅前5名作为研究性输入。涨跌幅只用于缩小审计输入，不直接决定龙头状态。
- 当前正式行业状态、股票硬过滤、连续性、回流、可交易性、风险和业务暴露证据仍未就绪，因此六个评分维度全部保持缺失、未验证或不适用，`score`不补值，真实0原样保存，`formal_usable=false`。运行链可以保存审计快照，但只读API继续返回真实 `not_ready`，不会把全部不可评分或降级零候选冒充真实空榜。
- 新增严格时点边界：拒绝未来市场/行业输入，行业映射按精确 `industry_release_id` 读取，上一状态只读取本轮 `as_of` 之前的记录；只读API拒绝明显未来的业务快照，并使用业务 `asOf` 而不是写入时间判断新鲜度。
- 阶段6错误日志只记录运行标识和聚合原因，不输出证券代码、名称、行情字段或异常堆栈；阶段6任务单独进入 `radarLeaderStage6` 健康状态。
- 验证：阶段6相关73项、后端完整622项通过；相关Python语法编译通过。完整回归中的外部抓取失败文本来自既有受控降级测试，最终结果为 `OK`。
- 本阶段没有读取或写入生产SQLite，没有执行版本5生产迁移，没有修改LaunchAgent或环境变量，没有重载4000/8001，没有执行Git暂存、提交或推送。

## 阶段6L-A完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-07-26-stage6l-a-leader-research-features-design.md` 和 `docs/superpowers/plans/2026-07-26-stage6l-a-leader-research-features.md`；均为本地未提交文件。
- 新增纯计算 `backend/radar/leader_research_features.py`，公式版本为 `radar-leader-research-feature-v1`。当前只计算行业强度25分、市场领先性25分和量比辅助5分，并输出 `researchPartialScore`、`participatingWeight=55`、`requiredFormalWeight=95`、`scoreReady=false`。
- 行业强度由正向行业等权收益横截面、上涨广度和正向剔除首股收益横截面组成；市场领先性由完整行业内涨幅排名、正向行业贡献排名和所属板块指数超额排名组成；量比小于等于1保留为真实0分。
- 所有百分位要求有限数值、唯一且同一运行的至少20个行业、至少2只行业成分和至少100只沪深A股；并列值使用平均百分位。未知板块、过期/未来数据、来源降级、市场聚合不完整、重复行业、`NaN/Inf` 和样本不足均不计分。
- 候选先执行时间和有限数门禁，再按行业涨跌幅取前5名；过期或非有限高涨幅不会占位。北交所既不进入候选，也不进入行业或全市场分母。
- 全市场指数超额和量比分母每批次只预计算一次，全部候选复用。无网络、无数据库的5,530只股票、83行业、415候选合成容量演练耗时约0.476秒。
- 研究结果只写入 `evidence.researchFeatures`；现有六维正式评分输入继续无分，全部硬门槛继续为假，状态机保持 `blocked/out`。新增API测试确认覆盖率为0时只读接口返回 `not_ready`，不会向榜单暴露嵌套研究分。
- 阶段6L-A新增和扩展测试后，龙头专项95项、后端完整644项通过；相关Python语法编译和 `git diff --check` 通过。完整回归中的外部抓取失败文本仍是既有受控降级测试，最终结果为 `OK`。
- 本阶段没有读取或写入生产SQLite，没有执行版本5生产迁移，没有修改LaunchAgent或环境变量，没有停止或重载4000/8001，没有执行Git暂存、提交或推送。

## 阶段6L-B1完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-07-27-stage6l-b1-adjusted-history-features-design.md` 和 `docs/superpowers/plans/2026-07-27-stage6l-b1-adjusted-history-features.md`；均为本地未提交文件。
- 新增纯计算 `backend/radar/leader_history_features.py`，公式版本为 `radar-leader-history-feature-v1`；调用方必须显式提供候选前复权价格、按当时行业成员计算的行业价格收益基准、连续板块指数，以及至少21个逐日对齐的预期交易日。
- 合同拒绝日期断档或错位、非正数或非有限价格、错误复权/收益口径、未来时间、抓取早于来源时间和来源失败；真实0保留，失败不补0，证据不保存完整价格序列。
- 原始研究证据计算3/5/10/20日价格收益、相对行业和板块指数超额、最近5/10日跑赢天数、20日最大回撤、抗跌差值和谷底修复；无实际回撤时修复保持缺失。
- 运行时新增可选 `history_inputs_by_symbol`，默认继续 `continuity_history_missing`；历史证据可用时只写 `researchFeatures.historyContinuity`，正式维度仍无分并标记 `continuity_research_only`，`continuity_passed=false`、状态机继续 `blocked/out`、API继续 `not_ready`。
- 专项纯函数8项、运行时11项和临时SQLite隔离下全部龙头105项测试通过；相关Python语法编译与 `git diff --check` 通过。没有访问外部历史源，没有读取或写入生产SQLite，没有新增迁移、依赖、调度、环境变量、API或前端，也没有执行Git暂存、提交或推送。

## 阶段6L-B2完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-07-27-stage6l-b2-liquidity-tradability-design.md` 和 `docs/superpowers/plans/2026-07-27-stage6l-b2-liquidity-tradability.md`；均为本地未提交文件。
- 腾讯行情字段37的万元口径已用同一响应字段35的精确人民币元金额交叉校验；只有两个非负有限值误差不超过10000元时，才保存精确 `turnoverAmountCny` 并标记单位已验证。原始 `turnoverAmountSource` 和既有覆盖率语义保持兼容，真实0保留，缺失、负值和超差保持未验证。
- 新增纯计算 `backend/radar/leader_liquidity_features.py`，公式版本为 `radar-leader-liquidity-feature-v1`；只输出当期人民币元成交额、换手率和缺失状态，不抓取网络、不连接数据库。
- 运行时新增 `researchFeatures.liquidityTradability`；同时间历史基线、停牌状态、涨跌停、一字板和可信价差仍缺失或未验证，所以 `scoreReady=false`、`formalUsable=false`、正式维度无分，`liquidity_passed=false`、`tradability_passed=false`，状态机和只读API继续保持真实阻断。
- 来源与纯函数21项、运行时12项、临时SQLite隔离下全部龙头112项和后端完整664项测试通过；相关Python语法编译通过。完整回归中的外部抓取失败文本仍是既有受控降级测试，最终结果为 `OK`。
- 本阶段没有读取或写入生产SQLite，没有新增迁移、依赖、调度、环境变量、API或前端，没有停止或重载4000/8001，也没有执行Git暂存、提交或推送。

## 阶段6L-B3完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-07-27-stage6l-b3-business-catalyst-evidence-design.md` 和 `docs/superpowers/plans/2026-07-27-stage6l-b3-business-catalyst-evidence.md`；均为本地未提交文件。
- 新增纯计算 `backend/radar/leader_business_catalyst_features.py`，公式版本为 `radar-leader-business-catalyst-feature-v1`；只接受公司/交易所/指定披露平台官方材料，或明确引用官方材料的版本化人工审核映射。
- 合同校验证券、行业、发布版本、催化、文档、时间、有效期、引用、冲突和当前版本；未来、未生效、过期、身份错配、重复、审核依据过期、AI审核和非HTTPS来源均不会生成可用证据。
- 输出只保留压缩文档身份、版本和有效期，不输出完整摘要；直接关联和人工确认的高度相关可以成为研究证据，未确认和反证语义严格分开。
- 运行时新增可选 `business_catalyst_inputs_by_symbol`，结果只进入 `researchFeatures.businessCatalyst`；固定 `scoreReady=false`、`formalUsable=false`、`researchScore=null`，正式主营维度、来源合同、门禁、状态机和只读API继续保持缺失/阻断/`not_ready`。
- B3纯函数与运行时24项、临时SQLite隔离下全部龙头124项、后端完整676项测试通过；相关Python语法编译和 `git diff --check` 通过。
- 本阶段没有读取或写入生产SQLite，没有新增迁移、依赖、调度、环境变量、API或前端，没有停止或重载4000/8001，也没有执行Git暂存、提交或推送。

## 阶段6L-C1完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-07-27-stage6l-c1-security-tradability-contract-design.md` 和 `docs/superpowers/plans/2026-07-27-stage6l-c1-security-tradability-contract.md`；均为本地未提交文件。
- `QuoteSnapshot` 和腾讯全市场行情适配器从现有同一响应增加昨收、开盘、最高和最低价；没有增加请求次数，也没有把新增字段提前加入现有批次必填覆盖率。
- 新增纯计算 `backend/radar/leader_tradability_features.py`，公式版本为 `radar-leader-tradability-feature-v1`；显式消费版本化证券生命周期、当日交易状态和当日涨跌停规则，不从简称猜ST，不按固定百分比计算边界。
- 合同区分正常、ST、*ST、退市整理、重新上市、异常、停牌、上市前5日、第6至10日、无涨跌幅限制、普通涨跌停和一字涨跌停；缺失、过期、来源失败、未来时间、身份/批次错配、非法HTTPS、规则冲突、字符串伪枚举和OHLC矛盾均有稳定降级语义。
- 运行时新增可选 `tradability_inputs_by_symbol`，结果只进入 `researchFeatures.securityTradability`；正式15分、`stock_gate_passed`、`liquidity_passed`、`tradability_passed`、风险门禁和状态机继续保持关闭。
- C1来源/纯函数/运行时专项39项、临时SQLite隔离下全部龙头139项、后端完整693项测试通过；相关Python语法编译和 `git diff --check` 通过。
- 4000/8001仍为原PID `23280/84334`；本阶段没有读取或写入生产SQLite，没有新增迁移、仓储、依赖、API、前端、调度或环境变量，没有停止或重载服务，也没有执行Git暂存、提交或推送。

## 阶段6L-C2完成记录

- 设计与执行记录分别保存在 `docs/superpowers/specs/2026-07-27-stage6l-c2-tradability-source-poc-design.md` 和 `docs/superpowers/plans/2026-07-27-stage6l-c2-tradability-source-poc.md`；均为本地未提交文件。
- 官方主档只读实测取得上交所主板1,698只、科创板612只和深交所A股2,892只，三组上市日期完整度均为100%；公开标准字段没有带生效区间的风险警示、停复牌和重新上市状态。
- 上交所原始列表的状态码与当前ST简称数量相关，但缺公开编码合同、状态时间和有效区间；深交所公开Excel没有独立状态字段。简称前缀只允许人工核对，不得作为正式证据。
- 终止上市只读实测上交所159条、深交所208条，深交所暂停上市列表为真实空列表；上交所原始`DELIST_DATE`被现有AKShare命名为“暂停上市日期”，接入前必须纠正语义。交易所公告和深交所月报可补历史事实，但不能承担盘中全市场正常/停牌快照。
- 沪深交易所均于2026-04-24发布2026年交易规则，并自2026-07-06施行；主板风险警示股由5%调整为10%，主板普通股10%，科创板/创业板20%，IPO后前5个交易日、重新上市首日和退市整理首日不设涨跌幅限制。历史回放必须按生效日期选择2023/2026版本。
- 深交所`securities.xml`与`cashauctionparams.xml`、上交所`cpxx0201/0202`及盘中停牌文件是字段粒度最符合C1的官方参考数据，但通过市场参与者文件/行情网关发布；当前项目没有对应访问权限，只能登记为正式候选来源，不能伪造可用批次。
- 腾讯同一响应字段47/48的六只跨板块样本与当日10%/20%规则一致，可作为辅助校验；腾讯不是官方状态源。沪深5,202只全量覆盖探针因外部只读命令授权额度到达上限未执行，不能宣称100%覆盖。
- 来源等级和冲突策略已冻结：官方参考文件优先，正式公告可修正事实，腾讯只做交叉校验；任何冲突、身份/交易日/源时间/有效期缺失均降级，未取得官方停复牌证据时保持`unknown`，不从简称、零成交或无行情推断。
- C1继续`scoreReady=false`、`formalUsable=false`，正式可交易性门禁、状态机和API结果不变。C2没有修改核心代码，没有读取或写入生产SQLite，没有重载4000/8001，没有执行Git暂存、提交或推送。

## 阶段6L-C3完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-07-28-stage6l-c3-tradability-source-contract-design.md` 和 `docs/superpowers/plans/2026-07-28-stage6l-c3-tradability-source-contract.md`；均为本地未提交文件。
- 新增纯计算 `backend/radar/leader_tradability_sources.py`，规则目录只覆盖2023-04-10起的沪深2023/2026规则版本；2026-07-05以前主板风险警示股为5%，自2026-07-06起为10%，科创板/创业板为20%，更早历史和未知板块明确拒绝。
- 规则目录保存官方文号、发布日、生效/失效区间和HTTPS发布页；上下限按前收盘价和0.01元最小单位十进制四舍五入。上市前5个交易日、重新上市首日和退市整理期首日解析为无涨跌幅限制。
- `QuoteSnapshot`和腾讯适配器从同一响应可选解析字段47/48为`upperLimitPriceSource`和`lowerLimitPriceSource`；不增加请求次数，不加入既有必填覆盖率，真零与缺失分开，非法、负值和非有限数保持缺失。
- 新增严格 `OfficialDailyTradabilityReference` 和五级来源准入。只有`official_primary`且证券、板块、交易日、生命周期、交易状态、规则边界、时间和HTTPS来源完整一致时才生成C1输入；无真实参考文件返回`official_reference_source_unavailable`，任何官方/目录/生命周期/腾讯冲突均降级。
- 来源组装在返回`ready`前执行C1合同预检；输出固定`scoreReady=false`、`formalUsable=false`。现有运行时继续通过可选`tradability_inputs_by_symbol`接收输入，`stock_gate_passed`、`liquidity_passed`和`tradability_passed`仍为假，状态机和只读API不变。
- TDD专项先确认缺失模块、缺失字段和C1预检边界失败，再完成实现。最终C3来源专项14项、龙头全量154项、后端完整709项通过；所有导入`main`的测试均先把`database.DB_PATH`重定向到初始化后的临时SQLite。相关语法编译和`git diff --check`通过。
- 本阶段没有读取或写入生产SQLite，没有新增迁移、仓储、调度、API、前端、依赖或环境变量，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交或推送。阶段5观察继续并行，2026-07-27仍只记“已观察、日结证据不完整”，不计合格日。

## 阶段6L-D1完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-07-28-stage6l-d1-risk-invalidation-evidence-design.md` 和 `docs/superpowers/plans/2026-07-28-stage6l-d1-risk-invalidation-evidence.md`；均为本地未提交文件。
- 新增纯计算 `backend/radar/leader_risk_invalidation_features.py`，统一覆盖减持、解禁、监管、调查、诉讼、业绩和审计七类风险；主营与催化关联被证伪继续由6L-B3负责，不重复定义。
- 合同冻结来源合同 `radar-leader-risk-official-coverage-v1`、适配器合同 `radar-leader-risk-official-coverage-adapter-v1` 和官方域名注册表；覆盖必须从证券上市时间连续查询、完成开放事件结转，并满足最长24小时的新鲜度和有效期。空事件只有在七类来源、身份、HTTPS原文和覆盖证明全部通过时才能表达“当前未观察到有效风险”；空榜、覆盖不全、过期和来源失败不能冒充无风险。
- 风险事件使用明确类别子类型和版本；调查/诉讼必须有案件身份，减持/解禁必须有明确结束区间，业绩/审计必须有报告期。解除证据必须精确引用 `event_id + event_version`；`CORRECTED` 当前不能关闭风险，而是降级等待D2冻结替代版本链。未来证据、身份错配、非官方来源、重复文档、冲突版本和无效解除引用统一降级。
- 输出只保存安全URL、哈希文档/案件引用、压缩身份、有效类别和解除关系，不保存完整事实摘要或URL查询参数，不接受调用方自定义严重程度；固定 `scoreReady=false`、`formalUsable=false`、`researchScore=null`，不接运行时、不改变 `risk_filter_passed`、评分、状态机、API或前端。
- 严格TDD先确认模块缺失和新增边界用例失败，再完成实现。多轮独立复审发现的覆盖、来源、版本、更正、敏感输出和非法嵌套类型阻断项均已修复，最终复审无P1/P2阻断。D1专项22项、龙头全量176项、后端完整737项通过；相关语法编译通过。完整回归中的外部抓取失败文本来自既有受控失败测试，最终结果为 `OK`。
- 本阶段没有读取或写入生产SQLite，没有新增迁移、仓储、调度、API、前端、依赖或环境变量，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交或推送。

## 阶段6L-D2完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-07-28-stage6l-d2-official-risk-source-poc-design.md` 和 `docs/superpowers/plans/2026-07-28-stage6l-d2-official-risk-source-poc.md`；均为本地未提交文件。
- 新增隔离来源模块 `backend/radar/sources/leader_risk_official.py`：只通过HTTPS和有界超时发现、校验并归一化巨潮官方公告元数据，不下载PDF正文、不连接数据库、不生成D1正式风险事件、解除证据或覆盖证明。查询关键词只保存为候选类别上下文，所有文档固定 `formal_usable=false`。
- 七类关键词真实只读探针均取得代表公告；记录数只表示全文检索命中。更正、修正、提前终止和处罚关键词的元数据没有稳定的原事件或原版本引用；证监会与审计原文可补文号、案号、报告期和结论，但不能单独证明上市以来全覆盖。
- 适配器真实核验“立案告知书”时，巨潮返回343条、114页、`hasMore=true`和3条本页文档，按每页3条计算应为115页。模块保留3条通过身份、时间和官方URL校验的文档，同时记录 `cninfo_response_pagination_inconsistent`，批次保持 `partial`，完整覆盖、开放事件继承、更正关系和正式可用性全部为假。
- 严格TDD先确认来源模块缺失，再覆盖正常、真实空结果、非法响应、未来/窗口外时间、路径穿越、公告ID与PDF身份错配、重复身份、短页/越界页、关联语义、传输失败和真实分页不一致。独立复核发现的3个P2边界均已修复，第二轮复核无P1/P2阻断。D2专项15项、龙头全量191项、后端完整752项通过；相关Python语法编译和 `git diff --check` 通过。
- 本阶段没有读取或写入生产SQLite，没有接迁移、仓储、运行时、调度、API、前端、提醒或正式门禁，没有新增依赖或环境变量，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交或推送。阶段5观察继续并行。

## 阶段6L-D3完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-07-28-stage6l-d3-risk-document-facts-design.md` 和 `docs/superpowers/plans/2026-07-28-stage6l-d3-risk-document-facts.md`；均为本地未提交文件。
- 新增纯计算 `backend/radar/leader_risk_document_facts.py`：只消费D2官方文档元数据和已解码的有界逐页文本，确定性提取带明确标签的案号、报告期、审计报告号、原公告编号、生效日期和实施区间；不负责下载、OCR或存储正文。案号和审计报告号只输出SHA-256身份，事实只保存归一化值、页码、事实ID和片段哈希。
- 新增版本化 `manual` 人工关联合同。`supersedes/resolves` 必须精确引用当前文档事实、目标 `event_id + event_version` 和原公告；调查/诉讼还必须匹配案号，业绩/审计还必须匹配报告期。AI审核、自关联、非前序事件、错证券/发行人/文档、缺事实、未来审核、冲突或两份同时有效映射全部降级；历史映射过期且存在当前映射时只忽略历史版本。
- 接受的关系仍只是研究关系，不生成 `LeaderRiskEventEvidence` 或 `LeaderRiskResolutionEvidence`，不修改D1 `CORRECTED` 阻断语义；固定 `formal_usable=false`、`correction_links_complete=false`，不建立历史覆盖证明。
- 三份巨潮官方PDF仅在内存中完成真实只读解析：`cninfo:1225443882`（1页）、`cninfo:1214867413`（1页）和 `cninfo:1224836470`（16页）。三份正文均未满足冻结的明确标签格式，结果如实为 `missing`，没有因标题或全文检索命中猜事实；Fixture只证明正向合同可复现，不能冒充真实来源覆盖。
- 严格TDD先确认模块缺失，再修复D1案号身份兼容和畸形嵌套值；独立复核发现的过期映射并存、自关联/非前序事件、畸形URL和编号泄露4个P2均已修复，第二轮复核无P1/P2阻断。D3专项19项、D1-D3联合56项、龙头全量210项、后端完整771项通过；相关Python语法编译和 `git diff --check` 通过。
- 本阶段没有读取或写入生产SQLite，没有保存官方PDF或正文，没有接迁移、仓储、运行时、调度、API、前端、提醒或正式门禁，没有新增依赖或环境变量，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交或推送。阶段5观察继续并行。

## 阶段6L-D4完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-07-29-stage6l-d4-risk-document-content-design.md` 和 `docs/superpowers/plans/2026-07-29-stage6l-d4-risk-document-content.md`；均为本地未提交文件。
- 新增隔离来源模块 `backend/radar/sources/leader_risk_document_content.py`：只接受D2巨潮官方HTTPS PDF身份，使用禁代理继承、20秒超时、禁重定向和流式读取；声明长度与实际字节都限制为10MiB，最终URL、内容类型、长度、`%PDF-`文件头和文档ID必须一致。
- 使用项目已有 `pypdf` 只在内存解码，拒绝加密、损坏、零页和超过200页的PDF；单页最多100,000字符、总计最多2,000,000字符。响应字节和页文本从 `repr` 隐藏，不创建临时文件，不保存正文。
- D3无明确事实时生成 `fact_extraction_missing`，有事实但缺人工关系时生成 `relation_review_required`；候选只保存稳定身份、PDF哈希、字节数、页数、事实ID/类型和原因码。身份、哈希、空身份、伪造ready结果、畸形关系或来源失败都不能生成候选；已有有效关系则不重复创建待办。
- 真实巨潮 `cninfo:1225443882` 仅在内存中完成探针：125,092字节、1页、SHA-256为 `00d21192fe1280c8940027cfda9e4272b1d66a9d52af5a2774e4747789ff563d`，D4为`ready`；D3如实为`missing/risk_document_facts_missing`，D4生成`fact_extraction_missing`，全部保持`formal_usable=false`。
- 严格TDD先确认模块缺失，再覆盖官方身份、重定向、状态、类型、文件头、大小、加密/损坏、页数、解析异常、字符上限、候选身份/哈希/状态和输出泄露。复核发现并修复了非标准巨潮路径、伪造空页ready结果、畸形关系跳过审核、解析异常外泄和双空身份相等5类边界。
- D4专项13项、D3-D4联合32项、D1-D4联合69项、龙头全量223项、后端完整784项通过；最终语法检查和差异检查须以当前工作树最新执行结果为准。
- 本阶段没有读取或写入生产SQLite，没有保存官方PDF或正文，没有接迁移、仓储、运行时、调度、API、前端、提醒或正式门禁，没有新增依赖或环境变量，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交或推送。阶段5观察继续并行。

## 阶段6L-D5完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-07-29-stage6l-d5-manual-risk-review-artifacts-design.md` 和 `docs/superpowers/plans/2026-07-29-stage6l-d5-manual-risk-review-artifacts.md`；均为本地未提交文件。
- 新增纯计算 `backend/radar/leader_risk_review_artifacts.py`。每次审核先使用现有D3和D4重新构造事实结果与人工候选，文档、证券、发行人、PDF哈希、候选类型或原因码漂移时拒绝，不信任调用方直接提供的dataclass。
- `fact_extraction_missing` 只接受当前D4页文本中真实存在的人工片段，支持案号、报告期、审计报告号、原公告编号、生效日期和实施区间六类归一化；案号和审计报告号只保存哈希身份，原始值和片段不进入结果或`repr`。错页、错片段、值不在片段、非法值、重复和同类冲突事实均拒绝。
- `relation_review_required` 不接受同时补录事实；人工关系必须与D5审核人、时间、有效期和版本一致，并使用当前D2文档、D4页、D1事件版本重新调用D3。只有D3再次接受的关系才进入压缩工件，`decision_summary`不进入输出。
- 审核工件ID由候选稳定生成；首版不得声明前序版本，后续版本必须精确替代同一候选的最后版本且审核时间递增。重复、分叉、跨候选、伪造历史审核人/载荷和非法过期提交均拒绝。
- 严格TDD先确认模块缺失，再分三轮修复历史载荷复验、状态优先级、D1事件摘要`repr`泄露、同类事实冲突和过期非法载荷语义。D5专项10项、D1-D5联合79项、龙头全量233项、后端完整794项通过；最终语法和差异检查须以当前工作树最新执行结果为准。
- 所有工件固定 `formal_usable=false`、`applied_to_d3=false`、`applied_to_d1=false`。本阶段没有运行真实人工审核，没有读取或写入生产SQLite，没有保存PDF、正文、人工原始值或片段，没有接迁移、仓储、运行时、调度、API、前端、提醒或正式门禁，没有新增依赖或环境变量，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交或推送。阶段5观察继续并行。

## 阶段6L-D6完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-07-29-stage6l-d6-risk-review-replay-design.md` 和 `docs/superpowers/plans/2026-07-29-stage6l-d6-risk-review-replay.md`；均为本地未提交文件。
- 新增纯计算 `backend/radar/leader_risk_review_replay.py`。输入D2文档、D4内容、未带人工关系的D3基础事实、D1事件版本和D5工件链；每次重新计算D3基础结果并重建D4候选，任何事实、身份或哈希漂移均拒绝。
- D5工件链必须同候选、同工件ID、同文档/证券/发行人/PDF哈希，首版无前序、后续精确引用上一版、审核时间严格递增且不晚于`as_of`。只选择最后版本；最后版本过期返回`stale`，绝不回退已被替代的旧版本。
- 人工事实重新校验手工来源版本、事实ID、归一化格式、页码和片段哈希；D3确定性事实优先，相同事实只保留确定性版本，不同值冲突拒绝。输出分别保留`deterministic_facts/manual_facts/facts`，原D3对象不变。
- 接受关系重新核对映射版本、审核时间、当前文档、目标事件版本/文档/证券/发行人和全部D3事实依据；调查/诉讼匹配案号，业绩/审计匹配报告期。错目标、错依据、畸形嵌套标识和`None`关系集合稳定降级。
- 严格TDD先确认模块缺失，再分两轮修复畸形关系标识和空关系集合异常。D6专项9项、D1-D6联合88项、龙头全量242项、后端完整803项通过；相关Python语法和差异卫生检查通过。
- 研究视图固定 `formal_usable=false`、`d3_mutated=false`、`applied_to_d1=false`、`correction_links_complete=false`。本阶段没有运行真实工件重放，没有读取或写入生产SQLite，没有保存PDF、正文、人工原始值或片段，没有接迁移、仓储、运行时、调度、API、前端、提醒或正式门禁，没有新增依赖或环境变量，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交或推送。阶段5观察继续并行。

## 阶段6L-D7完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-07-29-stage6l-d7-supplemented-risk-relation-design.md` 和 `docs/superpowers/plans/2026-07-29-stage6l-d7-supplemented-risk-relation.md`；均为本地未提交文件。
- 新增纯计算 `backend/radar/leader_risk_supplemented_relation.py`。输入完整D6重放输入、调用方持有的D6结果和一次人工关系审核；入口先重新执行D6并逐字段比对，伪造或过期研究视图不能进入关系审核。
- 只接受当前 `fact_extraction_missing` 工件形成的 `ready` D6视图，必须存在人工补录事实且尚无关系。审核方式固定为人工，审核时间必须晚于当前D5工件且不晚于`as_of`；过期审核返回`stale`。
- 关系必须精确命中D1事件ID和事件版本、目标文档、证券和发行人，目标事件早于当前文档；全部依据必须存在于D6合并事实，且至少引用一个人工补录事实。调查/诉讼必须匹配案号，业绩/审计必须匹配报告期，`resolves`与`supersedes`使用不同的替代版本规则。
- 输出使用D7独立 `AcceptedSupplementedRiskDocumentRelation`，保留D5工件ID/版本、D6重放版本、全部依据和人工依据子集，不复用D3接受关系类型。输入和输出`repr`不包含PDF页文本、D1事件摘要或人工审核摘要。
- 严格TDD先确认D7模块缺失。D7专项9项、D1-D7联合97项、龙头全量251项、后端完整812项通过；相关Python语法编译和`git diff --check`通过。完整回归中的损坏PDF、外部来源失败、任务锁和LLM不可用文本来自既有受控失败测试，最终结果为`OK`。
- D7结果与关系固定 `formal_usable=false`、`applied_to_d3=false`、`applied_to_d1=false`。本阶段没有运行真实人工审核，没有读取或写入生产SQLite，没有保存PDF、正文、人工原始值或片段，没有接迁移、仓储、运行时、调度、API、前端、提醒或正式门禁，没有新增依赖或环境变量，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交或推送。阶段5观察继续并行。

## 阶段6L-D8完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-07-29-stage6l-d8-risk-evidence-bundle-design.md` 和 `docs/superpowers/plans/2026-07-29-stage6l-d8-risk-evidence-bundle.md`；均为本地未提交文件。
- 新增纯计算 `backend/radar/leader_risk_evidence_bundle.py`。输入完整D7关系复核输入和调用方持有的D7结果；入口重新执行D7并逐字段比对，因此D2文档、D4内容哈希、D3事实、D5工件链、D6研究视图和D7关系继续处于同一条可重算链。
- 冻结 `RiskResearchEvidenceBundle` 只保存D2-D7压缩引用：来源/内容合同、文档与内容哈希、确定性/人工/合并事实ID、D5工件ID和版本、D6重放版本、D7关系与目标D1事件精确版本、事实依据以及稳定证据包ID。不保存PDF页文本、URL、标题、事件摘要、审核摘要、人工原始值或片段。
- 证据包研究状态可为`ready`，但正式门禁固定关闭。D8明确输出来源文档、正文、事实结果、完整更正链、研究重放、研究关系、D3应用和D1正式解除证据八项当前缺口；结果与证据包固定 `formal_gate_ready=false`、`formal_usable=false`、`applied_to_d3=false`、`applied_to_d1=false`。
- 严格TDD先确认D8模块缺失。D8专项8项、D1-D8联合105项、龙头全量259项、后端完整820项通过；相关Python语法、敏感调用搜索和`git diff --check`通过。完整回归中的损坏PDF、外部来源失败、任务锁和LLM不可用文本来自既有受控失败测试，最终结果为`OK`。
- 本阶段没有生成D1正式解除证据，没有运行真实人工审核，没有读取或写入生产SQLite，没有接迁移、仓储、运行时、调度、API、前端或提醒，没有新增依赖或环境变量，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交或推送。阶段5观察继续并行。

## 阶段6L-D9完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-07-29-stage6l-d9-risk-evidence-bundle-version-audit-design.md` 和 `docs/superpowers/plans/2026-07-29-stage6l-d9-risk-evidence-bundle-version-audit.md`；均为本地未提交文件。
- 新增纯计算 `backend/radar/leader_risk_evidence_bundle_audit.py`。输入只包含按旧到新排列的至少两个D8冻结证据包；D9校验D8合同、唯一包ID、带时区且严格递增的构建时间、同来源文档/内容快照/目标事件身份和固定关闭的正式标志，不从版本名称推断替代关系。
- 冻结差异合同按相邻版本分别输出确定性/人工/合并事实ID增删、人工工件字段变化、研究关系字段与依据事实ID变化、正式门禁缺口增删；最后一个合法包被选择为当前研究版本。三个及以上版本保持逐对差异，不跨版本压缩。
- 混入其他文档或内容快照、乱序、重复包ID、伪造D8合同、重复门禁缺口，以及只改变`built_at`和`bundle_id`而没有实质证据变化的后继包均整体返回不可验证，不输出部分当前版本或部分差异。D9不自动合并冲突、不声称历史完整，不修改D1/D3/D8。
- 严格TDD先确认D9模块缺失。D9专项8项、D1-D9联合113项、龙头全量267项通过；后端完整回归在导入前把默认生产数据库路径拦截并重定向到`/private/tmp`后846项通过。相关Python语法、敏感依赖搜索和`git diff --check`通过；损坏PDF、外部来源失败、任务锁和LLM不可用文本均为既有受控失败测试，最终结果为`OK`。
- 边界事件：第一次执行后端完整回归时没有先重定向数据库路径，`database.py`导入导致默认生产SQLite发生一次幂等初始化连接和建表/加列尝试。发现后立即停止同类命令，没有读取生产业务数据，也没有打开数据库核对；随后使用导入前`sqlite3.connect`路径拦截和临时数据库重跑完整846项。后续任何会导入`database`或`main`的完整测试必须继续使用这一重定向方式。
- D9模块本身没有数据库、网络、AI、仓储、调度、API或前端依赖；本阶段没有新增迁移、依赖、环境变量或运行开关，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交或推送。阶段5观察和22:00 AI自然轮次验收继续并行。

## 阶段6L-E1完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-07-30-stage6l-e1-risk-candidate-projection-design.md` 和 `docs/superpowers/plans/2026-07-30-stage6l-e1-risk-candidate-projection.md`。
- 新增纯计算 `backend/radar/leader_risk_candidate_projection.py`。输入候选证券、发行人、批次时点、D9原始输入和调用方审计结果；入口重新执行D9并逐字段比对，伪造结果不能进入候选证据。
- 当前D8包的证券、发行人、构建时间、内容抓取时间和目标事件发布时间必须与候选身份及 `as_of` 一致；身份冲突、未来数据、D9历史不足或来源不可验证均稳定拒绝，不输出部分投影。
- 冻结投影通过 `to_evidence()` 输出E1/D9/D8合同、候选身份、有序包ID、当前包、压缩事实/工件/关系身份、相邻版本差异和全部正式门禁缺口；不保存URL、标题、正文、事件摘要、人工审核摘要、人工原始值或PDF片段。
- `risk_filter_passed`、`formal_gate_ready`、`formal_usable`、`applied_to_d3`和`applied_to_d1`固定为`false`，没有可开启参数。E1只生成研究证据，没有修改 `LeaderInputEvidence`、运行时、评分或状态机。
- 严格TDD先确认模块缺失。E1专项7项、D1-E1联合120项、龙头全量274项通过；完整后端回归在导入前把生产数据库连接重定向到 `/private/tmp` 后856项通过。相关Python语法、敏感依赖搜索和差异卫生检查通过。
- 本阶段没有读取或写入生产SQLite，没有连接网络、仓储、调度、API、前端、提醒或AI，没有新增迁移、依赖、环境变量或运行开关，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交、推送或部署。阶段5观察继续并行。

## 阶段6L-E2完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-07-30-stage6l-e2-risk-runtime-projection-design.md` 和 `docs/superpowers/plans/2026-07-30-stage6l-e2-risk-runtime-projection.md`。
- `backend/radar/leader_runtime_inputs.py` 新增可选 `risk_candidate_projections_by_symbol`。运行时只消费调用方显式提供且已冻结的 `LeaderRiskCandidateProjection`，不在候选循环中重算D9，不自动发现文档或抓取来源。
- 每只候选独立校验证券、带时区且精确匹配的批次时点、E1合同版本，以及 `risk_filter_passed/formal_gate_ready/formal_usable/applied_to_d3/applied_to_d1` 五个标志严格为 `false`。缺失、身份冲突、批次冲突、合同不可信和伪造正式标志只降级当前候选，不污染其他候选。
- 结果写入 `researchFeatures.riskCandidateProjection`；运行时输入合同从 `radar-leader-runtime-input-v1` 升为 `v2`，其余既有字段保持兼容。合法投影也继续保留 `invalidation.missingPrerequisites=risk_evidence`，正式风险门禁、评分和状态机均未开启。
- 严格TDD先确认新参数不存在。E1-E2专项32项、龙头全量278项通过；完整后端回归在导入 `database` 后立刻把 `DB_PATH` 重定向到 `/private/tmp`、初始化临时基础表后860项通过。第一次完整回归的10个失败均来自临时库未初始化基础表，修正测试夹具后全部通过。
- 相关Python语法、敏感依赖搜索和 `git diff --check` 通过。2026-07-30 13:40只读核对4000仍为PID 23280、8001仍为PID 38620，首页和 `/docs` 均返回200；没有停止、重启或重载服务。
- 本阶段没有读取或写入生产SQLite，没有连接网络、仓储、调度、API、前端、提醒或AI，没有新增迁移、依赖、环境变量或运行开关，也没有执行Git暂存、提交、推送或部署。阶段5观察继续并行。

## 阶段6L-E3完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-07-30-stage6l-e3-risk-projection-batch-design.md` 和 `docs/superpowers/plans/2026-07-30-stage6l-e3-risk-projection-batch.md`。
- 新增纯计算 `backend/radar/leader_risk_candidate_projection_batch.py`。输入是统一带时区 `as_of` 和有序证券条目；每条只接受隐藏在 `repr` 之外的 E1 原始输入，并逐只重新执行 E1，不接受调用方预制投影结果，不直接调用 E2 运行时。
- 映射证券或 E1 输入证券重复会整体拒绝，避免最后写入覆盖；单条载荷错误、身份冲突、批次时点冲突、D9伪造、E1非就绪或未预期异常只降级当前条目。结果状态区分 `ready/partial/missing/stale/source_failed/source_unverified`，只读 `projections_by_symbol` 只包含合法投影。
- 批次审计只保存索引、证券、输入证券、状态、稳定原因、是否纳入和投影合同ID，不重复嵌入完整E1投影。批次、条目和结果均冻结，`risk_filter_passed/formal_gate_ready/formal_usable/applied_to_d3/applied_to_d1`全部固定为`false`。
- 独立审查发现的畸形 E1 `reasons` 逃逸问题已通过新增红绿测试修复；非E1载荷按单条失败隔离语义保留并在设计中澄清；E2和E3现同时校验E1投影、D9审计和D8证据包三个固定合同ID，任一伪造均拒绝。新增E3只读映射进入E2的集成测试，运行时候选数量、风险门禁和 `risk_evidence` 缺口保持不变。
- E3专项13项、E1-E3联合46项、龙头全量292项通过；完整后端回归在导入 `database` 后立即把 `DB_PATH` 重定向到 `/private/tmp`、初始化临时基础表后874项通过。相关Python语法、敏感依赖搜索和差异卫生检查通过。
- 2026-07-30 14:52只读核对4000仍为PID 23280、8001仍为PID 38620，首页和 `/docs` 均返回200；没有停止、重启或重载服务。
- 本阶段没有读取或写入生产SQLite，没有连接外部网络、仓储、调度、API、前端、提醒或AI，没有新增迁移、依赖、环境变量或运行开关，也没有执行Git暂存、提交、推送或部署。阶段5观察继续并行。

## 阶段6L-F1完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-07-30-stage6l-f1-research-readiness-audit-design.md` 和 `docs/superpowers/plans/2026-07-30-stage6l-f1-research-readiness-audit.md`。
- 新增纯计算 `backend/radar/leader_research_readiness_audit.py`。输入使用横截面、历史、流动性、主营催化、可交易性五类强类型结果和E3单证券风险批次条目，不解析运行时松散JSON，不重算上游特征。
- 七项固定审计顺序为行业强度、证券可交易性、市场领先性、流动性、历史连续性、主营催化和风险投影；辅助量比不计入必需项。真实0研究分继续算作证据可用，缺失、过期、来源失败和来源未验证保持原语义。
- 审计状态只表示证据完整度：全部研究证据可解释为`ready`，存在未就绪项为`partial`，输入合同、身份、时点或固定子合同不可信为`blocked`。主营被证伪、可交易性明确排除等可信负面结果仍属于证据完整，但进入研究阻断，不冒充候选通过。
- 正式 `first_veto_reason` 与候选 `first_research_blocker_reason` 分开。F1没有正式行业门槛输入，因此当前合法结果的正式首次否决固定为`leader_formal_industry_gate_unavailable`；候选研究首次阻断才按七项证据顺序选择。正式评分、门禁、可用性和状态迁移标志全部固定为`false`。
- 风险条目严格校验证券、批次时点、E1/D9/D8三个合同ID、E3就绪语义和全部固定关闭标志；伪造身份、时点、合同、嵌套枚举、就绪原因或正式标志会使审计整体`blocked`，不输出部分可信结论。
- 严格TDD先确认模块缺失。F1专项13项、直接相关联合81项、全部龙头305项通过；完整后端回归在导入`database`前拦截默认生产SQLite路径并重定向到`/private/tmp`临时库后887项通过。相关Python语法、敏感依赖搜索和差异卫生检查通过。
- 2026-07-30 15:42只读核对4000仍为PID 23280、8001仍为PID 38620，首页和`/docs`均返回200；LaunchAgent中阶段5开关仍为`true`，阶段6开关仍不存在。
- 本阶段没有读取或写入生产SQLite，没有连接外部网络、仓储、调度、API、前端、提醒或AI，没有新增迁移、依赖、环境变量或运行开关，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交、推送或部署。阶段5观察继续并行。

## 阶段6L-F2完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-07-30-stage6l-f2-research-readiness-audit-batch-design.md` 和 `docs/superpowers/plans/2026-07-30-stage6l-f2-research-readiness-audit-batch.md`。
- 新增纯计算 `backend/radar/leader_research_readiness_audit_batch.py`。输入是统一带时区 `as_of` 和有序证券条目；逐股调用F1，不从证据映射交集推导证券名单，不重算任何上游特征。
- 批次结构、无时区和重复证券整体拒绝；单股载荷、身份、时点、F1阻断、未预期异常或伪造结果只阻断当前条目。输入顺序保持不变，等价时区按同一真实时刻接受，异常正文不进入结果。
- 批次状态区分 `ready/partial/blocked/missing`。F1 `ready`和`partial`进入不可变 `audits_by_symbol`；F1 `blocked`条目与稳定原因保留，但因合同或身份不可信不进入可消费映射。批次、条目和结果的正式评分、门禁、可用性和状态迁移标志全部固定为`false`。
- 独立审查发现嵌套审计状态组合和正式原因码可伪造；已通过新增红绿测试修复。F2现在重新校验状态与证据可用性、满足与否决的一致性，并从七项审计重新推导正式/研究阻断顺序；白名单外正式原因统一拒绝和脱敏。
- 严格TDD先确认F2模块缺失。F2专项13项、F1-F2联合26项、全部龙头318项通过；完整后端回归在导入`database`前精确拦截默认生产SQLite路径并重定向到`/private/tmp`临时库后900项通过。相关Python语法、敏感依赖和差异卫生检查通过。
- 2026-07-30 16:05只读核对4000仍为PID 23280、8001仍为PID 38620，首页和`/docs`均返回200；LaunchAgent中阶段5开关仍为`true`，阶段6开关仍不存在。
- 本阶段没有读取或写入生产SQLite，没有连接外部网络、仓储、调度、API、前端、提醒或AI，没有新增迁移、依赖、环境变量或运行开关，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交、推送或部署。阶段5观察继续并行。

## 阶段6L-F3完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-07-30-stage6l-f3-research-readiness-runtime-design.md` 和 `docs/superpowers/plans/2026-07-30-stage6l-f3-research-readiness-runtime.md`。
- `backend/radar/leader_runtime_inputs.py` 新增可选 `research_readiness_audits_by_symbol`。运行时只消费调用方显式提供的F2可信审计映射，不在候选循环中重算F1/F2，不从映射反推或改变候选名单。
- 运行时证据合同从 `radar-leader-runtime-input-v2` 升为 `v3`，新增 `researchFeatures.researchReadinessAudit`。结果区分 `ready/partial/missing/source_unverified`，只输出合同、候选、完整度和首次阻断的压缩摘要，不输出七项完整审计、来源或原始研究对象。
- 每只候选独立校验对象类型、证券、等价UTC时点、F1合同、四个固定关闭标志和F2深层不变量。F1 `blocked`、错误映射容器、身份/时点/合同/嵌套语义或任意原因伪造稳定降级且不透传正文；错误映射容器也不使运行时整轮崩溃。
- `backend/radar/leader_research_readiness_audit_batch.py`新增公共只读守卫，F3复用F2完整校验，不导入私有函数或复制校验逻辑。导入链为运行时到F2再到F1，没有循环依赖。
- 严格TDD先确认运行时不接受F3参数，并确认错误映射容器会触发异常；修复后运行时专项32项、F1-F3联合58项、全部龙头324项通过。完整后端回归在导入`database`前精确拦截默认生产SQLite路径并重定向到`/private/tmp`临时库后906项通过；Python语法、敏感依赖和差异卫生检查通过。
- 2026-07-30 16:47只读核对4000仍为PID 23280、8001仍为PID 38620，首页和`/docs`均返回200；LaunchAgent中阶段5开关仍为`true`，阶段6开关仍不存在。
- 本阶段没有读取或写入生产SQLite，没有连接外部网络、仓储、调度、API、前端、提醒或AI，没有新增迁移、依赖、环境变量或运行开关，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交、推送或部署。阶段5观察继续并行。

## 阶段6L-F4完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-08-01-stage6l-f4-research-readiness-runtime-batch-design.md` 和 `docs/superpowers/plans/2026-08-01-stage6l-f4-research-readiness-runtime-batch.md`。
- `backend/radar/leader_runtime_inputs.py`在原候选循环内同时保留有序冻结强类型组件项，显式绑定证券、统一时点、行业、行业发布ID、组件合同和集合ID；历史/流动性指标与引用映射递归转为只读视图，不从运行时JSON恢复强类型对象，也不重复计算五类研究特征。
- 新增纯计算 `backend/radar/leader_research_readiness_runtime_batch.py`。F4严格消费状态为ready的Assembly和完整E3 BatchResult，要求同一真实时点及完全一致的候选证券集合；E3结构合法的非ready条目保留缺失/过期/失败语义，条目直接缺席、重复、额外证券或批次合同错误整批阻断，不现场补造缺失风险对象。
- F4按运行时候选顺序组装F2；单股组件证券/时点/行业/集合ID错配或E3投影深层错配只阻断对应F2条目。F2 blocked项不进入可信映射，完整诊断保留在F4/F2批次；F3只回挂ready/partial审计并继续把不可信项显示为missing。
- 新增不可变 `attach_leader_research_readiness_audits()`，只复制并替换 `researchFeatures.researchReadinessAudit`，不修改原Assembly、候选数量、顺序、维度、来源、门禁、失效条件或风险投影。测试在回挂期间把五类构造器全部替换为抛错函数，调用次数仍为0。
- 严格TDD先确认F4模块缺失。F4专项8项、F1-F4联合66项、全部龙头332项通过；完整后端回归在导入`database`前精确拦截默认生产SQLite路径并重定向到`/private/tmp`临时库后914项通过。相关Python语法和差异卫生检查通过。
- 2026-08-01 15:04只读核对4000仍为PID 23280、8001仍为PID 38620，首页和`/docs`均返回200；LaunchAgent中阶段5开关仍为`true`，阶段6开关仍不存在。
- 本阶段没有读取或写入生产SQLite，没有接入生产运行时、仓储、调度、API、前端、提醒或AI，没有新增迁移、依赖、环境变量或运行开关，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交、推送或部署。阶段5观察继续并行。

## 阶段6L-F5完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-08-01-stage6l-f5-research-input-provider-batch-design.md` 和 `docs/superpowers/plans/2026-08-01-stage6l-f5-research-input-provider-batch.md`。
- Assembly及冻结研究组件新增 `radar_run_id`、行情批次ID和行情来源合同身份，组件合同升级为v2，集合ID同时绑定run、批次、来源、证券、时点、行业和行业发布ID，阻止跨轮拼接。
- 新增纯内存 `backend/radar/leader_research_input_provider_batch.py`。Assembly候选是唯一权威集合；每个候选必须有显式条目，`None`表示可信缺失，强类型输入必须匹配证券、等价UTC时点、行业/发布ID及行情来源。单股错配只阻断该股，候选缺席/重复/额外证券或E3批次错误整批阻断。
- 结果按Assembly顺序输出冻结条目和三类只读映射；完整E3非ready条目保留缺失语义，原始历史序列、披露/人工审核内容和风险投影不进入repr或压缩证据，所有正式评分、门禁、可用性和状态迁移标志保持false。
- 新增不可变 `attach_leader_risk_candidate_projections()`；F4先将E3投影挂到同一个Assembly，再组装F2并回挂F3，不再要求为风险证据第二次构建Assembly。测试把五类研究特征构造器全部替换为抛错函数，调用次数仍为0，原Assembly保持不变。
- F4/F5专项15项、F1-F5联合86项、全部龙头339项通过；完整后端第一次隔离命令在导入`database`后才替换路径，导入因此短暂打开默认生产SQLite并执行既有幂等初始化，虽未让测试业务使用生产库，但该次证据作废且不能表述为完全未触碰生产库。随后改为在导入前精确拦截默认路径并重定向到临时库，921项通过；相关Python语法与 `git diff --check` 通过。
- 本阶段没有接入 `backend/radar/runtime.py`、仓储、调度、API、页面、提醒或AI，没有新增迁移、依赖、环境变量或运行开关，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交、推送或部署。阶段5观察继续并行。

## 阶段6L-F6完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-08-01-stage6l-f6-single-pass-orchestration-design.md` 和 `docs/superpowers/plans/2026-08-01-stage6l-f6-single-pass-orchestration.md`。
- 新增纯内存 `backend/radar/leader_runtime_candidate_plan.py`，按现有A股、行业映射、行情资格和每行业前5名口径冻结run、行情批次、时点、证券顺序、行业、发布ID、排名及行情来源合同；稳定SHA-256生成 `candidate_set_id`，计划阶段不调用五类研究特征构造器。
- F5提供器保留原Assembly入口并新增Plan入口；结果新增 `candidatePlanId`，条目即使倒序输入也按计划顺序输出。显式缺失、单股错配、候选集合错误和E3批次错误继续严格分开。
- 新增最外层 `backend/radar/leader_research_single_pass_orchestration.py`。它在构建前校验计划、原始行情批次和Plan提供器；阻断输入调用五类构造器次数为0，合法输入只调用一次既有Assembly构建，并逐项核对结果与计划后直接进入F4。
- Fixture完整输入仍真实返回 `partial`：首个研究阻断为 `leader_liquidity_evidence_unavailable`，同时保留历史恢复和正式风险门禁缺口；没有为测试伪造ready或打开正式标志。
- F6专项6项、F1-F6联合92项、全部龙头345项通过；完整后端在导入`database`前拦截默认生产路径并重定向到临时SQLite后927项通过，Python语法与差异卫生检查通过。
- 2026-08-01 18:06只读核对4000仍为PID 23280、8001仍为PID 38620，首页和`/docs`均返回200；阶段5开关仍为`true`，阶段6开关仍不存在。
- 本阶段没有修改 `backend/radar/runtime.py`、F1-F4、E3或五类特征公式，没有接入仓储、调度、API、页面、提醒或AI，没有读取或写入生产SQLite，没有新增迁移、依赖、环境变量或运行开关，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交、推送或部署。阶段5观察继续并行。

## 阶段6本地运行时接入包完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-08-01-stage6-runtime-integration-package-design.md` 和 `docs/superpowers/plans/2026-08-01-stage6-runtime-integration-package.md`。
- 新增 `backend/radar/leader_research_runtime_provider.py`。默认提供器覆盖冻结候选全集，但只返回历史、主营催化、可交易性和风险证据的显式缺失；不联网、不缓存、不调用特征函数，也不补造数据。
- F6单次编排新增全缺失提前返回和稳定 `gate_reasons`；显式缺失时Assembly、五类特征构造器和F4均为零调用。非全缺失路径仍只构建一次Assembly，并把真实研究阻断原因带到健康状态。
- `backend/radar/runtime.py` 已在现有市场影子任务回调中接入候选计划、可注入批量提供器和F6。阶段6依赖改为开关分支内延迟导入；计划未就绪、合同阻断或全缺失时记录 `degraded`，且在创建 `LeaderRepository` 前结束，因此未应用迁移5也不会访问阶段6表。
- 已验证输入路径只复用本轮同一行情批次和候选集合，随后才读取历史状态、执行单次Assembly/F4并调用既有 `LeaderShadowRunner`；Fixture结果仍为真实 `partial`，临时迁移5数据库只写一份 analysis-only 快照，`formal_usable` 合计仍为0。
- 提供器异常继续由现有市场任务异常屏障隔离：阶段6记录失败，但既有市场快照、市场任务完成状态和市场健康不受影响。开关关闭时提供器零调用，没有新增独立调度任务。
- 默认提供器及F6/运行时直接相关47项、运行时30项、龙头全量350项通过；完整后端回归在导入 `database` 前精确重定向默认路径到临时SQLite后934项通过。相关Python语法和 `git diff --check` 通过。
- 2026-08-01 18:38只读现场核对：4000仍为PID 23280、8001仍为PID 38620，前端首页和后端 `/docs` 均返回200；LaunchAgent `com.linjian.stock-monitor.fastapi` 中阶段5开关仍为`true`，阶段6开关不存在，因此本地运行服务没有加载本次未提交代码。
- 本阶段没有读取或写入生产SQLite，没有连接外部网络或付费AI，没有新增迁移、依赖、环境变量、API或页面，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交、推送或部署。阶段5的20个交易日观察继续并行，不能由本地开发替代。

## 2026-07-27开盘现场与旧单股历史资金源

- 阶段5影子任务锁在09:33:09首次创建，腾讯代表ETF `159915`、`510300` 在09:49:45返回当日盘中源时间；当天没有阶段5调度失败日志。第1日只代表观察已经开始，不代表日结或20日门槛完成。
- 旧单股东方财富历史资金接口由不稳定的HTTP改为HTTPS，并仅对临时连接异常或无效JSON重试一次；原有K线保留和资金缺失语义不变。真实HTTPS探针返回200和120条记录，相关模块44项测试通过。
- 用户明确授权后只受控重载FastAPI LaunchAgent，8001由PID 90418更新为84334，`/docs`返回200；前端4000没有重载。
- 2026-07-27收盘前后日志仍间歇出现东方财富历史资金源 `RemoteDisconnected`，说明HTTPS加一次有界重试只降低失败概率，没有彻底解决断连；后续必须先设计备用源、熔断和缓存，再经授权实施。
- 收盘现场仅检查服务进程和日志，没有调用可能读取生产业务库的雷达API。服务持续运行，未发现当日阶段5专属失败行；但日志没有足够的阶段5完成/候选快照证据，市场与行业门禁临近收盘多次因来源时间过期或特征不完整拒绝，因此第1日暂不计为合格观察日。
- 修复测试初次导入 `main` 时曾触发生产SQLite的既有幂等初始化副作用，发现后已向用户披露并停止普通导入跑法；后续所有会导入 `main` 的测试必须先把生产数据库路径重定向到临时SQLite。

## 2026-07-28健康语义与数据源加固

- 后台任务新增 `degraded` 健康语义：来源门禁拒绝和可用性降级不再伪装成任务失败；真实异常仍保持 `failed`。公开健康探针只在Vercel、隧道或FastAPI不可达时返回503，后台数据降级返回HTTP 200并保留告警状态。
- 健康原因先做稳定分类和脱敏，同任务同原因按日去重；提醒中心仍保留历史系统事件，新语义只影响后续健康判定，不删除或改写历史提醒。
- 旧单股东方财富历史资金请求补齐浏览器兼容请求头并保留HTTPS和一次有界重试；腾讯行情与市场指数改为HTTPS，对传输失败和批次覆盖不完整各有一次有界重试，最终仍不完整时明确降级，不使用旧数据补齐。
- 不连接数据库的真实来源探针取得腾讯行情3/3、市场指数4/4、覆盖率1且问题为空；来源相关专项76项、影子运行相关45项通过。
- 用户明确授权后受控重载后端，8001由PID 91404更新为95079；`/docs`和`/openapi.json`均返回200。14:53、14:54两轮量价风险采样未再出现`RemoteDisconnected`，但样本仍短，只能继续观察，不能宣称长期彻底解决。
- 前端4000保持PID 23280且未重载；没有手动读取或写入生产SQLite，没有执行Git暂存、提交、推送或部署。

## 阶段6L-C4A完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-08-01-stage6l-c4a-provider-delivery-admission-design.md` 和 `docs/superpowers/plans/2026-08-01-stage6l-c4a-provider-delivery-admission.md`。
- 新增纯内存 `backend/radar/leader_tradability_provider_delivery.py`。系统侧必须单独注入 `ProviderAdmissionPolicy`，固定可信供应商、交付合同、上游来源、授权文档、域名、使用范围和有效期；供应商记录自报“已授权”不能自行获得准入。
- 静态与动态来源分别使用 `ProviderSourceObservation` 绑定证券、交易日、来源合同、文档身份、来源时间和SHA-256内容摘要。动态状态最长90秒，静态参考不套用90秒，但两者均拒绝未来时间和跨证券、跨交易日拼接。
- 批次严格核对目标证券全集、重复/缺失/额外证券、统一交易日、供应商、交付合同、交付批次、授权和价格模式。带用户名、密码、查询参数或片段的来源URL被拒绝且不会出现在 `repr`；只有通过准入的候选批次才生成绑定全部业务与来源内容的批次ID。
- C4A结果只可能表示“允许进入真实POC的候选”，`formal_score_ready/formal_gate_ready/formal_usable/state_transition_allowed`全部固定为`false`，没有修改C3的 `official_primary` 唯一准入语义。
- 严格TDD先确认模块不存在，再补审查红测并修复可信策略、来源独立绑定、畸形类型稳定阻断、敏感URL防泄露和全内容哈希。C4A专项12项、全部龙头364项、完整后端948项通过；完整回归在导入 `database` 前把默认生产SQLite路径精确重定向到临时库。
- 本阶段没有接入真实供应商账号、网络、SDK、依赖、环境变量、运行时、仓储、调度、API或页面，没有读取或写入生产SQLite，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交、推送或部署。阶段5的20个交易日观察继续并行。
- C4B-1和C4B-2入口已完成，RQData保留为可选备用对照；当前主路径转为C4C免费公开复合源。在任何真实来源的响应形状、字段覆盖、动态时效、许可证和来源血缘通过前，不把来源标为READY，也不接生产运行时。

## 阶段6L-C4B-1完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-08-01-stage6l-c4b-rqdata-field-poc-design.md` 和 `docs/superpowers/plans/2026-08-01-stage6l-c4b-rqdata-field-poc.md`。
- 新增隔离模块 `backend/radar/sources/leader_tradability_rqdata_poc.py`：只使用Python标准库访问固定 `https://rqdata.ricequant.com/api`，Token仅由调用方显式提供，不读取配置、不持久化响应正文，也不连接数据库、业务API或运行时。
- 请求严格有界：`instruments`、`get_price`和可选`current_snapshot`按批各最多一次；`is_suspended(count=1)`与`is_st_stock(count=1)`按每只证券各最多一次；首个空响应、缺字段、身份或合同错误立即停止，不重试、不调用付费AI。
- `get_price`兼容官方HTTP文档分别出现的`date`与`datetime`日期列，但只允许二者之一；动态交易日、窗口和90秒快照年龄以真实`fetched_at`为准，观察时刻最多允许5秒偏差。
- 独立审查发现Fixture伪装真实完成、异常链泄露、抓取时间未参与时效、失败后继续扣调用次数、全空仍标完成及枚举未校验等问题；均已增加红绿测试修复。Fixture现在固定`partial/not_run`，只有未注入测试opener的固定端点HTTP传输才可能得到`field_candidate/completed`，全空真实响应固定`partial/no_data`。
- C4B专项22项、C4B与C4A联合34项、全部龙头386项通过；完整后端回归在导入`database`前精确拦截默认生产SQLite路径并重定向到临时库后970项通过，Python语法检查通过。
- 2026-08-01 21:40只读核对：4000仍由PID 23280监听且首页HTTP 200，8001仍由PID 38620监听且`/docs` HTTP 200；本次未重启或重载服务。
- 本阶段没有申请或登录RQData账号，没有安装SDK、调用真实RQData或付费AI，没有读取或写入生产SQLite，没有修改依赖、环境变量、LaunchAgent、运行时、仓储、调度、API或页面，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交、推送或部署。阶段5的20个交易日观察继续并行。

## 阶段6L-C4B-2准备记录

- 新增正式单次入口 `backend/run_rqdata_tradability_poc.py` 和专项测试 `backend/tests/test_radar_leader_tradability_rqdata_live_poc.py`；没有接生产运行时、API、页面、调度或数据库。
- 入口必须显式传入`--confirm-live-poc`和1至8只沪深A股代码。随后依次核对带时区当前时间、上交所官方交易日历、A股连续交易窗口和C4B查询合同；失败时在读取Token前退出。
- Token只通过终端`getpass`隐藏输入并驻留当前进程内存，不接受命令行Token、环境变量、项目配置或输出文件；结果只打印脱敏证据，不打印逐股记录、响应正文、账号、Token、请求头或异常正文。
- 2026-08-01周六现场干跑返回退出码2、`not_run / rqdata_live_calendar_closed`，没有提示Token、没有调用RQData；该结果不是字段POC成功或失败，只证明休市门禁有效。
- C4B-2入口专项7项、C4A/C4B联合41项、全部龙头393项通过；完整后端回归在导入`database`前重定向到临时SQLite后977项通过，Python语法检查通过。
- 当前`rqdatac_installed=false`、当前进程`rqdata_token_present=false`；用户已提交试用申请，官方HTTP文档当前说明Token当天有效。Token仍不得写入项目配置、环境变量或文件。
- RQData真实小样本改为可选备用对照：只有试用获批且用户选择对照时，才在有效A股连续交易窗口通过隐藏输入执行一轮。未执行不会阻塞C4C免费方案；真实响应、字段、许可证和血缘未通过前继续保持非READY，不进入C4A转换或生产运行时。

## 阶段6L-C4C-1完成记录

- 设计与实施规格分别保存在 `docs/superpowers/specs/2026-08-02-stage6l-c4c-public-composite-tradability-poc-design.md` 和 `docs/superpowers/plans/2026-08-02-stage6l-c4c-public-composite-tradability-poc.md`。
- 新增纯内存模块 `backend/radar/sources/leader_tradability_public_poc.py`，合同版本为 `radar-leader-tradability-public-composite-poc-v1`。固定来源注册表按合同能力、字段、来源名称、官方域名和交易所绑定证券主档、交易日历、交易所状态、AKShare聚合备证和腾讯行情批次；未知合同、越权字段、敏感URL或来源冒充均阻断。
- 上市交易日计数不再接受调用方整数，而由交易所主档上市日期和每交易所最近6个官方交易日推导。静态证据必须具备覆盖目标交易日的生效区间；AKShare必须保留可信上游名称、文档和上游时间，旧上游动态状态只能标为`stale/partial`。
- 官方证据是生命周期、交易状态和特殊交易日的唯一主值，聚合源只作独立交叉，不得反向补成主值或制造无涨跌幅限制。腾讯普通行情存在、价格、成交量或正常行不得推断`trading`；只有显式停牌、退市或未上市状态可作动态反证。腾讯逐行`source`必须为`tencent_finance`，批次号与全部规范化行情内容的SHA-256必须匹配。
- 每个解析字段保存逐来源合同、源时间、抓取时间、摘要、上游信息、原始字段值和`current/effective/stale/derived`状态；缺失、`UNKNOWN`、过期、规则无法计算、来源冲突和真实0严格分开。所有交易日比较先归一到`Asia/Shanghai`。
- Fixture解析状态与真实POC状态已经拆分；纯内存输入即使字段解析完整，顶层`status`和`real_poc_status`仍固定为`not_run`。报告证据不包含逐证券记录、原始响应或带查询参数的URL，`formal_score_ready/formal_gate_ready/formal_usable/state_transition_allowed`全部固定为`false`。
- 严格TDD完成43项专项测试；C4相关回归98项、全部龙头436项、隔离生产SQLite的完整后端1020项均通过，Python语法编译通过。独立代理经过五轮对抗审查和修复后，最终结论为无P1/P2 findings。
- 本阶段没有执行真实公开源或RQData网络调用，没有读取或写入生产SQLite，没有修改依赖、环境变量、LaunchAgent、运行时、仓储、调度、API或页面，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交、推送或部署。阶段5的20个交易日观察继续并行。
- 下一步是C4C-2真实公开源小样本POC：只能在有效A股连续交易时段只读执行，最多8只证券；通过字段、时点、覆盖、冲突和来源血缘验收后，才能讨论适配生产研究提供器。RQData只保留同窗可选对照。

## 阶段6L-C4C-2A完成记录

- 新增隔离真实POC组装器 `backend/radar/sources/leader_tradability_public_live_poc.py`。它只读复用沪深交易所证券主档、上交所A股官方休市页、腾讯同批行情，以及AKShare的东方财富风险警示板和停复牌聚合备证；不连接生产数据库，不导入RQData，不接运行时、仓储、调度、API或页面。
- 新增单次入口 `backend/run_public_tradability_poc.py`。入口要求显式`--confirm-live-poc`、1至8只无重复沪深A股代码、官方交易日和A股连续交易窗口；通过后才使用固定包装的独立子进程采集。35秒是包含最多1秒强制终止/回收在内的总预算；子进程FD 1/2在采集前重定向到`/dev/null`，结果只通过受净化队列返回。旧`--worker`入口无条件拒绝，伪造同名参数或环境变量也不能触发采集。
- 证券身份保存官方端点、抓取时间、捕获文档ID和规范化行SHA-256，只有上游明确提供时才保存真实来源时间；缺少来源时间时保持`null/degraded`，不再用抓取时间冒充。沪深最近6个交易日由上交所官方A股休市页推导；日历来源时间优先接受合法`Last-Modified`，缺失时只接受页面内该年度官方休市安排通知的明确发布日期，二者都不存在才保持缺失。上交所日历只作为沪深共同交易窗口证据，不冒充深交所证券状态主证据。
- 腾讯继续只承认同批显式`S/D/U`和上下限字段，普通行情、价格或成交量不生成`trading`。AKShare明确标为聚合层并保留东方财富上游；当前默认调用拿不到可信来源时间，因此不生成聚合观察值并明确`degraded`。只有同时提供真实包装层和上游来源时间时，ST/停牌候选才可进入纯内存合同；两者不互相拼成同一来源事实，当日复牌、当日截止但无复牌时间或区间不完整时均不生成`suspended`。
- 修正C4C-1时效职责：交易所/聚合盘中动态状态仍受90秒限制，带明确生效区间的静态停复牌证据不再错误套用90秒。真实采集报告使用采集完成时点作为`as_of`，避免采集耗时被误判为未来数据。
- 2026-08-02周日闭市干跑返回退出码2、`not_run / public_live_calendar_closed`，没有启动worker，也没有执行真实公开源网络POC。该结果只证明闭市门禁有效，不代表免费源字段成功或失败。
- 2026-08-08加固测试覆盖伪造worker参数/环境仍拒绝、子进程FD输出隔离、35秒总预算、超时进程无残留、跨午休/收盘完成结果作废、来源时间不冒充、同日停复牌未知语义，以及静态证据不误用90秒动态时效。当前整条可交易性链路135项通过，独立只读复核最终无P1/P2；真实网络POC仍未执行，因此不能据此宣称免费源字段已成功。
- 本阶段没有读取或写入生产SQLite，没有执行真实公开源或RQData网络POC，没有修改依赖、环境变量、LaunchAgent、生产运行时、仓储、调度、API或页面，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交、推送或部署。阶段5的20个交易日观察继续并行。
- 下一步是C4C-2B：在下一有效A股连续交易时段执行最多8只真实样本。真实结果大概率仍可能是`partial`，因为公开免费源目前没有可靠的“显式正常交易”官方字段；不得为追求通过而从未命中、简称或普通行情反推正常。

## 2026-08-10阶段6L-C4C-2B真实验收记录

- 09:47发现一次性任务`6-c4c-2b`仍为`ACTIVE`但未产生执行记录，改由当前任务人工接管；完成后删除该失效任务，避免重复触发。
- 首批8只样本在证券主档阶段以`public_live_security_master_incomplete`提前阻断，未进入字段采集；随后只用确认在市的`000725/600000/300081/688981`形成纠正批次。
- 纠正批次首次暴露上交所休市页不再返回`Last-Modified`。代码没有拿抓取时间补位，而是新增该年度官方休市安排通知发布日期的严格解析回退；缺少两种官方时间时仍失败关闭。可交易性相关88项、全部雷达888项、语法和差异检查通过。
- 静态证券身份改为严格校验“交易所官方合同 + 内容SHA-256 + 带时区抓取时间”；若上游确有业务时间仍校验未来时间和先后顺序，但不再强迫静态名录伪造发布时间。动态交易、ST、停牌和价格字段未放宽。
- 10:22纠正批次中4只样本已全部返回，证券主档由`degraded`修正为`completed`，整批从`blocked`推进为`partial`。东方财富风险警示板改为固定公开HTTPS接口的有界分页采集，保留逐行`f124`源时间；10:40只读采集包已实际生成1条`300152 -> star_st`聚合交叉证据，普通样本没有被反推为正常交易。
- 免费路径当前的真实上限为`partial`：东方财富停复牌接口有生效区间但没有可验证发布时间，交易所公开网页也不提供全市场盘中“正常交易”快照。因此不形成`field_candidate`、不执行C3/C1准入、不接运行时。本轮未读写生产SQLite，未重启服务，未使用付费API，未执行Git或部署。
- 本轮公开可交易性专项80项、临时SQLite隔离下全部雷达892项通过；相关语法编译和差异检查通过。
- 11:04只读现场核对：4000仍由PID 23280监听，8001仍由PID 38620监听，`/radar`与`/docs`均返回200；本轮没有停止、重启或重载服务。

## 2026-08-10阶段6 D2官方来源级预验收

- 当前阶段6生产开关关闭，系统没有可用的当轮冻结候选计划。本轮只复用C4C同组`000725/600000/300081/688981`作为来源级研究样本，不把它们冒充真实龙头候选。
- 巨潮官方代码查询已对4只样本全部精确解析发行人身份。过去365日七类有界首页查询修复后全部为可解析`partial`：减持/解禁/监管/诉讼四类为真实空结果，立案2条、业绩预告6条、审计报告8条，共16条待审元数据。关键词发现不等于风险覆盖证明，所有正式门保持关闭。
- 现场确认巨潮在合法零结果时返回`announcements=null`。解析器仅在`totalRecordNum=0`、`totalpages=0`、`hasMore=false`同时成立时归一化为空列表；其他`null`、非零或分页矛盾继续`source_unverified`。
- 风险来源/候选/生命周期/交付链65项、临时SQLite隔离下全部雷达893项通过。本轮未读写生产SQLite，未重启服务，未提交或推送Git。
- 13:05的对话心跳曾因未投递而删除。2026-08-10 15:25已另行创建同ID `6-d2`的独立本地项目自动任务，状态`ACTIVE`，计划于2026-08-11 09:35 CST执行一次阶段6真实D2验收。任务直接绑定唯一真实项目，使用临时SQLite、真实候选全集和最多910次无重试公开D2请求；生产库、服务重启、Git、部署、付费API和伪造人工复核全部禁止。心跳创建成功仍不当成执行成功，明日只以实际运行记录和脱敏结果为准。

## 2026-08-10阶段6 D2真实冻结候选验收

- 13:12再次由上交所官方日历确认当日为`full`且处于`trading`连续交易时段；全程只使用`/private/tmp`临时SQLite和临时锁，完成后已自动清理。
- 真实注册链返回7451项，证券主档与ETF名册均为`healthy`；中上协行业版本有5463条来源记录、5439条已映射记录和100只当前主档缺口，保持`degraded/shadowUsable=true/formalUsable=false`。
- 真实行业影子聚合门禁通过并保存83个行业；真实市场影子聚合门禁通过，股票池5539只、ETF池1912只。按既有运行时口径生成的冻结候选计划为`ready`：扫描7451项、精确行业映射5155项、候选385只，计划门禁原因为空。
- 现有巨潮候选范围合同`MAXIMUM_CANDIDATE_SCOPE_COUNT=30`，无法接收385只冻结候选全集。验收按`cninfo_query_candidate_scope_unverified`停止：未解析候选发行人、未发起七类D2查询、请求数0、人工审核版本0；没有任意裁剪30只、抽样、Fixture或手工证券补位。
- 所有正式评分、正式门禁、`formalUsable`和状态迁移继续为`false`。专项风险来源/候选/生命周期/交付链65项通过，相关9个Python文件AST语法检查通过；工作树原有改动全部保留。本轮未读取或写入生产SQLite，未修改环境变量或LaunchAgent，未停止或重启4000/8001，未执行Git暂存、提交、推送或部署。

## 2026-08-10阶段6 D2候选分片/合并合同

- 交付层已按冻结候选原始顺序自动分片，来源层“单次最多30只”硬限制保持不变。385只计划稳定分为13片（12×30+1×25），七类首轮最低全局预算为91次；未显式给预算时按分片数自动计算最低值，显式预算不足则在来源请求前阻断。
- 每个D2原始页新增`candidatePlanId/shardIndex/shardCount`绑定；候选发现和生命周期同时核对同一计划ID、分片顺序、全集覆盖、七类×全分片矩阵、逐片分页守恒和同类跨片公告ID唯一性。无作用域、混入其他计划页、缺片/重片/乱序、原始页数缺失或跨页漂移全部失败关闭，不合成伪造的“大批次”。
- 多分片首次来源失败现在内外层均稳定保留`source_failed`；不重试、不会因分片未收齐被改写为`source_unverified`。来源直接传入31只仍在联网前被拒绝。
- 四层官方来源/候选发现/生命周期/交付专项77项、风险全链200项、临时SQLite隔离下全部雷达905项通过；相关8个Python文件语法编译通过。本轮没有读取或写入生产SQLite，没有网络或付费API调用，没有停止/重启服务，没有Git或部署操作。
- 下一步是在新的有效候选计划上解析全部发行人作用域，再用本分片合同执行真实D2。当天14:55距收盘过近，未冒险重跑全市场候选链和385只网络采集；不伪造已产生真实D2证据或D8连续版本。

## 2026-08-11阶段6 D2真实长跑与修复

- 真实冻结计划稳定为385只、13片、7类，`candidatePlanId=radar-leader-runtime-candidate-plan-v1:6c473d1f4295a1bbc8e6baa8cb56f55796a4deb51d74e1ff4e87781873c08229`。一次完整运行共789次请求，其中4次 `cninfo_source_request_failed` 均在同页唯一重试后成功；形成785个有效页和22018份去重文档，未耗尽910次预算。
- 真实来源同时出现两种此前未覆盖但可严格验证的官方终页：`adjunctType=.PDF` 的PDF，以及 `adjunctType=null` 的 `finalpage/*.html`。上游解析按精确来源事实加入这两种变体，仍拒绝绝对地址、路径穿越、查询参数、片段、错误扩展名和类型不匹配。
- 完整运行最后被 `risk_official_candidate_discovery_batch_unverified` 阻断。根因已定位为上游接受官方HTML后，下游候选合并器仍只校验PDF并固定裁掉4个字符提取公告ID；下游现改用 `PurePosixPath` 同步验证 `.pdf/.html` 并按真实后缀取stem。该回归测试先失败后通过，相关专项65项和全部雷达911项通过。
- 修复后的真实复验于15:02开始时已超过A股连续竞价窗口，被 `calendar_or_continuous_session_gate_failed` 在候选和来源请求前以0请求正确停止。因此D2仍缺下一有效交易窗口的最终证据，D8继续 `not ready`；真实人工审核双版本仍为空，禁止复制或伪造版本补齐。
- 自动任务 `6-d2` 已更新为下一次09:35单次执行，合同包含网络失败唯一重试、HTML/PDF一致校验和首个具体合并失败条件诊断。本轮所有临时SQLite、锁、脚本和结果文件均已清理；未触碰生产SQLite、服务、Git、部署或付费AI。

## 2026-08-08旧单股历史资金源并发降载

- 现场日志确认东方财富历史资金源间歇断连被本地缓存并发穿透放大：3分钟缓存过期后，相邻轮询可同时进入上游，每个请求内部又执行最多2次有界尝试。
- `backend/main.py`已按股票和预期交易日合并并发刷新；同一轮只有一个请求访问上游，其余请求最多等待30秒并复用该轮结果，不无限重试、不回填旧资金值、不伪造成功。
- 真实资金历史成功时仍缓存3分钟；资金字段缺失或来源失败时缓存15分钟，期间继续保留真实K线和明确缺失语义。`fund_flow=0.0`按真实成功数据处理，不进入失败缓存。
- 新增3项回归测试并在临时SQLite隔离下完成先红后绿；`backend/tests/test_risk_engine.py`共47项通过，`git diff --check`通过。未读取或写入生产SQLite，未重载8001，因此当前运行进程尚未加载本次修复。
- 后续受控重载8001须单独获得授权；重载后只读观察自然轮询日志，确认同股同轮断连不再成批出现。阶段6L-C4C-2B仍须等待下一个有效A股连续交易时段执行，与本修复互不替代。

## 2026-08-08阶段6L-B1免费历史输入POC

- 新增纯内存 `backend/radar/sources/leader_history_public_poc.py`：严格校验21个官方交易日、点时行业成员、腾讯前复权日K和连续板块指数；行业基准按沪深范围内全部成员逐日等权收益复合。成员缺日、晚观察、错来源、非法价格或日期错位均不输出可接入历史输入，真实0保留。
- 新增单次入口 `backend/run_public_history_poc.py`：闭市也可运行，显式确认后只读调用上交所日历、中上协行业分类和腾讯历史日K；最多1只候选、80个行业成员、8路并发、每请求8秒、总预算120秒，子进程输出隔离并只返回脱敏摘要。
- 2026-08-08修正项目范围后复验真实样本600519：只保留上交所、深交所行业成员47只，范围外排除1只，预期48条序列，完整47条，覆盖率97.9167%，结果为`partial`；北交所920023不再进入抓取目标、成员数或覆盖率分母。
- 当前准确缺口只剩中上协版本在本系统中的首次观察晚于21日窗口首日，以及600238缺少至少一个官方交易日价格；没有停牌官方证据，因此未将缺日补成0。中上协仍报告`current_master_mapping_incomplete`和`source_records_excluded`；证券主档和官方日历完成，历史输入未就绪，四个正式标志与状态迁移继续全部为false。本结果不能替代C4C-2B，也没有接运行时、仓储、API或页面。
- 专项15项、隔离生产SQLite的全部龙头477项测试、Python语法和`git diff --check`通过。没有读取或写入生产SQLite，没有修改依赖、迁移、环境变量或调度，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交、推送或部署。

## 2026-08-08阶段6剩余合并开发检查点

- 新增合并计划 `docs/superpowers/plans/2026-08-08-stage6-remainder-consolidated-delivery.md`，以后按“运行时组合、真实来源、集中验收”三批连续推进，不再新增阶段6小编号。
- `backend/radar/leader_research_runtime_provider.py` 新增已验证组件组合入口：严格绑定同一候选计划、run、asOf和候选顺序，拒绝计划外证券；单个组件缺失只保留该组件缺失，风险批次缺失时生成覆盖候选全集的显式缺失批次。部分真实输入可进入既有F5/F6和临时迁移5仓储，但只写analysis-only快照，正式标志仍全为false。
- `backend/radar/leader_risk_candidate_projection_batch.py` 新增D8连续证据包装配入口：逐证券重算D9审计与E1投影，少于两个连续版本保持`missing`，单只失败不污染其他候选，非沪深证券整体拒绝。
- 新增 `backend/radar/sources/leader_risk_candidate_discovery.py`：把D2巨潮官方发现批次按同轮候选计划整理为人工审核队列，保持候选全集顺序；同一公告跨关键词合并但保留类别、查询词和来源状态，发行人身份冲突只阻断对应证券，候选外及北交所文档排除并计数。真实空结果、查询失败、脏页和七类查询缺口均显式保留，完整覆盖、开放事件结转、更正关系、正式门禁和状态迁移固定关闭；该队列不生成D1事件、D8证据包或运行时候选。
- 主营催化证据新增来源类型与官方域名绑定：交易所披露只接受上交所/深交所域名，指定披露平台只接受巨潮域名；公司官网暂不直接准入，等待与证券身份绑定的版本化域名名册，调用方不能自行声明域名绕过。北交所证券继续排除，风险官方来源中的北交所域名和证券残留也已清理。
- `backend/radar/leader_business_catalyst_official_adapter.py` 新增官方结构化材料入口：只接受上交所、深交所、巨潮固定来源合同，平台自动派生来源类型与名称，严格拒绝合同/域名错配、查询凭据、畸形时间和非沪深证券；入口没有关系、直接关联或AI审核字段，主营证明的关联列表和审核列表固定为空，因此只会产出待真实人工审核的研究输入。来源合同和材料版本继续保留到压缩研究引用。
- 官方材料入口新增候选计划级批处理：必须显式覆盖候选全集并按计划顺序输出，重复、缺失或计划外证券整体阻断；单只来源失败保留为逐证券状态，不丢候选，只有`ready`条目进入只读输入映射。
- 新增 `backend/radar/leader_business_catalyst_manual_review.py`：单只和候选全集批处理均只接受上述官方材料合同和`human`人工复核工件，严格绑定证券、行业版本、催化、依据证据和有效期，生成可重放的确定性复核ID；缺审核、材料失败和单只审核失败均逐证券保留，调用方不能传审核方式，AI没有审核入口。直接相关、高相关、证伪和未确认语义保持分离，只有审核就绪条目进入只读运行时输入映射，所有正式评分、门禁和状态迁移继续关闭。
- 免费公开可交易性链新增严格运行时适配：只有整批`field_candidate`、报告可由原始查询/行情/官方观察/聚合观察完全重放，并再次通过既有C3与C1预检时，才输出逐证券 `LeaderTradabilityFeatureInput`；其他状态不接入。公开POC和真实入口均在采集前拒绝北交所代码。
- 独立审查发现并已修复7类真实性边界：声明失败的历史/主营/可交易性输入不能标成提供器`ready`，注入Fixture的公开采集不能声称真实POC完成，主营URL拒绝查询凭据，公司域名不能由调用方自报，历史与风险所有入口统一拒绝非沪深证券，证券主档必须保留官方批次、行来源和原始代码字段身份。新增测试先红后绿。
- 隔离默认生产SQLite后的全部龙头与运行时回归559项通过；相关Python语法、前端TypeScript、ESLint和`git diff --check`通过。因4000正在使用同一`.next`且本轮未改前端，未运行会改写共享构建目录的Next.js生产build；最近一次生产build仍是2026-07-30发布检查点。
- 只读现场核对4000仍为PID 23280、8001仍为PID 38620；应用内浏览器真实打开`/radar`和`/docs`，两页控制台均无错误。雷达当前如实显示“阶段6未启用”；市场模块显示最近一次读取/来源失败、行业模块可用，这是当前运行数据状态，不是本轮源码测试失败。
- 本轮没有读取或写入生产SQLite，没有真实公开源/付费来源调用，没有修改依赖、迁移、环境变量或LaunchAgent，没有停止、重启或重载服务，也没有执行Git暂存、提交、推送或部署。

## 2026-08-09阶段6四来源统一准入收口

- 新增 `backend/radar/leader_research_source_admission.py`，把历史、主营催化、可交易性和风险四条来源在同一候选计划下统一准入。入口只接受冻结的严格适配结果，不接受松散字典，不联网、不落盘、不补0或伪造READY。
- 历史逐候选核对21日POC、完整点时行业成员和同轮身份后内部重放；`partial/failed`不接入。主营批次严格绑定候选计划、人工复核合同和逐只输入；可交易性必须从原始查询、行情、官方观察和聚合观察重新构建且与报告一致；风险复用F4完整批次校验。候选顺序错位、契约改写、时间、行业发布、风险身份或来源原因漂移均在F5执行前阻断。
- 全量就绪输入一次生成现有F5提供器批次并进入F6单轮编排；部分来源失败只影响对应证券/组件，并精确保留`source_failed/stale/source_unverified`，全部缺失生成显式MISSING批次。F6强制接收有效冻结来源上下文，逐候选比对当前行情内容，即使研究提供器全缺失也不能绕过；畸形嵌套合同统一返回BLOCKED。当前仍因既有研究门槛返回PARTIAL，所有正式评分、正式门禁、`formal_usable`和状态迁移保持false。
- 统一准入专项14项、F6专项9项、直接相关历史/主营/可交易性/风险/F5/F6整链122项通过；精确重定向默认SQLite到临时目录后，全部 `test_radar*.py` 844项通过。Python语法编译和 `git diff --check` 通过；最终独立只读复审无P0/P1/P2。
- 本轮没有读取或写入生产SQLite，没有网络或付费来源调用，没有改依赖、迁移、环境变量、LaunchAgent、API或前端，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交、推送或部署。

## 2026-08-09阶段6风险生命周期批次收口

- 新增 `backend/radar/leader_risk_lifecycle_batch.py`，将候选全集D2官方发现页、人工复核提交和事件关系版本作为原始输入；模块内部重新执行D5人工工件、D6重放、D7关系审核和D8证据包，再复用既有D9/E1/E3生成风险投影批次并进入四来源统一准入，不信任调用方注入的中间结果。
- D2查询只接受规划书冻结的七类关键词；分类、页码、`has_more`、页大小、总页数、总记录数、每页记录数和跨页文档ID必须全部守恒。任何原始页`source_failed/source_unverified`都会保留准确状态；关键词发现仍不等于完整覆盖，`coverage_complete`和所有正式标志固定为false。
- 开放事件按证券、发行人、类别、子类型、案件/报告期、来源类型和生效身份连续结转；同ID偷换身份、静默改为完成/撤回或修改有效期均失败关闭。更正关系必须按时间向前，替代事件在更正审核时点已由官方产生且在后续人工版本落成；倒序、未来事实、虚构替代和漏审更正事件全部拒绝，合法三版本更正链通过。
- 至少两个D8版本才可生成风险研究投影；最新人工提交和关系审核必须满足`submission.reviewed_at <= relation_review.reviewed_at <= version.as_of`，并沿用D1覆盖合同的24小时时效，过期明确为`stale`。空候选不伪造发行人，外部原因只允许安全原因码，压缩证据不暴露URL、正文、人工摘要或敏感参数。
- 生命周期与E3专项35项、风险相关整链163项、临时目录隔离下全部`test_radar*.py` 862项通过；Python语法编译和`git diff --check`通过。独立代理三轮对抗复审后最终无P0/P1/P2。
- 本轮没有读取或写入生产SQLite，没有网络或付费来源调用，没有修改依赖、迁移、环境变量、LaunchAgent、API或前端，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交、推送或部署。
- 下一步仍分两条并行：下一有效A股连续交易时段执行C4C-2B免费可交易性真实小样本；风险侧收集真实D2完整页和真实人工审核工件，通过本生命周期形成至少两个连续D8版本。两条都不能用Fixture或代码测试冒充真实来源成功。

## 2026-08-09阶段6风险生命周期受控交付入口

- 新增 `backend/radar/leader_risk_lifecycle_delivery.py`：把冻结候选计划、D2查询窗口和强类型 `LeaderRiskLifecycleBatchEntry` 作为唯一输入；未显式确认、候选顺序错位、窗口不连续、采集时间不可信、页预算非法或transport不可调用时，在任何来源请求前返回`not_run/blocked`。
- D2按七类固定关键词先各取第一页，再按页码和分类顺序在全局预算内轮转补页；单次请求沿用现有20秒超时，不重试。来源失败立即停止，预算不足、响应分页异常及生命周期复验发现的页数、总数、记录数或跨页文档身份漂移均保持`source_unverified`，不得生成风险投影。
- 完整批次只交给既有生命周期模块内部重放D5-D8，再接D9/E1/E3。人工输入不接受松散JSON，也不能覆盖D2文档、D4正文、D3事实、D1事件或正式门；Fixture transport固定不算真实POC。脱敏输出只含计划身份、时间、请求/页/分类计数、稳定原因码和生命周期/投影状态，不含标题、URL、发行人、正文、人工摘要、异常原文或路径。
- 独立审查发现并已按红绿测试修复五类真实性边界：真实采集可晚于候选计划但最多24小时，公告发布时间仍不得晚于计划；查询截止日和生命周期窗口统一按上海业务日；非预期来源异常仅返回稳定失败码且不重试；失败请求不计作已抓取页面；报告中的计划/运行/行情批次ID只输出安全字符白名单值。
- 交付专项12项、直接相关风险链85项、临时目录隔离下全部`test_radar*.py` 875项通过；相关Python语法编译和差异卫生检查通过。独立代理复审确认原1个P1和4个P2全部解决，无新增P0/P1/P2。本轮没有调用网络或付费来源，没有读取或写入生产SQLite，没有修改调度、API、前端、依赖、迁移、环境变量或LaunchAgent，没有停止、重启或重载4000/8001，也没有执行Git暂存、提交、推送或部署。
- 下一步仍是两条真实验收并行：交易窗口内执行C4C-2B最多8只免费可交易性样本；风险侧在明确授权下用本入口采集真实D2完整页，并准备可信机器上下文和至少两个连续人工审核版本。未产生这些真实证据前，风险来源继续保持未就绪和analysis-only。

## 2026-08-12阶段6 D2真实全量验收

- 有效A股连续交易时段内，用全新 `/private/tmp` SQLite生成真实冻结候选计划：385只、13个分片、7类风险；没有读写生产SQLite、没有裁剪候选或人工补位。
- 真实证据确认巨潮按发布时间排序时会在翻页过程中漂移：同一分片两轮总记录数一致，但一轮有4个跨页重复、另一轮无重复且全集相差4条。不得通过盲目去重放行。
- D2官方查询改为按公告唯一ID降序，不改变公告全集和风险语义。真实POC同分片两轮各234条，零重复且完整元数据集合一致。交付器同时保留异常重复时的连续日期切片、两轮集合一致性和全局请求预算失败关闭合同。
- 最终真实D2在238次请求内完成：236个有效页、5365条官方公告，分类完整、分页完整、时间窗连续均为 `true`；一次网络失败按既有规则受控重试后成功。D2官方发现与分页门禁已通过。
- 最终生命周期状态为 `missing`，原因仅为真实人工审核版本为空；D8继续由 `risk_lifecycle_version_history_insufficient` 阻断，不得伪造或复制两个版本。本轮只是隔离验收，没有持久化D2公告或生成可继续复核的生产队列。下一步是先明确并授权真实审核记录的持久化边界，再对新的同轮D2批次开始可追溯人工复核和连续版本积累；不再重复开发D2分页能力来证明同一件事。
- D2四模块87项和后端完整1204项测试全部通过，相关Python语法、`git diff --check`通过。本次临时脚本、结果和SQLite已清理；4000为PID 30112、8001为PID 38620，`/radar` 与 `/docs` 均返回200。没有停止、重启或重载服务，没有修改环境变量或LaunchAgent，没有Git暂存、提交、推送或部署。

## 2026-08-12阶段6风险审核持久化基础

- 保持迁移5 `leader_state_storage` 原名称、SQL和校验和不变；新增独立可选迁移6 `leader_risk_review_storage`。现有 `STAGE6_RADAR_MIGRATIONS` 仍只要求版本1至5，新审核仓储才显式要求版本1至6，未抬高当前阶段6运行仓储门槛。
- 迁移6新增D2审核批次、官方公告目录、批次公告关联、选中审核正文快照/分页文本、人工审核版本六类表。只保存进入人工审核的有限正文页，不保存PDF二进制，也不批量保存5365份公告全文；继续执行最多200页、单页10万字符、总计200万字符上限。
- 新增 `LeaderRiskReviewRepository`：只接受调用方显式提供的已迁移连接；只持久化公告、正文、事件版本、人工事实提交和两层关系审核等可追溯输入。读取时重新执行D3事实提取、待审候选、D5人工工件、D6重放和D7补录关系验证，不直接信任缓存的D8结果或松散JSON。
- 人工审核版本必须直接引用上一版本；数据库触发器禁止更新和删除，只允许追加。同批次ID、公告ID、正文SHA或审核版本出现内容冲突时失败关闭；目录、正文页、批次和版本均有稳定校验和，超限或任一结构损坏整笔回滚/拒绝读取。
- 新仓储专项6项、D3-D8相关72项、迁移与现有龙头仓储35项、后端完整1210项全部通过，`git diff --check`通过。4000仍为PID 30112、8001仍为PID 38620，`/radar`和`/docs`只读返回200；没有停止、重启或重载服务。
- 本轮没有连接、读取或写入生产SQLite，没有应用迁移6到生产，没有新增公开写API，没有Git暂存、提交、推送或部署。D8仍因真实人工审核版本为空而未就绪。下一步需单独授权后，先做生产库在线备份，再仅对复制库执行迁移6、写入/关闭/重开/回读和恢复演练；演练通过后才能讨论生产迁移与真实审核入口。
- 上述复制库演练随后已获用户明确授权并完成。生产源库只通过SQLite `mode=ro`读取和在线备份，基线及最终复核均为迁移版本1至4、`quick_check=ok`、外键问题0、迁移6表0；未对生产原库执行迁移或业务写入。
- 保留备份为 `backend/data/backups/stock_monitor-20260812-140103.db.backup`，大小1,269,137,408字节，SHA-256 `f8018f844549b1e1a3d2e973908a459c84dc90566823880ab411a90b9ac68759`；备份与校验文件权限均为`0600`，独立复算哈希与校验文件一致。
- 仅在 `/private/tmp` 复制库从版本1至4只增应用迁移5、6，写入1个明确的演练批次、1份有限正文和2个连续人工审核版本；关闭重开后强类型回读完全一致，正式标志全为false，更新和删除均被只增触发器阻断，`quick_check/integrity_check=ok`、外键问题0。
- 恢复演练副本与保留备份哈希完全一致，重开后仍为版本1至4、没有迁移6表，完整性与外键检查通过。临时脚本、迁移副本和恢复副本均已删除；4000保持PID 30112、8001保持PID 38620，`/radar`和`/docs`返回200。本轮仍未重载服务、未Git暂存/提交/推送、未部署。

## 2026-08-12阶段6生产迁移与真实D2审核批次

- 用户明确授权后，生产SQLite先完成新的在线备份，再只增应用迁移5、6；未覆盖既有数据。最终迁移版本为1至6，`quick_check/integrity_check=ok`、外键问题0。迁移前保留备份 `backend/data/backups/stock_monitor-20260812-140730.db.backup`，SHA-256 `52de5a433c1148a287a9a4051d29314ffa0bac01b2cbba20185b7ade69db60dc`；真实D2写入前再保留备份 `backend/data/backups/stock_monitor-20260812-142429.db.backup`，SHA-256 `f8cee282ef493a915eb065f1e37a38f88e24d75f1cdc7367160382d698db654b`，备份及校验文件权限均为`0600`。
- 8001通过既有LaunchAgent受控重载两次，最终PID 837；4000保持PID 30112。阶段6和阶段5开关均生效。迁移6出现后，旧ETF/龙头仓储曾把已知后续版本误判为漂移；迁移校验现允许当前规范迁移序列的已知后续版本，同时保持自定义迁移链隔离。后端完整1210项通过，当前ETF自然任务已在新进程成功执行，接口不再返回存储未就绪。
- 新增 `leader_risk_review_persistence.py`，只接受真实交付标记、同一冻结候选计划和强类型生命周期结果；候选身份、发行人作用域、七类、全分片分页、连续窗口、来源合同、文档类别/查询词和计数均在写入前重验。相同真实批次幂等，不完整或篡改结果不写库，且本入口绝不生成D8人工审核版本。
- 巨潮逐证券发行人搜索在一次6路并发后触发短时HTTP 403；两轮均在D2前失败关闭且未写审核数据。随后新增巨潮官方全量证券名册批量解析：单次读取官方 `szse_stock.json`，只接受`category=A股`、精确代码和唯一安全`orgId`，按候选原顺序返回作用域；候选缺失、重复或身份冲突整批拒绝。该官方名册现场含6152只A股并完整覆盖本轮385只候选。
- 14:35连续交易时段重新冻结真实候选计划：扫描7449项、映射5155项、候选385只、13片，`candidatePlanId=radar-leader-runtime-candidate-plan-v1:3ba109dc170b52e15e070067632bf2836a7dba508c4eb5c20799ded66f042f3e`。官方名册作用域385/385就绪后，真实D2以910次上限运行，实际128次请求、127个有效页；一次连接失败只做一次受控重试并成功，七类完整、分页完整、时间窗连续全部为`true`。
- 真实审核批次已写入生产：`reviewBatchId=radar-leader-risk-review-batch-v1:be8b20e29eb5ae6155880d9e2dccf96cfd4dd31b5072915edf30198e7b163af7`，候选385只、官方公告1919份、公告关联1919条。关闭重开后1919份强类型元数据完整回读；正文快照0、人工审核版本0，因此D8继续未就绪，不得把D2成功写成龙头正式可用。
- 新桥接/名册/生命周期/仓储专项58项和后端完整1216项通过，Python语法与`git diff --check`通过。4000/8001监听正常，`/radar`、`/docs`、`/api/radar/overview`、`/api/radar/leaders`均返回200；现有龙头只读接口仍显示`candidate_snapshot_missing`，因为尚未接入审核队列。没有停止服务、没有付费AI、没有Git暂存/提交/推送或部署。
- 下一步不再重复D2采集：先为最新真实审核批次增加内部只读队列/API，只暴露脱敏元数据和分页；再按有限选择下载正文、形成可追溯人工审核版本。只有同一公告至少两个合法连续人工版本并通过D3-D8重放后，才讨论龙头候选投影和页面展示。

## 2026-08-12阶段6 D2只读审核队列上线

- 已为最新真实审核批次增加只读仓储摘要、分页查询和API；`GET /api/radar/leaders/review-queue` 使用 `radar-leader-review-queue-v1` 合同，不提供任何写入口。`/api/radar/leaders` 同步暴露 `reviewQueue`，但龙头主状态继续保持未就绪。
- 用户明确授权后，15:26通过既有LaunchAgent受控重载8001；运行资产归档于 `/Users/linjian/Library/LaunchAgents/stock-monitor-archive/20260812-152624-radar-2b6b2b`。新后端PID 7316，阶段5 ETF与阶段6龙头开关均为`true`，ngrok未停止。
- 重载后真实只读核对：`/docs`、`/api/radar/leaders`、`/api/radar/leaders/review-queue?limit=2&offset=0`均返回200。审核队列为`ready`：候选385只、官方公告1919份、关联1919条、分页返回2/1919；七类完整、分页完整、时间窗连续均为`true`。
- D8仍严格未就绪：正文快照0、已审核公告0、人工审核版本0，原因码为`d8_review_versions_missing`，`formalUsable=false`。D2队列可读不等于正式龙头可用，当前龙头快照仍因`candidate_snapshot_missing`为空。
- 前端已接入D2审核队列摘要，ESLint、TypeScript和Next.js生产构建通过；后端队列/API/持久化针对性29项通过，`git diff --check`通过。验收时发现4000在本次受控重载前已无监听，未在本次仅授权重载8001的范围内擅自启动，因此真实浏览器页面验收仍待4000恢复后完成。
- 下一步先单独确认并恢复固定前端4000，只做真实桌面/手机页面验收；通过后再进入有限正文选择和D8人工审核版本积累。不得重复D2采集，不得伪造人工版本，不得把D2成功写成正式龙头状态。

## 2026-08-12阶段6 D8只读审核工作台上线

- 在既有D2只读队列上新增单条公告审核状态接口：`GET /api/radar/leaders/review-queue/document`。接口严格绑定当前真实批次、公告ID和风险类别，只返回官方公告元数据、正文快照状态及D8版本计数；旧批次或类别错配失败关闭，不抓取正文、不提供写入口。
- 前端“龙头梯队”增加风险公告审核工作台：每页8条，支持只读翻页、查看单条审核状态和跳转巨潮官方公告；明确展示“正文未抓取”“D8版本 0”“正式可用 否”，不把D2队列就绪冒充龙头正式可用。
- 后端相关30项、后端完整1219项、前端ESLint、TypeScript、Next.js生产构建和`git diff --check`均通过。
- 用户明确授权后，21:04通过既有LaunchAgent受控重载8001；运行资产归档于 `/Users/linjian/Library/LaunchAgents/stock-monitor-archive/20260812-210453-radar-2b6b2b`。新后端PID 29817，前端4000保持原PID 10807，ngrok未停止。
- 现场只读验收通过：`/docs`、`/radar`、龙头接口、审核队列、单条详情及前端代理均返回200；旧批次详情返回503。真实队列仍为候选385只、官方公告1919份、正文快照0、人工审核版本0、`formalUsable=false`。
- 应用内浏览器验收通过：工作台显示`1–8 / 1919`，单条详情显示“尚未抓取 / D8人工版本 0 / 正式可用 否”，下一页显示`9–16 / 1919`；页面无横向溢出，日志只有React开发提示和HMR连接，没有业务错误。
- 下一步是有限选择真实公告并受控抓取正文快照，然后形成第一个可追溯人工审核版本；该步骤涉及外部网络和生产SQLite业务写入，实施前必须先说明选择范围、备份、写入和回退方案并单独取得授权。形成首版后还需等待下一合法连续审核版本，D8当前仍未就绪。

## 2026-08-13阶段6 D8首批真实正文快照

- 新增 `leader_risk_content_delivery.py` 和受控命令入口：只接受当前完整D2批次中明确选择的最多3份公告，先完成全部巨潮官方PDF下载和内存解析，再用单个SQLite事务原子保存；批次、类别、来源、时钟、下载或持久化任一失败均不生成部分写入。入口不生成事件、人工结论、D8版本或正式状态，已有快照重复执行不再调用来源。
- `LeaderRiskReviewRepository` 新增多正文快照原子保存；继续复用既有官方域名、无重定向、单份20秒且不重试、10MB、200页、单页10万字符、总计200万字符、身份和哈希校验。备份工具新增备份文件与校验文件默认`0600`权限，并补回归测试。
- 用户明确继续后，写入前创建生产SQLite在线备份 `backend/data/backups/stock_monitor-20260812-234911.db.backup`，大小1,298,722,816字节，SHA-256 `d17738ac26d506c36a66afc46b4a27c4581278e96a06c2967e4cfe347c735992`；独立复算一致，备份和校验文件均为`0600`，`quick_check=ok`、外键问题0、迁移版本1至6。
- 首批真实选择覆盖解禁、业绩、诉讼三类，三份巨潮官方PDF全部成功：共3个正文快照、19页，单份分别为8页/161261字节、3页/141278字节、8页/192461字节。没有保存PDF二进制，没有重试，没有失败公告或部分事务。
- 关闭重开生产库后 `quick_check/integrity_check=ok`、外键问题0；正文快照3、正文页19、人工审核版本0。只读API摘要已更新为`contentSnapshotCount=3`、`reviewVersionCount=0`、`formalUsable=false`。
- 浏览器验收显示三份目标公告为“正文已抓取”，单条详情为“1个快照 / D8人工版本0 / 正式可用否”；页面无横向溢出，日志仅React开发提示和HMR连接。工作台文案调整为“只读审核状态 · 页面不触发抓取”。
- 新专项与相关27项、后端完整1223项、前端ESLint、TypeScript、Next.js生产构建和`git diff --check`均通过。4000保持PID 10807、8001保持PID 29817，本轮未停止、重启或重载服务，没有付费AI、Git暂存、提交、推送或部署。
- 下一步不是继续自动抓取更多公告，而是基于这3份真实正文建立受控人工审核入口和第一个真实D8版本。人工事实、关联、事件状态和有效期必须由用户确认或明确人工操作，不能由AI代填；首版完成后仍需下一合法时点形成直接引用首版的第二个连续版本，才能进入D8重放和风险投影。

## 2026-08-13阶段6 D8人工审核入口本地完成

- 新增只读人工审核表单接口 `GET /api/radar/leaders/review-queue/review-form` 和受后端Token保护的提交接口 `POST /api/radar/leaders/review-queue/review-version`。表单只接受当前完整真实批次、已保存正文和 `fact_extraction_missing` 首版候选；服务端按当前审核时点重新执行D3事实提取，不信任旧候选缓存。
- 提交服务严格校验正文SHA、页码和原文片段、类别必需事实、事件身份与时序、关系类型、上一版本直接引用及D5-D7重放。公司公告编号可以作为真实原文证据，并通过独立 `mappedDocumentId` 显式映射到目标巨潮 `cninfo:` 文档身份；不能把公司公告编号直接伪装成巨潮内部ID。
- 前端D8工作台已接入真实分页正文预览和结构化人工表单，包含事实、目标事件、有效期、关系、审核摘要与人工确认；不会由AI自动填写。写开关 `RADAR_LEADER_D8_REVIEW_WRITE_ENABLED` 默认及示例配置均为 `false`，关闭时提交接口固定拒绝写入。
- 临时SQLite已覆盖首版到直接引用上一版的第二版连续写入/关闭/重开/回读，以及哈希、原文片段和公司公告编号映射拒绝/通过分支。相关专项30项、后端完整1236项、前端ESLint、TypeScript、Next.js生产构建、Python编译和 `git diff --check` 全部通过。
- 当前固定服务正常：4000为PID 70998、8001为PID 29817，前端首页和 `/docs` 均返回200。8001仍是本次修改前的旧进程，因此新D8表单路由现场返回404；本轮未停止、重启或重载服务，未修改生产配置，未写入生产SQLite，人工审核版本仍为0，也没有Git暂存、提交、推送或部署。
- 下一步需用户单独授权后受控重载8001，并保持D8写开关为 `false`，先完成 `/radar` 三份真实正文的只读浏览器验收。只读验收通过后，再单独说明生产备份、写入和回退方案，申请开启写开关及录入第一个真实人工版本；首版完成仍不代表D8就绪，必须在下一合法审核时点形成第二个连续版本。

## 2026-08-13阶段6 D8入口受控重载与只读验收完成

- 用户确认后通过 `ops/launchd/manage.sh enable-leader-stage6` 完成8001受控重载；旧运行资产归档于 `/Users/linjian/Library/LaunchAgents/stock-monitor-archive/20260813-115631-radar-2b6b2b`。8001新PID为90365，4000保持PID 70998，ngrok未停止；阶段5 ETF和阶段6龙头运行开关均保持 `true`，D8写入开关未配置，继续使用默认 `false`。
- 只读接口现场验收通过：三份已保存真实正文分别为解禁8页、业绩3页、诉讼8页；三类表单均返回200，必填事实分别为 `referenced_document_id`、`referenced_document_id + reporting_period`、`referenced_document_id + case_id`；均返回 `writeEnabled=false`、`writeReasonCode=d8_manual_review_write_disabled`、`formalUsable=false`、`reviewVersionCount=0`。
- 写入口未带后端Token返回401；前端服务代理读取表单返回200。`/`、`/radar`、`/docs`均返回200。应用内浏览器验收确认真实正文页、表单字段、只读提示、禁用提交按钮均可见，视口宽884像素时文档宽870像素，无横向溢出。
- 现场发现并修复前端状态语义错误：LaunchAgent阶段6已启用但页面硬编码显示“阶段6未启用”。现在状态栏显示“阶段6已启用 · 未准备”，总览显示“阶段6影子入口已启用”，并把D2完整队列、D8版本不足和候选快照缺失分开表达；正式状态仍明确为关闭/未就绪。
- 前端ESLint、TypeScript、Next.js生产构建和 `git diff --check` 在修复后全部通过；后端此前全量1236项仍通过。本轮没有开启D8写入、没有写生产数据库、没有付费AI、没有Git暂存/提交/推送/部署。
- 下一步不再继续改只读入口：由人工逐页确认3份真实正文中的原公告、事件身份、状态和有效期，形成第一个真实D8版本；这涉及生产SQLite写入，必须另行说明备份/回退并单独授权。首版完成后仍需下一合法审核时点形成直接引用上一版的第二个连续版本，D8才可能通过。

## 2026-08-13阶段6 D8第一个真实人工版本

- 写入前创建生产SQLite在线备份 `backend/data/backups/stock_monitor-20260813-140512.db.backup`，大小1,366,106,112字节，SHA-256 `46bd2c60c7db5c9b0ee4d1328f25342a62635273ca9d7375ba563c32d7d7cd51`；独立哈希、`quick_check`、`integrity_check`和外键检查均通过，备份及校验文件权限为`0600`。
- 新增独立LaunchAgent运行资产 `com.linjian.stock-monitor.fastapi.leader-d8-write-enabled.plist` 及 `enable-leader-d8-write` / `disable-leader-d8-write` 管理命令；普通阶段6运行资产继续固定关闭D8写入。LaunchAgent、雷达合同和数据完整性专项122项通过，运行资产验证和 `git diff --check` 通过。
- 用户逐项确认后写入第一个真实版本：当前公告 `cninfo:1225468495`（603055台华新材），正文第3页证据 `编号：2024-072` 映射到原公告 `cninfo:1220848442`；事件类型为 `share_unlock`，官方状态为 `completed`，关系为 `resolves`，事实为第二个解除限售期届满且1,428,900股于2026-08-17上市流通。
- 生产回读确认 `manual-review-v1` 仅有1条，审核公告数1、人工版本数1；事件、事实补录和生命周期关系与确认内容一致。页面“龙头梯队”同步显示 `D8 人工版本 1` 和该公告 `D8版本 1`，没有重复提交。
- 写入后立即恢复普通阶段6运行资产，D8写开关回到 `false`，表单返回 `writeReasonCode=d8_manual_review_write_disabled`；4000为PID 99804、8001为PID 1705，首页、`/radar`和`/docs`均返回200，ngrok未停止。
- 写入后在线备份为 `backend/data/backups/stock_monitor-20260813-142331.db.backup`，大小1,375,776,768字节，SHA-256 `678898d6285f6e8cf3f6d5d3d8416e2b344e6c4b7fdfda7444833ef1f3b7cfc8`；独立哈希一致，`quick_check/integrity_check=ok`、外键问题0，离线回读确认首版完整。
- D8仍为 `formalUsable=false`，原因仍是连续版本不足；下一步必须在新的合法审核时点形成直接引用 `manual-review-v1` 且具有实质证据变化的 `manual-review-v2`，不得复制首版凑版本。完成第二版后再执行D3-D9重放、风险投影和龙头候选快照验收。本轮没有Git暂存、提交、推送或部署。

## 2026-08-13阶段6 D8重复版本门禁修复

- 用写入后的生产备份副本演练发现，原D9会把新的审核版本号、工件ID、关系ID和映射版本误算为“实质证据变化”，导致原样复制 `manual-review-v1` 也能形成两个证据包并通过D9；该路径虽满足旧测试，但不满足真实有效底线，因此没有写入生产。
- D9现在仍完整输出全部版本差异，但实质变化只认可事实增删、关系类型、替代事件、官方状态、官方发布时间、关系依据事实及正式门禁缺口变化；仅改变审核时间、版本号或内部ID固定返回 `risk_evidence_bundle_audit_material_change_missing`。
- D8提交服务同步增加落库前门禁：第二版若与上一版的人工事实、关系语义和官方事件状态完全相同，直接拒绝并返回“第二个人工审核版本缺少实质证据变化”，避免无效版本污染只增生产链。
- 新增两项回归测试，并把旧生命周期Fixture改为首版只有必要人工事实、第二版新增真实页内事实的连续链；D9专项9项、D8提交专项13项、D8-D9及生命周期联合92项、后端完整1238项全部通过。生产备份副本实测重复第二版已在写入前被拒绝。
- 本轮没有开启D8写权限、没有写生产SQLite、没有重载服务；4000/8001继续运行。当前 `manual-review-v1` 保持唯一且不可修改，D8继续 `formalUsable=false`。下一步只能在出现可追溯的新增事实、官方状态变化、正式更正/替代关系或其他合法门禁变化后录入 `manual-review-v2`；不得用复核措辞或换审核ID凑版本。

## 2026-08-13阶段6 D8-D9持久化版本只读重放诊断

- `leader_risk_review_service.py` 新增纯内存只读诊断：对仓储已反序列化的人工审核版本重建D5工件、D6研究视图、D7关系、D8证据包并执行D9版本差异审计；不连接外部数据源、不写库、不改变正式状态。
- 既有 `GET /api/radar/leaders/review-queue/review-form` 新增强类型 `replayDiagnostic`，固定返回重放状态、审核版本数、成功重建证据包数、当前版本、实质变化需求和稳定原因码，`formalUsable` 继续固定为false。零版本明确为历史不足；单版本可重建1个D8包，但D9明确为 `risk_evidence_bundle_audit_history_insufficient`；两个有实质变化的版本才返回 `ready`。
- 前端D8审核工作台已接入该诊断，直接展示“连续证据链可重放/版本链不完整/校验失败”、已重建证据包数及中文阻塞原因；当前只有 `manual-review-v1` 时会明确提示还需一份包含实质证据变化的后续版本，不再只显示泛化空状态。
- 新增2项重放诊断回归并按先红后绿执行；D8提交服务整文15项、D5-D9相关79项、雷达API/数据完整性122项、后端完整1240项全部通过。前端ESLint、TypeScript、Next.js生产构建、Python语法和 `git diff --check` 全部通过。
- 本轮没有读写生产SQLite，没有开启D8写权限，没有停止、重启或重载4000/8001，也没有Git暂存、提交、推送或部署。当前运行进程尚未加载本地新合同和页面；下一步需用户单独授权受控重载后，再做只读API与浏览器验收，不需要等待新交易日。
- 用户随后明确确认受控重载与只读验收。8001通过 `enable-leader-stage6` 重载，旧运行资产归档于 `/Users/linjian/Library/LaunchAgents/stock-monitor-archive/20260813-172709-radar-2b6b2b`；新后端PID 14792，阶段5 ETF和阶段6龙头开关均为true，D8写入环境变量继续未配置并按默认false执行，ngrok未停止。4000按原 `stock-monitor-frontend` screen生产命令重启，新前端PID 14928。
- 主机侧真实只读表单返回200：`cninfo:1225468495` 已有1个人工版本，成功重建1个D8证据包，活跃版本为 `manual-review-v1`，下一版为 `manual-review-v2`；D9状态为 `missing`，唯一原因为 `risk_evidence_bundle_audit_history_insufficient`，`materialChangeRequired=true`。`writeEnabled=false`、`formalUsable=false`保持不变。
- 应用内浏览器真实验收通过：龙头页面显示D2候选385只、官方公告1919份、已审1份和D8版本1份；603055工作项显示“正文已抓取 / D8版本1”。点开后页面明确显示“已重建1/1个证据包，还需一份包含实质证据变化的后续版本”，`manual-review-v2` 提交按钮仍禁用。884像素和手机宽度均无横向溢出，浏览器控制台无错误。
- 验收期间两次受限命令环境内的localhost `curl` 报连接失败；主机侧证据确认8001未崩溃、LaunchAgent `runs=1`且持续返回200，根因是命令沙箱本机网络隔离，不是生产服务故障。本次未手工触发D8或付费AI，未执行Git暂存、提交、推送或部署。
